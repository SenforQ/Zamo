
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let appRecordingUrl:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "More attractive photo more calls" :*/
fileprivate let mainSunKey:[UInt8] = [0x5a,0x78,0x65,0x72,0x37,0x76,0x63,0x63,0x65,0x76,0x74,0x63,0x7e,0x61,0x72,0x37,0x67,0x7f,0x78,0x63,0x78,0x37,0x7a,0x78,0x65,0x72,0x37,0x74,0x76,0x7b,0x7b,0x64]

private func analysisLocalYellow(burn num: UInt8) -> UInt8 {
    return num ^ 23
}

/*: "Upload any photo you like" :*/
fileprivate let show_processorName:String = "address approval running both tarUploa"
fileprivate let constGoingName:String = " photcoordinate theme written carry that"
fileprivate let noti_relatedFollowKey:String = "lstandarde"

/*: "F4F4F4" :*/
fileprivate let userWeeklyEventValue:String = "FarrangeFarrangeFarrange"

/*: "btn_add_head_cover_nor" :*/
fileprivate let k_lensHistoryContent:String = "btn_adforth lay person field"
fileprivate let show_phaseUrl:String = "mistake equally bass chemistry eye_cov"
fileprivate let noti_installPath:[Character] = ["o","r"]

/*: "Please upload a clear and beautiful photo of yourself" :*/
fileprivate let constMilitaryMidNowText:[UInt8] = [0x7c,0x40,0x49,0x4d,0x5f,0x49,0xc,0x59,0x5c,0x40,0x43,0x4d,0x48,0xc,0x4d,0xc,0x4f,0x40,0x49,0x4d,0x5e,0xc,0x4d,0x42,0x48,0xc,0x4e,0x49,0x4d,0x59,0x58,0x45,0x4a,0x59,0x40,0xc,0x5c,0x44,0x43,0x58,0x43,0xc,0x43,0x4a,0xc,0x55,0x43,0x59,0x5e,0x5f,0x49,0x40,0x4a]

private func selectTitle(via num: UInt8) -> UInt8 {
    return num ^ 44
}

/*: "No violence, pornography, or politics allowed" :*/
fileprivate let mainOldKitListenText:[UInt8] = [0x64,0x45,0xa,0x5c,0x43,0x45,0x46,0x4f,0x44,0x49,0x4f,0x6,0xa,0x5a,0x45,0x58,0x44,0x45,0x4d,0x58,0x4b,0x5a,0x42,0x53,0x6,0xa,0x45,0x58,0xa,0x5a,0x45,0x46,0x43,0x5e,0x43,0x49,0x59,0xa,0x4b,0x46,0x46,0x45,0x5d,0x4f,0x4e]

private func sumLibrary(timing num: UInt8) -> UInt8 {
    return num ^ 42
}

/*: "Next" :*/
fileprivate let user_perTitle:String = "po floor number calculate mouthNext"

/*: "edit_head_Image_male_ :*/
fileprivate let mainSupportId:[Character] = ["e","d","i","t","_"]
fileprivate let show_priorityKey:String = "hetip"
fileprivate let k_fragmentKey:String = "on past leading interest resolution_Image"

/*: "edit_head_Image_ :*/
fileprivate let k_meetingNobodyContent:String = "edit_comment inside"
fileprivate let user_literalFormat:[Character] = ["m","a","g","e","_"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DriveThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2024/5/24.
//

//: import UIKit
import UIKit

//: class TalkingLoginEditPhotoView: UIView {
class DriveThen: UIView {
    //: typealias InfoActionBlock = (_ type: EditInfoType) -> Void
    typealias InfoActionBlock = (_ type: BlankOutPackCharacteristicInfoType) -> Void

    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()
    //: var btnBlock: InfoActionBlock?
    var btnBlock: InfoActionBlock?

    //: private var nameCount: Int = 20
    private var nameCount: Int = 20

    //: override init(frame: CGRect) {
    override init(frame: CGRect) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: self.backgroundColor = UIColor.white
        self.backgroundColor = UIColor.white

        //: self.setupSubviews()
        self.result()
        //: self.setupSubViewsConstraint()
        self.database()
        //: self.bindInteraction()
        self.admin()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: appRecordingUrl.reversed(), encoding: .utf8)!)
    }

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let contentView = UIView.init()
        let contentView = UIView()
        //: return contentView
        return contentView
        //: }()
    }()

    //: lazy var tipsLab: UILabel = {
    lazy var tipsLab: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 17)
        label.font = .landEnableSize(type: .Semibold, fontSize: 17)
        //: label.textColor = .appTitleColor()
        label.textColor = .roundChromaticColor()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: if MentionTriggerAppManager.share.userFillInfoMode.sex == Gender.female.rawValue {
        if MentionTriggerAppManager.share.userFillInfoMode.sex == TopicKeyRepresentable.female.rawValue {
            //: label.text = "More attractive photo more calls".localized
            label.text = String(bytes: mainSunKey.map{analysisLocalYellow(burn: $0)}, encoding: .utf8)!.localized
            //: } else {
        } else {
            //: label.text = "Upload any photo you like".localized
            label.text = (String(show_processorName.suffix(5)) + "d any" + String(constGoingName.prefix(5)) + "o you " + noti_relatedFollowKey.replacingOccurrences(of: "standard", with: "ik")).localized
        }
        //: return label
        return label
        //: }()
    }()

    //: lazy var addHeadButton: UIButton = {
    lazy var addHeadButton: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.backgroundColor = UIColor(hex: "F4F4F4")
        btn.backgroundColor = UIColor(hex: (userWeeklyEventValue.replacingOccurrences(of: "arrange", with: "4")))
        //: btn.layer.cornerRadius = 5
        btn.layer.cornerRadius = 5
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true

        //: let headImg = MentionTriggerAppManager.share.userFillInfoMode.headImage ?? nil
        let headImg = MentionTriggerAppManager.share.userFillInfoMode.headImage ?? nil
        //: if headImg != nil {
        if headImg != nil {
            //: btn.setImage(headImg, for: .normal)
            btn.setImage(headImg, for: .normal)
            //: } else {
        } else {
            //: btn.setImage(UIImage.BundleImageNamed(name: "btn_add_head_cover_nor"), for: .normal)
            btn.setImage(UIImage.lastTo(name: (String(k_lensHistoryContent.prefix(6)) + "d_head" + String(show_phaseUrl.suffix(4)) + "er_n" + String(noti_installPath))), for: .normal)
        }
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var msgLab: UILabel = {
    lazy var msgLab: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 15)
        label.font = .landEnableSize(type: .Regular, fontSize: 15)
        //: label.textColor = .appValueColor()
        label.textColor = .todaySkinColor()
        //: if MentionTriggerAppManager.share.userFillInfoMode.sex == Gender.female.rawValue {
        if MentionTriggerAppManager.share.userFillInfoMode.sex == TopicKeyRepresentable.female.rawValue {
            //: label.text = "Please upload a clear and beautiful photo of yourself".localized
            label.text = String(bytes: constMilitaryMidNowText.map{selectTitle(via: $0)}, encoding: .utf8)!.localized
            //: } else {
        } else {
            //: label.text = "No violence, pornography, or politics allowed".localized
            label.text = String(bytes: mainOldKitListenText.map{sumLibrary(timing: $0)}, encoding: .utf8)!.localized
        }
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: return label
        return label
        //: }()
    }()

    //: lazy var commitButton: UIButton = {
    lazy var commitButton: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        btn.titleLabel?.font = UIFont.landEnableSize(type: .Medium, fontSize: 17)
        //: btn.setTitle("Next".localized, for: .normal)
        btn.setTitle((String(user_perTitle.suffix(4))).localized, for: .normal)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: ScreenWidth-60, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.tillOfCreation(colors: UIColor.spectralColor(), size: CGSize(width: show_objectValue - 60, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Layout

//: extension TalkingLoginEditPhotoView {
extension DriveThen {
    // 添加视图
    //: private func setupSubviews() {
    private func result() {
        //: addSubview(contentView)
        addSubview(contentView)
        //: contentView.addSubview(tipsLab)
        contentView.addSubview(tipsLab)
        //: contentView.addSubview(addHeadButton)
        contentView.addSubview(addHeadButton)
        //: contentView.addSubview(commitButton)
        contentView.addSubview(commitButton)
        //: contentView.addSubview(msgLab)
        contentView.addSubview(msgLab)
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func database() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.leading.equalTo(24)
            make.leading.equalTo(24)
            //: make.height.equalTo(28)
            make.height.equalTo(28)
            //: make.top.equalTo(34)
            make.top.equalTo(34)
        }

        //: addHeadButton.snp.makeConstraints { make in
        addHeadButton.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.width.height.equalTo(159)
            make.width.height.equalTo(159)
            //: make.top.equalTo(tipsLab.snp.bottom).offset(34)
            make.top.equalTo(tipsLab.snp.bottom).offset(34)
        }

        //: let imgWidth: CGFloat = (ScreenWidth - 76) / 3.0
        let imgWidth: CGFloat = (show_objectValue - 76) / 3.0
        //: let imgHeight: CGFloat = imgWidth
        let imgHeight: CGFloat = imgWidth

        //: for i in 0 ..< 3 {
        for i in 0 ..< 3 {
            //: let imageView = UIImageView.init()
            let imageView = UIImageView()
            //: if MentionTriggerAppManager.share.userFillInfoMode.sex == "1" {
            if MentionTriggerAppManager.share.userFillInfoMode.sex == "1" {
                //: imageView.image = UIImage.BundleImageNamed(name: "edit_head_Image_male_\(i)")
                imageView.image = UIImage.lastTo(name: (String(mainSupportId) + show_priorityKey.replacingOccurrences(of: "tip", with: "ad") + String(k_fragmentKey.suffix(6)) + "_male_") + "\(i)")
                //: } else {
            } else {
                //: imageView.image = UIImage.BundleImageNamed(name: "edit_head_Image_\(i)")
                imageView.image = UIImage.lastTo(name: (String(k_meetingNobodyContent.prefix(5)) + "head_I" + String(user_literalFormat)) + "\(i)")
            }
            //: contentView.addSubview(imageView)
            contentView.addSubview(imageView)
            //: imageView.snp.makeConstraints { make in
            imageView.snp.makeConstraints { make in
                //: make.top.equalTo(addHeadButton.snp.bottom).offset(30)
                make.top.equalTo(addHeadButton.snp.bottom).offset(30)
                //: make.leading.equalTo(30 + (imgWidth+8)*CGFloat(i))
                make.leading.equalTo(30 + (imgWidth + 8) * CGFloat(i))
                //: make.width.equalTo(imgWidth)
                make.width.equalTo(imgWidth)
                //: make.height.equalTo(imgHeight)
                make.height.equalTo(imgHeight)
            }
        }
        //: msgLab.snp.makeConstraints { make in
        msgLab.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.top.equalTo(addHeadButton.snp.bottom).offset(146)
            make.top.equalTo(addHeadButton.snp.bottom).offset(146)
        }

        //: commitButton.snp.makeConstraints { make in
        commitButton.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
            //: make.top.equalTo(msgLab.snp.bottom).offset(30)
            make.top.equalTo(msgLab.snp.bottom).offset(30)
        }
    }

    // 添加事件
    //: private func bindInteraction() {
    private func admin() {
        //: commitButton.rx.tap
        commitButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if self.btnBlock != nil {
                if self.btnBlock != nil {
                    //: self.btnBlock!(.Finish)
                    self.btnBlock!(.Finish)
                }
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: addHeadButton.rx.tap
        addHeadButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if self.btnBlock != nil {
                if self.btnBlock != nil {
                    //: self.btnBlock!(.HeadPic)
                    self.btnBlock!(.HeadPic)
                }
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
