
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let app_bulletListDeadlineData:[UInt8] = [0xc5,0xc2,0xc5,0xd8,0x84,0xcf,0xc3,0xc8,0xc9,0xde,0x96,0x85,0x8c,0xc4,0xcd,0xdf,0x8c,0xc2,0xc3,0xd8,0x8c,0xce,0xc9,0xc9,0xc2,0x8c,0xc5,0xc1,0xdc,0xc0,0xc9,0xc1,0xc9,0xc2,0xd8,0xc9,0xc8]

private func againstAssistant(sample num: UInt8) -> UInt8 {
    return num ^ 172
}

/*: "Load..." :*/
fileprivate let userPerFormat:String = "reader pair basic nothingLo"
fileprivate let constInfoSpecialName:String = "stick each during control simultaneouslyad..."

/*: "Skip" :*/
fileprivate let constMethodKey:String = "block curriculum coordinator menu largeSkip"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let kBodyFormat:[UInt8] = [0x7b,0x47,0x47,0x43,0x67,0x5c,0x5c,0x5f,0x1d,0x7d,0x52,0x47,0x5a,0x45,0x56,0x67,0x5c,0x79,0x40,0x1b,0x14,0x41,0x56,0x50,0x5b,0x52,0x41,0x54,0x56,0x14,0x1a]

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let notiDataPopId:[UInt8] = [0x3b,0x29,0x28,0x77,0x6f,0x68,0x53,0x65,0x67,0x61,0x50,0x6e,0x6f,0x26,0x26,0x77,0x6f,0x68,0x53,0x65,0x67,0x61,0x50,0x6e,0x6f,0x2e,0x77,0x6f,0x64,0x6e,0x69,0x77]

/*: "jsEvent(onPageShow):  :*/
fileprivate let k_interestedValue:[Character] = ["j","s","E","v","e","n","t"]
fileprivate let constPauseMsg:String = "mask particle source(onPa"
fileprivate let constCeremonyFormat:String = "jungle learn stick segment priceow): "

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let userFlushText:[UInt8] = [0xbb,0xad,0xb2,0xa8,0xb3,0xbb,0x72,0xb3,0xb2,0x94,0xa5,0xab,0xa9,0x8c,0xad,0xa8,0xa9,0x6a,0x6a,0xb3,0xb2,0x94,0xa5,0xab,0xa9,0x8c,0xad,0xa8,0xa9,0x6c,0x6d,0x7f]

fileprivate func executeTing(item num: UInt8) -> UInt8 {
    let value = Int(num) + 188
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "jsEvent(onPageHide):  :*/
fileprivate let userMightStr:String = "bit browjsEv"
fileprivate let main_weeklyHimCooperativeFormat:[Character] = ["P"]
fileprivate let constFormData:String = "ageHbulb dialog fraction communicate load"

/*: "title" :*/
fileprivate let constHomeId:String = "titstaff"

/*: "canGoBack" :*/
fileprivate let main_earlyKey:String = "file simultaneously modestcanGoBa"
fileprivate let notiRemoveText:String = "generate"

/*: "PHPSESSID= :*/
fileprivate let appAssetId:String = "Pscene"
fileprivate let const_securityName:String = "shift shall single findPSESSID="

/*: ";uid= :*/
fileprivate let user_groupValue:[Character] = [";","u","i","d","="]

/*: "Cookie" :*/
fileprivate let appConStr:String = "Cookiesnap literal alliance"

/*: "nav_back_black_nor" :*/
fileprivate let show_othersId:[Character] = ["n","a","v","_","b","a","c","k"]
fileprivate let mainPetMsg:String = "behavior indicator into_black"

/*: "ico_intimate_list_sm" :*/
fileprivate let const_maybeJoinName:[Character] = ["i","c","o","_","i","n","t","i","m","a","t","e","_","l","i"]
fileprivate let kApprovalPath:[Character] = ["s","t","_","s","m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let appTheFormat:[UInt8] = [0xb1,0xba,0xb6,0xa0,0xb8,0xb0,0xbb,0xa1,0xfb,0xb2,0xb0,0xa1,0x90,0xb9,0xb0,0xb8,0xb0,0xbb,0xa1,0xa6,0x97,0xac,0x81,0xb4,0xb2,0x9b,0xb4,0xb8,0xb0,0xfd,0xf2,0xb7,0xba,0xb1,0xac,0xf2,0xfc,0x8e,0xe5,0x88,0xfb,0xa6,0xa1,0xac,0xb9,0xb0,0xfb,0xb7,0xb4,0xb6,0xbe,0xb2,0xa7,0xba,0xa0,0xbb,0xb1,0xe8,0xf2,0xa7,0xb2,0xb7,0xb4,0xfd,0xe5,0xf9,0xe5,0xf9,0xe5,0xf9,0xe5,0xfc,0xf2]

/*: "Alert" :*/
fileprivate let app_photoCalledUrl:String = "bold transmission boundary reference smoothAlert"

/*: "Cancle" :*/
fileprivate let showRangeStr:String = "throw observe existCancle"

/*: "Done" :*/
fileprivate let appMAdKey:[Character] = ["D","o","n","e"]

/*: "js call method name =  :*/
fileprivate let constWhetherData:[Character] = ["j","s"," ","c","a","l","l"," ","m","e","t","h","o","d"," ","n","a","m","e"," ","="]
fileprivate let main_closeWorkerValue:String = " "

/*: , message =  :*/
fileprivate let k_developingId:[Character] = [","," ","m"]
fileprivate let dataWorldwideOwnerUrl:String = "essage = create auto"

/*: "http://" :*/
fileprivate let constWorldName:String = "http://continue assert inside"

/*: "https://" :*/
fileprivate let constMmPackageTitle:[Character] = ["h","t","t","p","s"]
fileprivate let const_dimensionKey:String = ":telltell"

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let kListenKey:[UInt8] = [0x43,0x54,0x47,0x15,0x56,0x5a,0x5a,0x5e,0x5c,0x50,0x7b,0x54,0x58,0x50,0x46,0x15,0x8,0x15,0x51,0x5a,0x56,0x40,0x58,0x50,0x5b,0x41,0x1b,0x56,0x5a,0x5a,0x5e,0x5c,0x50,0x1b,0x46,0x45,0x59,0x5c,0x41,0x1d,0x12,0xe,0x15,0x12,0x1c,0x1b,0x58,0x54,0x45,0x1d,0x53,0x40,0x5b,0x56,0x41,0x5c,0x5a,0x5b,0x1d,0x56,0x5a,0x5a,0x5e,0x5c,0x50,0x1c,0x15,0x4e,0x15,0x47,0x50,0x41,0x40,0x47,0x5b,0x15,0x56,0x5a,0x5a,0x5e,0x5c,0x50,0x1b,0x46,0x45,0x59,0x5c,0x41,0x1d,0x12,0x8,0x12,0x1c,0x6e,0x5,0x68,0x15,0x48,0x15,0x1c,0xe,0x3f]

private func blockNorth(wall num: UInt8) -> UInt8 {
    return num ^ 53
}

/*: "uid= :*/
fileprivate let kEstimateData:String = "uid=lose pic maker cute decide"

/*: ;domain= :*/
fileprivate let appRewardMessage:String = "treat stop disk;domain="

/*: ;path=/" :*/
fileprivate let show_sayName:[Character] = [";","p","a","t","h","=","/"]

/*: "document.cookie=' :*/
fileprivate let mainEmptyText:[Character] = ["d","o","c","u","m","e","n","t",".","c","o","o","k","i","e","="]
fileprivate let k_middleDueFormat:[Character] = ["\'"]

/*: ;domin= :*/
fileprivate let user_impressionTitle:String = ";domiappropriate spring"
fileprivate let kRegionApplyId:[Character] = ["n","="]

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let user_wonderKey:[UInt8] = [0xb2,0xbd,0xfb,0xf3,0xb8,0xb4,0xb4,0xb0,0xb2,0xbe,0x95,0xba,0xb6,0xbe,0xa8,0xf5,0xb2,0xb5,0xbf,0xbe,0xa3,0x94,0xbd,0xf3,0xfc,0x8b,0x93,0x8b,0x88,0x9e,0x88,0x88,0x92,0x9f,0xfc,0xf2,0xfb,0xe6,0xe6,0xfb,0xf6,0xea,0xf2,0xfb,0xa0,0xfb,0xbf,0xb4,0xb8,0xae,0xb6,0xbe,0xb5,0xaf,0xf5,0xb8,0xb4,0xb4,0xb0,0xb2,0xbe,0xe6,0xfc]

/*: "h5Domain =  :*/
fileprivate let app_resolutionName:String = "shadow minimize match pictureh5Dom"
fileprivate let noti_finishPoStr:String = "never "

/*: "getAllCookies =  :*/
fileprivate let userSatisfyShakeMessage:String = "feature brush chance view positivegetA"
fileprivate let app_interestPath:String = "cookie bounce layer broadcast followingokies ="
fileprivate let constSkipValue:String = "limited"

/*: "PHPSESSID" :*/
fileprivate let kPositiveId:String = "PaPSE"
fileprivate let mainWomanTitle:String = "SSbenefitD"

/*: "uid" :*/
fileprivate let main_eachStr:[Character] = ["u","i","d"]

/*: "appCookies =  :*/
fileprivate let kGolfContent:[Character] = ["a","p","p","C","o","o","k","i","e","s"," ","="," "]

/*: "UID" :*/
fileprivate let app_accuracyMsg:[Character] = ["U","I","D"]

/*: "navigator.userAgent" :*/
fileprivate let show_cigaretteMsg:String = "imagevi"
fileprivate let appShrinkEntryMakeupKey:String = "upinr"
fileprivate let data_roundingVictoryTitle:[Character] = ["A","g","e","n","t"]

/*: "xunwu_iphone" :*/
fileprivate let showLightHeavyStr:String = "xunwu_ifind initial task"
fileprivate let mainRestMsg:String = "phonmax"

/*: " xunwu_device_id/ :*/
fileprivate let kHeelPath:String = "core medal protection xunwu"
fileprivate let k_fineKey:String = "ice_id/direction description cite rap transport"

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let user_arrangeStr:[Character] = ["x","u","n","w","u","_","i","p","h","o","n","e"," ","x","u","n","w","u","_","v","e","r","s"]
fileprivate let appShadowKey:String = "half leadion/"

/*:  xunwu_package/ :*/
fileprivate let main_colorMessage:String = "statement string feed point xunwu_pa"
fileprivate let const_rawMediaTitle:String = "normally communicationckage/"

/*:  xunwu_bundleId/ :*/
fileprivate let mainBracePath:[Character] = [" ","x","u","n","w","u","_","b","u","n","d","l","e"]
fileprivate let noti_betterMessage:String = "Id/listen visible"

/*:   webkit/1" :*/
fileprivate let kAgeStr:String = "creation screen  webk"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ViewAssistantMessageHandler.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class ViewAssistantMessageHandler: MotivationReactiveCompatible {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: RidConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: SpeakForeverCustomReflectable?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: SpeakForeverCustomReflectable?, webConfig: RidConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: app_bulletListDeadlineData.map{againstAssistant(sample: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        beam()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        makeUp()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(userPerFormat.suffix(2)) + String(constInfoSpecialName.suffix(5)))
        //: addNotification()
        signNotification()
        //: func__checkConfig()
        tiptoe()
        //: func__setUpWebView()
        finishListener()
        //: func__registerObserver()
        doinglyThin()
        //: func__beginStartRequest()
        jumpEnableSame()
        //: func_checkAppleIAP()
        canvas()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: TalkingAppPushManager.share.func__pushToWebVC(webViewType: .DiscountPopup)
                RiseMalePushManager.share.monthType(webViewType: .DiscountPopup)
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: appDeviceKey, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = []
        webConfig.mediaTypesRequiringUserActionForPlayback = []

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            nameurePrimary(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = true
        w.allowsBackForwardNavigationGestures = true
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.thirdRevenue(), .font: UIFont.landEnableSize(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(constMethodKey.suffix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension ViewAssistantMessageHandler {
    /// 添加通知
    //: private func addNotification() {
    private func signNotification() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(thirdSkipByzant),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: data_saveLinePath,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(beam),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func thirdSkipByzant() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: kBodyFormat.map{$0^51}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func beam() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: notiDataPopId.reversed(), encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(k_interestedValue) + String(constPauseMsg.suffix(5)) + "geSh" + String(constCeremonyFormat.suffix(5))) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func makeUp() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: userFlushText.map{executeTing(item: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(userMightStr.suffix(4)) + "ent(on" + String(main_weeklyHimCooperativeFormat) + String(constFormData.prefix(4)) + "ide): ") + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension ViewAssistantMessageHandler {
    //: func func__registerObserver() {
    func doinglyThin() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (constHomeId.replacingOccurrences(of: "staff", with: "le")))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(main_earlyKey.suffix(7)) + notiRemoveText.replacingOccurrences(of: "generate", with: "ck")))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(palpebra), name: dataProductionRouteKey, object: nil)
    }

    //: func func__setUpWebView() {
    func finishListener() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        noteAgent()
        //: func__addBridgeMethod()
        noAdmin()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.appColor()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            LovelyLifetimeProgressHUD.pointBlank(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(app_pathData)
            }
        }
    }

    //: func func__beginStartRequest() {
    func jumpEnableSame() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.realiseTrilled()
            }
            //: } else {
        } else {
            //: func__startRequest()
            realiseTrilled()
        }
    }

    //: func func__startRequest() {
    func realiseTrilled() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = MentionTriggerAppManager.share.loginUid
        let uid = MentionTriggerAppManager.share.loginUid
        //: let ssid = MentionTriggerAppManager.share.loginSessionId
        let ssid = MentionTriggerAppManager.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (appAssetId.replacingOccurrences(of: "scene", with: "H") + String(const_securityName.suffix(8))) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(user_groupValue)) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(appConStr.prefix(6))))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        featureExecute()
        //: func__initRightButton()
        stageMulti()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func canvas() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            HeManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.location(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            HeManager.shared.versionDismissTransactions()
        }
    }

    //: func func__initLeftButton() {
    func bestirDoingeself() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.lastTo(name: (String(show_othersId) + String(mainPetMsg.suffix(6)) + "_nor")), style: .plain, target: self, action: #selector(commentaryCircle(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func stageMulti() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(SpeakForeverCustomReflectable.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.lastTo(name: (String(const_maybeJoinName) + String(kApprovalPath))), style: .plain, target: self, action: #selector(tweedledeeAndTweedledum))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func commentaryCircle(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        weeklyMethod()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.transplant() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: ViewAssistantMessageHandler.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! ViewAssistantMessageHandler).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! ViewAssistantMessageHandler).beam()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func tiptoe() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func accelerateDoing(sucess: Bool) {
        //: ProgressHUD.dismiss()
        LovelyLifetimeProgressHUD.arcCurDismiss()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func featureExecute() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: appTheFormat.map{$0^213}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func popNo(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: TalkingAppPushManager.share.func__pushToWebVC(urlStr: url)
            RiseMalePushManager.share.errConfig(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func appealingness(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func palpebra() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: dataProductionRouteKey, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func tweedledeeAndTweedledum() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = SumegrityThen(frame: CGRect(x: 0, y: 0, width: show_objectValue, height: constBlockIdentityContent))
        //: view.show()
        view.customerShape()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension ViewAssistantMessageHandler: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            LovelyLifetimeProgressHUD.pointBlank(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            accelerateDoing(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        bestirDoingeself()
        //: func__clearJSBgColor()
        featureExecute()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            accelerateDoing(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            accelerateDoing(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(app_photoCalledUrl.suffix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(showRangeStr.suffix(6))), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(appMAdKey)).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension ViewAssistantMessageHandler: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(constWhetherData) + main_closeWorkerValue.capitalized) + "\(message.name)" + (String(k_developingId) + String(dataWorldwideOwnerUrl.prefix(9))) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.scriptVisible(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func noAdmin() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in LicenseTitleLiteral.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func weeklyMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in LicenseTitleLiteral.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension ViewAssistantMessageHandler {
    //: func func__addUserScript() {
    func asValid() {
        //: var htmlDomain: String = MentionTriggerAppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = MentionTriggerAppManager.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(constWorldName.prefix(7)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(constWorldName.prefix(7))), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(constMmPackageTitle) + const_dimensionKey.replacingOccurrences(of: "tell", with: "/"))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(constMmPackageTitle) + const_dimensionKey.replacingOccurrences(of: "tell", with: "/")), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = MentionTriggerAppManager.share.loginUserMode.userID
        let tempUid: String = MentionTriggerAppManager.share.loginUserMode.userID
        //: let tempSessionID: String = MentionTriggerAppManager.share.loginSessionId
        let tempSessionID: String = MentionTriggerAppManager.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: kListenKey.map{blockNorth(wall: $0)}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(kEstimateData.prefix(4))) + "\(tempUid)" + (String(appRewardMessage.suffix(8))) + "\(htmlDomain)" + (String(show_sayName))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((String(mainEmptyText) + String(k_middleDueFormat)) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (appAssetId.replacingOccurrences(of: "scene", with: "H") + String(const_securityName.suffix(8))) + "\(tempSessionID)" + (String(user_impressionTitle.prefix(5)) + String(kRegionApplyId)) + "\(htmlDomain)" + (String(show_sayName))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((String(mainEmptyText) + String(k_middleDueFormat)) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: user_wonderKey.map{$0^219}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(kEstimateData.prefix(4))) + "\(tempUid)" + (String(appRewardMessage.suffix(8))) + "\(htmlDomain)" + (String(show_sayName))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(mainEmptyText) + String(k_middleDueFormat)) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(kEstimateData.prefix(4))) + "\(tempSessionID)" + (String(appRewardMessage.suffix(8))) + "\(htmlDomain)" + (String(show_sayName))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(mainEmptyText) + String(k_middleDueFormat)) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func nameurePrimary(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = MentionTriggerAppManager.share.appConfigMode.urlH5Domain
            let htmlDomain: String = MentionTriggerAppManager.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(app_resolutionName.suffix(5)) + "ain " + noti_finishPoStr.replacingOccurrences(of: "never", with: "=")) + "\(htmlDomain)")
            //: let userID: String = MentionTriggerAppManager.share.loginUserMode.userID
            let userID: String = MentionTriggerAppManager.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(userSatisfyShakeMessage.suffix(4)) + "llCo" + String(app_interestPath.suffix(7)) + constSkipValue.replacingOccurrences(of: "limited", with: " ")) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (kPositiveId.replacingOccurrences(of: "a", with: "H") + mainWomanTitle.replacingOccurrences(of: "benefit", with: "I")) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (String(main_eachStr)), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.jumpEnableSame()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.rawBuildSession(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func rawBuildSession(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = MentionTriggerAppManager.share.appConfigMode.urlH5Domain
                let htmlDomain: String = MentionTriggerAppManager.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(app_resolutionName.suffix(5)) + "ain " + noti_finishPoStr.replacingOccurrences(of: "never", with: "=")) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: ProjectionThen.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(kGolfContent)) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (String(app_accuracyMsg)), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (String(main_eachStr))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (kPositiveId.replacingOccurrences(of: "a", with: "H") + mainWomanTitle.replacingOccurrences(of: "benefit", with: "I")), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (kPositiveId.replacingOccurrences(of: "a", with: "H") + mainWomanTitle.replacingOccurrences(of: "benefit", with: "I"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.jumpEnableSame()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func noteAgent() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((show_cigaretteMsg.replacingOccurrences(of: "image", with: "na") + "gator." + appShrinkEntryMakeupKey.replacingOccurrences(of: "pin", with: "se") + String(data_roundingVictoryTitle))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (show_cigaretteMsg.replacingOccurrences(of: "image", with: "na") + "gator." + appShrinkEntryMakeupKey.replacingOccurrences(of: "pin", with: "se") + String(data_roundingVictoryTitle)))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(showLightHeavyStr.prefix(7)) + mainRestMsg.replacingOccurrences(of: "max", with: "e"))) {
                //: let str = getUserAgentString()
                let str = getAgentSnaplineUser()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.realise { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(kHeelPath.suffix(6)) + "_dev" + String(k_fineKey.prefix(7))) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func getAgentSnaplineUser() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (String(user_arrangeStr) + String(appShadowKey.suffix(4))) + "\(userDoingId)" + (String(main_colorMessage.suffix(9)) + String(const_rawMediaTitle.suffix(6))) + "\(user_underFormat)" + (String(mainBracePath) + String(noti_betterMessage.prefix(3))) + "\(mainAgeData)" + (String(kAgeStr.suffix(6)) + "it/1")
    }
}
