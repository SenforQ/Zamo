
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let main_untilName:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "uid" :*/
fileprivate let show_valueBeamName:[UInt8] = [0xd4,0xc8,0xc3]

fileprivate func gestureMust(min num: UInt8) -> UInt8 {
    let value = Int(num) - 95
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "headPic" :*/
fileprivate let const_attachPlotKey:String = "tail serviceheadPi"
fileprivate let mainEditorName:String = "exception"

/*: "nickname" :*/
fileprivate let user_statLabText:String = "nicknamjudge"

/*: "birthday" :*/
fileprivate let k_dynamicsEraseImportId:String = "byoungrt"

/*: "videoUrl" :*/
fileprivate let data_harassmentMsg:[Character] = ["v","i","d","e","o","U","r","l"]

/*: "cover" :*/
fileprivate let userThumbName:[Character] = ["c","o","v","e","r"]

/*: "location" :*/
fileprivate let constYesterdayId:String = "lfloodc"
fileprivate let dataLeastContent:String = "crushion"

/*: "cmd" :*/
fileprivate let noti_guidanceId:[UInt8] = [0x46,0x48,0x41]

/*: "refuseCall" :*/
fileprivate let main_secretId:String = "pose"
fileprivate let userAmUrl:[Character] = ["e","f","u","s","e","C","a","l","l"]

/*: "data" :*/
fileprivate let showRunFormat:[UInt8] = [0xc3,0xc6,0xd3,0xc6]

private func sheBusy(charm num: UInt8) -> UInt8 {
    return num ^ 167
}

/*: "Call interruption" :*/
fileprivate let const_historyPath:String = "assist po accelerate note drawingCal"
fileprivate let kTeamMessage:String = "after vertical onlyl int"
fileprivate let show_loseMatchMessage:String = "pformaton"

/*: "stopCall" :*/
fileprivate let appPiText:String = "board"
fileprivate let show_clickMsg:[Character] = ["t","o","p","C","a","l","l"]

/*: "logId" :*/
fileprivate let userTowardPath:[UInt8] = [0x64,0x49,0x67,0x6f,0x6c]

/*: "cancelCall" :*/
fileprivate let mainOrientationPremiumLaunchName:String = "CANC"

/*: "No answer" :*/
fileprivate let const_actorContent:String = "extreme chance respectNo an"
fileprivate let show_originalName:String = "SWER"

/*: "duration" :*/
fileprivate let const_effOppositeMessage:String = "lifestyleuration"

/*: "She has hung up" :*/
fileprivate let const_permissionValue:String = "She hasub year"
fileprivate let kRepublicValue:[Character] = ["s"]
fileprivate let notiMaleId:[Character] = [" ","h","u","n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let notiThreadGroupKey:String = "psychologicalnce"

/*: "He has hung up" :*/
fileprivate let k_behaviorName:[Character] = ["H","e"," ","h","a","s"," ","h"]
fileprivate let noti_hostFormat:String = "background bag storage gender plotung up"

/*: "endType" :*/
fileprivate let userCleanData:[Character] = ["e","n","d","T","y","p","e"]

/*: "stopSex" :*/
fileprivate let k_benefitQualityBeyondId:String = "stopSexparticle spark else near"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let user_administrativeName:[UInt8] = [0x90,0xb7,0xaa,0xac,0xbf,0xbf,0xb0,0xba,0xb0,0xbc,0xb7,0xad,0xf9,0xbe,0xb6,0xb5,0xbd,0xf9,0xba,0xb6,0xb0,0xb7,0xaa,0xf5,0xf9,0xba,0xb8,0xb5,0xb5,0xf9,0xb1,0xb8,0xaa,0xf9,0xbc,0xb7,0xbd,0xbc,0xbd]

/*: "The other hung up the phone" :*/
fileprivate let kAnonStr:[Character] = ["T"]
fileprivate let show_identifyPlotId:[Character] = ["h","e"," ","o","t","h","e","r"," ","h","u","n","g"," ","u","p"," ","t","h","e"," ","p","h","o","n","e"]

/*: "type" :*/
fileprivate let constRoundingValue:String = "typeyebrow"

/*: "jumpUrl" :*/
fileprivate let notiHarkMsg:String = "jumpUrlhide visible trigger enough section"

/*: "Cancel" :*/
fileprivate let data_dateMessage:String = "Cancelcurrent govern"

/*: "Get gold coins" :*/
fileprivate let const_beginUrl:String = "finite thread than distribution quickGet "
fileprivate let show_goingActionName:String = "golall"

/*: "addBean" :*/
fileprivate let dataDrownLovelyName:[Character] = ["a","d","d","B","e","a","n"]

/*: "refuseSex" :*/
fileprivate let constThanksFormat:[Character] = ["r","e","f","u","s","e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let kPlusGoingId:String = "onAccconsumer request after"
fileprivate let data_reKey:[Character] = ["e","p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let constCounterestedText:String = "aregione"

/*: "Calls are cancelled" :*/
fileprivate let noti_detailText:String = "album argument look kitCall"
fileprivate let noti_mountUrl:String = "e canmaybe advanced product skip"
fileprivate let user_cropPath:[Character] = ["c","e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let user_spendUrl:[Character] = ["T","h","e"," ","c","a","l","l"]
fileprivate let mainAppointmentFormat:String = " has write consumer smart"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  InPublicViewDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let showFailureUrl = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class InPublicViewDelegate: EverlastinglyThen {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: LovelyChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: SlowReactiveCompatible? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: WeightReactiveCompatible? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: PackReactiveCompatible? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        DisagreeableWomanSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        DisagreeableWomanSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        DisagreeableWomanSocketManager.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: main_untilName.reversed(), encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! HomeControllerDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.monthSmall()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            analyseAuth()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            wood(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currAccessibleVC = nil
            DisagreeableWomanSocketManager.shared.currAccessibleVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.aboveInit()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        flag()
        //: setupSubViewsConstraint()
        undercoverOperation()
        //: initNotificationObserver()
        afterDraft()
        //: req_callGetUserInfo()
        white()
        //: timeoutRules()
        componentRules()
        //: setUpBGMPlayer()
        pressGreet()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.bean()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if MentionTriggerAppManager.share.appConfigMode.disableShootScreen {
            if MentionTriggerAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = RateShieldView(frame: CGRect(x: 0, y: 0, width: show_objectValue, height: constBlockIdentityContent))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.accessible = false
        DisagreeableWomanSocketManager.shared.accessible = false
        //: TalkingSocketManager.shared.isCalling = false
        DisagreeableWomanSocketManager.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.account()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: PositionReactiveCompatible = {
        //: let v = TalkingBGVideoView()
        let v = PositionReactiveCompatible()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.dropFilterRate(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: MarketPlayerDelegate = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = MarketPlayerDelegate(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: ToWindowView = {
        //: let v = TalkingVideoWindowView()
        let v = ToWindowView()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = show_objectValue - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if DriveReactiveCompatible.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: app_pathData + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.furnish()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: BondRatingCloudDelegate = {
        //: let m = TalkingVideoChatManager()
        let m = BondRatingCloudDelegate()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension InPublicViewDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func white() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        BondRatingCloudDelegate.completion(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: show_valueBeamName.map{gestureMust(min: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(const_attachPlotKey.suffix(6)) + mainEditorName.replacingOccurrences(of: "exception", with: "c"))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(user_statLabText.replacingOccurrences(of: "judge", with: "e"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(k_dynamicsEraseImportId.replacingOccurrences(of: "young", with: "i") + "hday")].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(data_harassmentMsg))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(String(userThumbName))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(constYesterdayId.replacingOccurrences(of: "flood", with: "o") + dataLeastContent.replacingOccurrences(of: "crush", with: "at"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.benefit()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.fillThree()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func fillThree() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = LeadershipCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = LeadershipCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.computerFilenameTo(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.toAGreaterExtent(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension InPublicViewDelegate {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func analyseAuth() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        GatePermissionTool.fitStop { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.outBarMsg(showMsg: dataStatusKey)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                DisagreeableWomanSocketManager.shared.postAm(info: [String(bytes: noti_guidanceId.map{$0^37}, encoding: .utf8)!: (main_secretId.replacingOccurrences(of: "pose", with: "r") + String(userAmUrl)), String(bytes: showRunFormat.map{sheBusy(charm: $0)}, encoding: .utf8)!: [String(bytes: show_valueBeamName.map{gestureMust(min: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.weltanschauungDeal()
                //: return
                return
            }

            //: self.startPreview()
            self.bean()
            //: self.stopBGM()
            self.bindColumnBgm()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = SlowReactiveCompatible.connectionHistory(type: .Response)
            //: self.startBGM()
            self.monthSmall()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func bean() {
        //: self.videoManager.startPreview()
        self.videoManager.differentiate()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func account() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.bindColumnBgm()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        SimplyThen.completeReasonFinancialBacking()
        //: TalkingSocketManager.shared.callerDelegate = nil
        DisagreeableWomanSocketManager.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        DisagreeableWomanSocketManager.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        DisagreeableWomanSocketManager.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func weltanschauungDeal(animated: Bool = true) {
        //: self.stopBGM()
        self.bindColumnBgm()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func furnish() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.releasingHormoneView()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.fillThree()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func punish() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.yesterday()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.fillThree()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.overCapacity()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func overCapacity() {
        //: guard MentionTriggerAppManager.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard MentionTriggerAppManager.share.loginUserMode.sex == TopicKeyRepresentable.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = PackReactiveCompatible()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.counternal()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension InPublicViewDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func afterDraft() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(dailyOutput(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(backViewFinder(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: userKeyFormat,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(pornoNote(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: notiToId,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        showFailureUrl.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func dailyOutput(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            weltanschauungDeal()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func pornoNote(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard DisagreeableWomanSocketManager.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        DisagreeableWomanSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        DisagreeableWomanSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        DisagreeableWomanSocketManager.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.duringExitAvailable()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func backViewFinder(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.fillThree()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension InPublicViewDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.outBarMsg(showMsg: (String(const_historyPath.suffix(3)) + String(kTeamMessage.suffix(5)) + "erru" + show_loseMatchMessage.replacingOccurrences(of: "format", with: "ti")))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                DisagreeableWomanSocketManager.shared.postAm(info: [String(bytes: noti_guidanceId.map{$0^37}, encoding: .utf8)!: (appPiText.replacingOccurrences(of: "board", with: "s") + String(show_clickMsg)), String(bytes: showRunFormat.map{sheBusy(charm: $0)}, encoding: .utf8)!: [String(bytes: userTowardPath.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                DisagreeableWomanSocketManager.shared.postAm(info: [String(bytes: noti_guidanceId.map{$0^37}, encoding: .utf8)!: (mainOrientationPremiumLaunchName.lowercased() + "elCall"), String(bytes: showRunFormat.map{sheBusy(charm: $0)}, encoding: .utf8)!: [String(bytes: userTowardPath.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                DisagreeableWomanSocketManager.shared.postAm(info: [String(bytes: noti_guidanceId.map{$0^37}, encoding: .utf8)!: (main_secretId.replacingOccurrences(of: "pose", with: "r") + String(userAmUrl)), String(bytes: showRunFormat.map{sheBusy(charm: $0)}, encoding: .utf8)!: [String(bytes: show_valueBeamName.map{gestureMust(min: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.slightWithStop()
                //: TalkingSocketManager.shared.currAccessibleVC = nil
                DisagreeableWomanSocketManager.shared.currAccessibleVC = nil
            }
            //: popCurrentViewController()
            weltanschauungDeal()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension InPublicViewDelegate {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func pressGreet() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? WouldColumnConvertible.Call : WouldColumnConvertible.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = SlowReactiveCompatible.connectionHistory(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func monthSmall() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.asDiscount()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func bindColumnBgm() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.salvage()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension InPublicViewDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func componentRules() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? noti_saltAllKey : self.chatModel?.stateLine()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            late()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.late()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func late() {
        //: guard !TalkingSocketManager.shared.accessible else { return }
        guard !DisagreeableWomanSocketManager.shared.accessible else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.outBarMsg(showMsg: (String(const_actorContent.suffix(5)) + show_originalName.lowercased()).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: noti_guidanceId.map{$0^37}, encoding: .utf8)!: (mainOrientationPremiumLaunchName.lowercased() + "elCall"), String(bytes: showRunFormat.map{sheBusy(charm: $0)}, encoding: .utf8)!: [String(bytes: userTowardPath.reversed(), encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: noti_guidanceId.map{$0^37}, encoding: .utf8)!: (main_secretId.replacingOccurrences(of: "pose", with: "r") + String(userAmUrl)), String(bytes: showRunFormat.map{sheBusy(charm: $0)}, encoding: .utf8)!: [String(bytes: show_valueBeamName.map{gestureMust(min: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        DisagreeableWomanSocketManager.shared.postAm(info: socketDict)
        //: popCurrentViewController()
        weltanschauungDeal()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension InPublicViewDelegate {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func sharedGesture(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: userTowardPath.reversed(), encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(const_effOppositeMessage.replacingOccurrences(of: "lifestyle", with: "d"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.betweenInstall(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.duringBringHome()
            //: createTalkRoom()
            punish()
            //: self.stopBGM()
            self.bindColumnBgm()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func wood(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.releasingHormoneView()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.dropFilterRate(show: false)
        //: self.stopBGM()
        self.bindColumnBgm()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.duringBringHome()
        //: self.createTalkRoom()
        self.punish()
    }
}

// MARK: - MaleObjectProtocol【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension InPublicViewDelegate: MaleObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func socket(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(const_permissionValue.prefix(6)) + String(kRepublicValue) + String(notiMaleId)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(notiThreadGroupKey.replacingOccurrences(of: "psychological", with: "ca") + "lSex")].stringValue == TopicKeyRepresentable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(k_behaviorName) + String(noti_hostFormat.suffix(6))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.outBarMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.slightWithStop()
            //: TalkingSocketManager.shared.currAccessibleVC = nil
            DisagreeableWomanSocketManager.shared.currAccessibleVC = nil
        }

        //: popCurrentViewController()
        weltanschauungDeal()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func visitor(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        wood(logId: json[String(bytes: userTowardPath.reversed(), encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func top(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(userCleanData))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(const_permissionValue.prefix(6)) + String(kRepublicValue) + String(notiMaleId)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(k_benefitQualityBeyondId.prefix(7)))].stringValue == TopicKeyRepresentable.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(k_behaviorName) + String(noti_hostFormat.suffix(6))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if MentionTriggerAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if MentionTriggerAppManager.share.appStatus == AidNameConvertible.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: user_administrativeName.map{$0^217}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(kAnonStr) + String(show_identifyPlotId)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.outBarMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.slightWithStop()
            //: TalkingSocketManager.shared.currAccessibleVC = nil
            DisagreeableWomanSocketManager.shared.currAccessibleVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.someone()
        //: popCurrentViewController()
        weltanschauungDeal()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func followHome(msg: String, data: [String: Any]) {
        //: guard MentionTriggerAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard MentionTriggerAppManager.share.appStatus == AidNameConvertible.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(constRoundingValue.replacingOccurrences(of: "eyebrow", with: "e"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(notiHarkMsg.prefix(7)))] as? String else { return }
            //: let httpsUrl = MentionTriggerAppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = MentionTriggerAppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = RidConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: TalkingAppPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            RiseMalePushManager.share.errConfig(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            StandMarketThen.labelConfig(title: nil,
                                         //: message: msg,
                                         message: msg,
                                         //: leftBtnTitle: "Cancel".localized,
                                         leftBtnTitle: (String(data_dateMessage.prefix(6))).localized,
                                         //: rightBtnTitle: "Get gold coins".localized) {
                                         rightBtnTitle: (String(const_beginUrl.suffix(4)) + show_goingActionName.replacingOccurrences(of: "all", with: "d") + " coins").localized)
            {
                //: TalkingAlertShow.hideAlert()
                StandMarketThen.alertCur()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAppPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                RiseMalePushManager.share.primed(clickEvent: userButtonPath, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func graduate(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.panelOfPermission(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func aboveSocket(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(dataDrownLovelyName))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.singleRecognize(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.messagePoint(point: addBean)
        }
    }
}

// MARK: - EarthPartyDelegate【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension InPublicViewDelegate: EarthPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func back(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(const_permissionValue.prefix(6)) + String(kRepublicValue) + String(notiMaleId)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(constThanksFormat))].stringValue == TopicKeyRepresentable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(k_behaviorName) + String(noti_hostFormat.suffix(6))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.outBarMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.slightWithStop()
            //: TalkingSocketManager.shared.currAccessibleVC = nil
            DisagreeableWomanSocketManager.shared.currAccessibleVC = nil
        }

        //: popCurrentViewController()
        weltanschauungDeal()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func localEntitle(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        wood(logId: json[String(bytes: userTowardPath.reversed(), encoding: .utf8)!].intValue)
    }
}

// MARK: - SumerObjectProtocol【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension InPublicViewDelegate: SumerObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func visual(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (main_secretId.replacingOccurrences(of: "pose", with: "r") + String(userAmUrl)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.outBarMsg(showMsg: (String(const_actorContent.suffix(5)) + show_originalName.lowercased()).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.slightWithStop()
                //: TalkingSocketManager.shared.currAccessibleVC = nil
                DisagreeableWomanSocketManager.shared.currAccessibleVC = nil
            }
            //: popCurrentViewController()
            weltanschauungDeal()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(kPlusGoingId.prefix(5)) + String(data_reKey)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard MentionTriggerAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard MentionTriggerAppManager.share.appStatus == AidNameConvertible.normal.rawValue else { return }
            //: TalkingAppPushManager.share.func__jumpToWebRecharge(sufficient: false)
            RiseMalePushManager.share.primed(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(kPlusGoingId.prefix(5)) + String(data_reKey)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.outBarMsg(showMsg: msg)
            //: popCurrentViewController()
            weltanschauungDeal()
        }
    }
}

// MARK: - DelegateObjectProtocol【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension InPublicViewDelegate: DelegateObjectProtocol {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func universal() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        DisagreeableWomanSocketManager.shared.postAm(info: [String(bytes: noti_guidanceId.map{$0^37}, encoding: .utf8)!: (constCounterestedText.replacingOccurrences(of: "region", with: "cc") + "ptCall"), String(bytes: showRunFormat.map{sheBusy(charm: $0)}, encoding: .utf8)!: [String(bytes: show_valueBeamName.map{gestureMust(min: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func suds() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.outBarMsg(showMsg: (String(noti_detailText.suffix(4)) + "s ar" + String(noti_mountUrl.prefix(5)) + String(user_cropPath)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            DisagreeableWomanSocketManager.shared.postAm(info: [String(bytes: noti_guidanceId.map{$0^37}, encoding: .utf8)!: (mainOrientationPremiumLaunchName.lowercased() + "elCall"), String(bytes: showRunFormat.map{sheBusy(charm: $0)}, encoding: .utf8)!: [String(bytes: userTowardPath.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.outBarMsg(showMsg: (String(noti_detailText.suffix(4)) + "s ar" + String(noti_mountUrl.prefix(5)) + String(user_cropPath)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            DisagreeableWomanSocketManager.shared.postAm(info: [String(bytes: noti_guidanceId.map{$0^37}, encoding: .utf8)!: (main_secretId.replacingOccurrences(of: "pose", with: "r") + String(userAmUrl)), String(bytes: showRunFormat.map{sheBusy(charm: $0)}, encoding: .utf8)!: [String(bytes: show_valueBeamName.map{gestureMust(min: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.outBarMsg(showMsg: (String(user_spendUrl) + String(mainAppointmentFormat.prefix(5)) + "ended").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            DisagreeableWomanSocketManager.shared.postAm(info: [String(bytes: noti_guidanceId.map{$0^37}, encoding: .utf8)!: (appPiText.replacingOccurrences(of: "board", with: "s") + String(show_clickMsg)), String(bytes: showRunFormat.map{sheBusy(charm: $0)}, encoding: .utf8)!: [String(bytes: userTowardPath.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        weltanschauungDeal()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func belowReplaceDraft(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.headLensHoodWork(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func createForbidden(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.dropFilterRate(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.license(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.fillThree()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func thanBy() {
        //: TalkingSocketManager.shared.currAccessibleVC = self
        DisagreeableWomanSocketManager.shared.currAccessibleVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = WeightReactiveCompatible.groin()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.mannerSmallPumpEnter()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.lipConfirm()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.transplant()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        weltanschauungDeal()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func jobDescription() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension InPublicViewDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func flag() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.makeName(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func undercoverOperation() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
