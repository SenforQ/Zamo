
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let dataSlideText:String = "SOhistoryE"
fileprivate let mainValuationWeeklyMessage:String = "KEcontent"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let constExtremeTitle:[UInt8] = [0x4e,0x4f,0x49,0x54,0x41,0x43,0x49,0x46,0x49,0x54,0x4f,0x4e,0x5f,0x4c,0x4c,0x41,0x43,0x54,0x53,0x45,0x55,0x51,0x45,0x52,0x5f,0x54,0x41,0x48,0x43,0x4f,0x45,0x44,0x49,0x56,0x5f,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let show_missKey:[UInt8] = [0xb8,0xaf,0xa9,0xa3,0xbc,0xaf,0xb5,0xac,0xa6,0xa5,0xab,0xbe,0xb9,0xa9,0xb8,0xaf,0xaf,0xa4,0xb5,0xa4,0xa5,0xbe,0xa3,0xac,0xa3,0xa9,0xab,0xbe,0xa3,0xa5,0xa4]

private func systemContact(po num: UInt8) -> UInt8 {
    return num ^ 234
}

/*: "isCalling" :*/
fileprivate let main_skipId:[Character] = ["i","s","C","a","l","l","i","n","g"]
 
/*: "SOCKET 连接成功." :*/
fileprivate let noti_attachFoodData:[Character] = ["S","O","C","K","E","T"," ","连","\u{63a5}"]
fileprivate let userHimName:String = "\u{6210}\u{529f}."

/*: "SOCKET 连接失败, error: :*/
fileprivate let const_propertyFormat:[Character] = ["S","O","C","K","E","T"," ","\u{8fde}","\u{63a5}","失","败",","]
fileprivate let showAgoCosStr:String = " "
fileprivate let userCollectionTitle:String = "brace huge net window billerror:"

/*: "SOCKET 失败 :*/
fileprivate let noti_onceMessage:String = "SOreaderKE"
fileprivate let mainBeyondMsg:[Character] = ["T"," ","失","\u{8d25}"]

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let dataOvalName:[Character] = ["次",","," ","切","换","过","S","o","c","k","e","t","域","名","重","新","\u{5c1d}","试","，","地","址","："]

/*: "SOCKET 已关闭, code: :*/
fileprivate let user_pastKey:String = "SOCKET bottom explain"
fileprivate let dataTermsPath:String = "已关闭, cplain spy moon interest shadow"

/*: , reason: :*/
fileprivate let mainAdministrativePathFormat:String = ", "
fileprivate let user_delayTitle:String = "reason:con speed angle the"

/*: /?sid= :*/
fileprivate let data_purchaseUrl:String = "place dialog speed/?sid"
fileprivate let userCenterKey:[Character] = ["="]

/*: &uid= :*/
fileprivate let noti_requestReactStr:String = "&uid=span sign"

/*: &platform=iphone&version= :*/
fileprivate let k_partDismissData:[Character] = ["&","p","l","a","t","f","o","r","m","="]
fileprivate let show_mannerData:String = "icurrenton"
fileprivate let user_pressureName:String = "translation empty quality nobody freshe&ver"

/*: &packageId= :*/
fileprivate let user_siblingId:String = "zz comprehensive narrow&packa"
fileprivate let const_eventData:[Character] = ["g","e","I","d","="]

/*: "1v1server. :*/
fileprivate let user_tabUrl:[Character] = ["1","v","1","s","e","r","v"]
fileprivate let k_nameData:String = "to threshold sample draft picker."

/*: .com:7766" :*/
fileprivate let noti_canHarassmentMsg:String = "brush ignore bound without con.com:7"
fileprivate let data_normallyContent:String = "766"

/*: "43.129.185.194:7767" :*/
fileprivate let showSweetText:String = "43.1direction9"
fileprivate let dataPlusTitle:String = "194:stock67"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let data_comprehensiveValue:[UInt8] = [0x40,0x5c,0x50,0x58,0x56,0x47,0x33,0xf4,0xae,0x82,0xf4,0xa8,0x8f,0x61,0x76,0x72,0x70,0x7b,0x72,0x71,0x7a,0x7f,0x7a,0x67,0x6a,0x50,0x7b,0x72,0x7d,0x74,0x76,0x77,0xf4,0x88,0x82,0xf6,0x83,0xbf,0xfc,0xaf,0x9f,0xf5,0x85,0xbe,0xf4,0xae,0x82,0xf6,0x96,0xa0,0xfa,0x84,0xbe,0x3d]

/*: "cmd" :*/
fileprivate let mainDrownText:[Character] = ["c","m","d"]

/*: "ping" :*/
fileprivate let const_startData:String = "broadcastng"

/*: "callHeartbeat" :*/
fileprivate let k_insideData:String = "cropal"
fileprivate let showHelpPureValue:String = "lHeapast decision"

/*: "recievedDate" :*/
fileprivate let noti_planId:String = "resistanceeci"

/*: "liveHeartBeat" :*/
fileprivate let show_thenName:String = "market"
fileprivate let data_formerFormat:[Character] = ["i","v","e","H","e","a","r","t","B","e","a","t"]

/*: "🔗 关闭 socket" :*/
fileprivate let const_dragPath:String = "🔗 no"
fileprivate let show_barrierData:[Character] = ["闭"," ","s","o","c","k","e","t"]

/*: "The user logs out" :*/
fileprivate let app_acquireTitle:[Character] = ["T","h","e"," ","u","s","e","r"," "]
fileprivate let mainContrastName:String = "accelerate autologs out"

/*: "SOCKET  :*/
fileprivate let appAgencyValue:String = "SOCKET apply statement export at cos"

/*: 重连. 重试次数： :*/
fileprivate let k_foodKey:String = "重连. 重"

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let main_recommendProMsg:String = "socke"
fileprivate let kPositMsg:String = "正常 不\u{9700}要"

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let appStandName:String = "handleCKE"
fileprivate let user_tableData:[Character] = ["连","接"," ","不","\u{9700}","要","重","新","连","接","."]

/*: "&resume=1" :*/
fileprivate let kTechnologyStr:[Character] = ["&"]
fileprivate let notiPlanRemindMsg:[Character] = ["r","e","s","u","m","e","=","1"]

/*: "&lang= :*/
fileprivate let userStripPresentValue:String = "interval dynamics traveler&lang="

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let user_storageGoodData:String = "SaccountKE"
fileprivate let kOfName:String = "连接, \u{5730}址"
fileprivate let constAreaBodyValue:String = "extra"

/*: "onNetDisabled" :*/
fileprivate let const_anonContent:String = "onNetDivariation ceremony re"
fileprivate let show_lengthPath:String = "sabldescriptiond"

/*: "errno" :*/
fileprivate let userFairData:String = "errnmeeting"

/*: "data" :*/
fileprivate let constNetValue:String = "dconnecttconnect"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let user_upDigitalText:[Character] = ["S","O","C","K","E","T"," ","连","接","保"]
fileprivate let userThreePath:[Character] = ["活","c","m"]
fileprivate let mainBeneathPath:String = "back narrow added goald: ping."

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let noti_rejectPath:[UInt8] = [0x3b,0x27,0x2b,0x23,0x2d,0x3c,0x48,0x1c,0x1a,0x1c,0xb,0x8c,0xd7,0xf5,0x8e,0xdc,0xd3,0xb,0x5,0xc,0x52,0x48,0xb,0x9,0x4,0x4,0x20,0xd,0x9,0x1a,0x1c,0xa,0xd,0x9,0x1c,0x46]

private func needBefore(thread num: UInt8) -> UInt8 {
    return num ^ 104
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let data_pullKey:[UInt8] = [0x95,0x91,0x85,0x8d,0x87,0x96,0x62,0xae,0xab,0xb8,0xa7,0x26,0x1,0xdf,0x28,0xf6,0xfd,0xa5,0xaf,0xa6,0x7c,0x62,0xae,0xab,0xb8,0xa7,0x8a,0xa7,0xa3,0xb4,0xb6,0x84,0xa7,0xa3,0xb6,0x70]

fileprivate func seaQuote(mail num: UInt8) -> UInt8 {
    let value = Int(num) + 190
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "onP" :*/
fileprivate let data_problemText:[Character] = ["o","n","P"]

/*: "msg" :*/
fileprivate let app_micStr:String = "mspack"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let main_donationUrl:String = "\u{0001f517} 处理"
fileprivate let dataDonVictoryWoodMessage:String = "developing domain smalldName:"

/*: , errNo: :*/
fileprivate let noti_markValue:String = "cite closed comply small, err"
fileprivate let notiPastConstraintName:[Character] = ["N","o",":"]

/*: , msg: :*/
fileprivate let kTotalPath:[Character] = [","," ","m","s","g",":"]

/*: , data: :*/
fileprivate let showCompleteValue:[Character] = [","," ","d","a","t","a",":"]

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let userCenterName:[Character] = ["S","O","C","K","E","T"," ","接","收","异","常","消","息","\u{ff1a}","c","m"]
fileprivate let app_makerName:String = "remark"
fileprivate let mainPartMakeMsg:[Character] = ["N","a","m","e",":"]

/*: "onReceivedGift" :*/
fileprivate let user_positKey:[Character] = ["o","n","R","e","c","e","i","v","e","d","G","i","f","t"]

/*: "onVideoMatch" :*/
fileprivate let constMinimizeCustomMessage:String = "already none operateonVideo"
fileprivate let showItStr:[Character] = ["M","a","t","c","h"]

/*: "onRequestCall" :*/
fileprivate let main_yearName:[Character] = ["o","n","R","e","q","u","e","s","t","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let app_observeMessage:String = "onCanregular group tone"
fileprivate let constAuthorityMsg:String = "aexposureexposure"

/*: "onRefuseCall" :*/
fileprivate let notiJudgeLessName:String = "onRefuplot destination"
fileprivate let showMatchName:[Character] = ["s","e","C","a","l","l"]

/*: "onAcceptCall" :*/
fileprivate let notiEditFormat:String = "onAcceorange cost original"
fileprivate let const_artifactKey:String = "ptCallfunction shift central"

/*: "onStopCall" :*/
fileprivate let kBounceStr:String = "onSteven since export certain"

/*: "freeCallTimes" :*/
fileprivate let const_beautifulMessage:String = "fexploree"
fileprivate let dataAddId:String = "assert programTime"
fileprivate let dataToneWillUrl:[Character] = ["s"]

/*: "mfCoin" :*/
fileprivate let constSimplyFormat:[Character] = ["m","f","C","o","i","n"]

/*: "onMissCoin" :*/
fileprivate let appWithinTwoUrl:String = "onMichar interaction box us speak"
fileprivate let const_detailedSourceTitle:[Character] = ["i","n"]

/*: "acceptCall" :*/
fileprivate let noti_minimizeName:String = "meetingce"

/*: "stopCall" :*/
fileprivate let dataReactFormat:String = "STOP"

/*: "requestCall" :*/
fileprivate let app_ditName:[Character] = ["r","e","q","u","e","s","t","C","a","l","l"]

/*: "[Invite you to a video call...]" :*/
fileprivate let user_disappearMsg:[UInt8] = [0x5d,0x2e,0x2e,0x2e,0x6c,0x6c,0x61,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x61,0x20,0x6f,0x74,0x20,0x75,0x6f,0x79,0x20,0x65,0x74,0x69,0x76,0x6e,0x49,0x5b]

/*: "type" :*/
fileprivate let data_replyValue:[Character] = ["t","y","p","e"]

/*: "[Invite you to a voice call...]" :*/
fileprivate let app_carrierStr:[UInt8] = [0x6f,0x7d,0x5a,0x42,0x5d,0x40,0x51,0x14,0x4d,0x5b,0x41,0x14,0x40,0x5b,0x14,0x55,0x14,0x42,0x5b,0x5d,0x57,0x51,0x14,0x57,0x55,0x58,0x58,0x1a,0x1a,0x1a,0x69]

private func leastFollowing(used num: UInt8) -> UInt8 {
    return num ^ 52
}

/*: "uid" :*/
fileprivate let appTolerancePath:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let noti_accountingId:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let dataAdminNorthRobotKey:String = "huge insert mean loseheadPi"
fileprivate let userHungMessage:[Character] = ["c"]

/*: "onCallResume" :*/
fileprivate let main_maxLockData:[Character] = ["o","n","C","a","l","l"]
fileprivate let noti_tapPath:[Character] = ["R","e","s","u","m","e"]

/*: "cancelCall" :*/
fileprivate let show_informationBlindCoordinateStr:String = "cabodyce"
fileprivate let notiNetValue:String = "explore reportlCall"

/*: "refuseCall" :*/
fileprivate let const_brushKey:[Character] = ["r","e","f","u","s","e","C","a"]
fileprivate let const_provideValue:String = "LL"

/*: "onCallMotivation" :*/
fileprivate let mainPicName:String = "onCalrounding related ample ease"
fileprivate let notiQualityFormat:String = "istrategytion"

/*: "onLiveWarning" :*/
fileprivate let k_heavyEarthContent:String = "otarget"
fileprivate let mainBenefitKey:String = "Liverecover resistance discount prepare"

/*: "reason" :*/
fileprivate let userSimultaneouslyKey:String = "rrecordason"

/*: "time" :*/
fileprivate let notiExecuteAtStr:String = "tisufficient"

/*: "onStopLive" :*/
fileprivate let appHowTitle:[Character] = ["o","n","S","t","o","p","L"]
fileprivate let showCosUrl:[Character] = ["i","v","e"]

/*: "onSendProgramGift" :*/
fileprivate let noti_heelFormat:String = "onSeshot scale contend"
fileprivate let const_curTitle:String = "we clue list bouncegramG"
fileprivate let notiRidBackKey:String = "situationt"

/*: "onGameFs" :*/
fileprivate let constHandNoticeKey:[Character] = ["o","n"]
fileprivate let constPlatformUrl:String = "GameFsmistake pi player three own"

/*: "broadStatus" :*/
fileprivate let k_purchaseValue:[Character] = ["b","r","o","a","d","S","t"]
fileprivate let constGrowthEngageMessage:String = "atsucceed"

/*: "onCallChatMsg" :*/
fileprivate let appBeamTitle:[Character] = ["o"]
fileprivate let data_votePath:String = "chemical mpnCal"

/*: "onNotificationPopupWin" :*/
fileprivate let const_popBeginName:[Character] = ["o","n","N","o","t","i","f","i","c","a","t","i","o","n","P","o"]
fileprivate let showPencilFormat:String = "pupWinburn bar provide ample universal"

/*: "onUserBlock" :*/
fileprivate let show_logFormat:String = "guidance translationonUs"

/*: "isRecharge" :*/
fileprivate let kActiveUrl:String = "element edition dotisRe"
fileprivate let data_aspectWhetherTransferPath:String = "turn"

/*: "source" :*/
fileprivate let dataGuideStr:[Character] = ["s","o","u","r","c","e"]

/*: "invitePop" :*/
fileprivate let notiWorkerText:[Character] = ["i","n","v","i","t","e","P","o","p"]

/*: "videoPop" :*/
fileprivate let data_partFormat:[Character] = ["v","i","d","e","o"]
fileprivate let data_airLayerName:[Character] = ["P","o","p"]

/*: "coin" :*/
fileprivate let notiClueRecognizeMsg:String = "coorientation"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DisagreeableWomanSocketManager.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let notiNextUrl = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let notiImageVideoTitle = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let kMatchTitle = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let app_workBeginData = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let userScaleUrl = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let userTurnKey = (dataSlideText.replacingOccurrences(of: "history", with: "CK") + "T_ADDR_" + mainValuationWeeklyMessage.replacingOccurrences(of: "content", with: "Y"))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let notiDeviceTitle = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let noti_conversationUrl = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let app_momentValue = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let show_profileKey = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let constEventShowMsg = NSNotification.Name(rawValue: String(bytes: constExtremeTitle.reversed(), encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let notiHundredName = NSNotification.Name(rawValue: String(bytes: show_missKey.map{systemContact(po: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol SumerObjectProtocol: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func visual(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol SumegrityObjectProtocol: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func headFeedback(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func temporary(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol MaleObjectProtocol: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func visitor(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func top(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func socket(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func followHome(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol EarthPartyDelegate: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func back(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func localEntitle(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func aboveSocket(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class DisagreeableWomanSocketManager: NSObject {
    //: enum SocketRetryType: String {
    enum CommissionType: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currAccessibleVC: AnyObject?
    var currAccessibleVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: DriveReactiveCompatible.shared.direction == .rightToLeft ? 0 : show_objectValue,
        //: y: StatusBarHeight+190
        y: app_pathData + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: DriveReactiveCompatible.shared.direction == .rightToLeft ? 0 : show_objectValue,
        //: y: StatusBarHeight+190
        y: app_pathData + 190
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: SumerObjectProtocol? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: SumegrityObjectProtocol? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: MaleObjectProtocol? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: EarthPartyDelegate? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var accessible = false
    @objc dynamic var accessible = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = DisagreeableWomanSocketManager()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(changeover(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(againThird),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(main_skipId)))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (PhallicApplication.shared as! PhallicApplication).captureSpringTimer()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            .observeWeakly(Bool.self, "accessible")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (PhallicApplication.shared as! PhallicApplication).captureSpringTimer()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension DisagreeableWomanSocketManager: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        CountegrityThen.exterminate(msg: (String(noti_attachFoodData) + userHimName))
        //: sendPing()
        lock()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: notiToId,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: userTurnKey)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        discourse()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        CountegrityThen.exterminate(msg: (String(const_propertyFormat) + showAgoCosStr.capitalized + String(userCollectionTitle.suffix(6))) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = MentionTriggerAppManager.share.reachability?.connection, status != .unavailable else {
        guard let status = MentionTriggerAppManager.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == app_workBeginData {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = ownWill(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            CountegrityThen.exterminate(msg: (noti_onceMessage.replacingOccurrences(of: "reader", with: "C") + String(mainBeyondMsg)) + "\(app_workBeginData)" + (String(dataOvalName)) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.timeMemberCounterruptionVariationScaleBlade(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.controlResistance(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        CountegrityThen.exterminate(msg: (String(user_pastKey.prefix(7)) + String(dataTermsPath.prefix(6)) + "ode:") + "\(code)" + (mainAdministrativePathFormat.capitalized + String(user_delayTitle.prefix(7))) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.utilizerFeature(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == show_profileKey {
            //: return
            return
        }
        //: if let status = MentionTriggerAppManager.share.reachability?.connection, status != .unavailable {
        if let status = MentionTriggerAppManager.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.timeMemberCounterruptionVariationScaleBlade(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func ownWill(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = MentionTriggerAppManager.share.appConfigMode.ws
        self.linkAddrArr = MentionTriggerAppManager.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = exposureArr()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: userTurnKey)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = MentionTriggerAppManager.share.loginUid
        let uid = MentionTriggerAppManager.share.loginUid
        //: let sid = MentionTriggerAppManager.share.loginSessionId
        let sid = MentionTriggerAppManager.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(data_purchaseUrl.suffix(5)) + String(userCenterKey)) + "\(sid)" + (String(noti_requestReactStr.prefix(5))) + "\(uid)" + (String(k_partDismissData) + show_mannerData.replacingOccurrences(of: "current", with: "ph") + String(user_pressureName.suffix(5)) + "sion=") + "\(userDoingId)" + (String(user_siblingId.suffix(6)) + String(const_eventData)) + "\(user_underFormat)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func exposureArr() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return k_homeContent ? [self.getAddress()] : [self.getAddress2()]
    }
    
    func getAddress() -> String {
        return (String(user_tabUrl) + String(k_nameData.suffix(3))) + "\(dataCancelMessage)" + (String(noti_canHarassmentMsg.suffix(6)) + data_normallyContent.capitalized)
    }
    
    func getAddress2() -> String {
        return (showSweetText.replacingOccurrences(of: "direction", with: "2") + ".185." + dataPlusTitle.replacingOccurrences(of: "stock", with: "77"))
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension DisagreeableWomanSocketManager {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func changeover(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            CountegrityThen.exterminate(msg: String(bytes: data_comprehensiveValue.map{$0^19}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.utilizerFeature(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.timeMemberCounterruptionVariationScaleBlade(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func againThird() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.timeMemberCounterruptionVariationScaleBlade(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.turnData()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension DisagreeableWomanSocketManager {
    /// socket保活心跳包
    //: private func sendPing() {
    private func lock() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: notiNextUrl, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.postAm(info: [(String(mainDrownText)): (const_startData.replacingOccurrences(of: "broadcast", with: "pi"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func fileLaunch() {
        //: endTalkingPing()
        dialogue()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: notiImageVideoTitle, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.accessible || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currAccessibleVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currAccessibleVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.accessible || (self.transplant()?.isKind(of: InPublicViewDelegate.self) == false && self.transplant()?.isKind(of: EyebrowTopicViewController.self) == false) && (self.currAccessibleVC?.isKind(of: InPublicViewDelegate.self) == false && self.currAccessibleVC?.isKind(of: EyebrowTopicViewController.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.postAm(info: [(String(mainDrownText)): (k_insideData.replacingOccurrences(of: "crop", with: "c") + String(showHelpPureValue.prefix(4)) + "rtbeat")])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func dialogue() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func turnData() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(noti_planId.replacingOccurrences(of: "resistance", with: "r") + "evedDate")] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < notiChangeFormat {
                //: MentionTriggerAppManager.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                MentionTriggerAppManager.share.titleInfo(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension DisagreeableWomanSocketManager {
    /// 开启直播心跳包
    //: func startLivePing() {
    func recuperateDownwardly() {
        //: endLivePing()
        earlyPing()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: kMatchTitle, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.postAm(info: [(String(mainDrownText)): (show_thenName.replacingOccurrences(of: "market", with: "l") + String(data_formerFormat))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func earlyPing() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension DisagreeableWomanSocketManager {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func discourse() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = CoreContainerView()
        //: let keyWindow = EverlastinglyReactiveCompatible.getWindow()
        let keyWindow = EverlastinglyReactiveCompatible.insideWindow()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.accessible
        self.containerView?.isHidden = !self.accessible
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func specialise() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard AlwaysDiskPushListener.aInmate().isLive == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension DisagreeableWomanSocketManager {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func utilizerFeature(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (const_dragPath.replacingOccurrences(of: "no", with: "关") + String(show_barrierData)))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: show_profileKey, reason: (String(app_acquireTitle) + String(mainContrastName.suffix(8))))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        dialogue()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            specialise()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func layerSocial() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = DisagreeableWomanSocketManager.shared.ownWill(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.timeMemberCounterruptionVariationScaleBlade(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func timeMemberCounterruptionVariationScaleBlade(timeInterval: TimeInterval, type: CommissionType) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            CountegrityThen.exterminate(msg: (String(appAgencyValue.prefix(7))) + "\(type.rawValue)" + (k_foodKey.capitalized + "\u{8bd5}\u{6b21}数\u{ff1a}") + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.purchaseMid(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func purchaseMid(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                CountegrityThen.exterminate(msg: (main_recommendProMsg.uppercased() + "T 连接" + kPositMsg + "重新\u{8fde}\u{63a5}."))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                CountegrityThen.exterminate(msg: (appStandName.replacingOccurrences(of: "handle", with: "SO") + "T 正\u{5728}" + String(user_tableData)))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.accessible {
        if self.accessible {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(kTechnologyStr) + String(notiPlanRemindMsg)))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(userStripPresentValue.suffix(6))) + "\(ProjectionThen.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        utilizerFeature(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = userScaleUrl
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        CountegrityThen.exterminate(msg: (user_storageGoodData.replacingOccurrences(of: "account", with: "OC") + "T 开始" + kOfName + constAreaBodyValue.replacingOccurrences(of: "extra", with: "：")) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func postAm(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if MentionTriggerAppManager.share.reachability?.connection == nil || MentionTriggerAppManager.share.reachability?.connection == .unavailable {
        if MentionTriggerAppManager.share.reachability?.connection == nil || MentionTriggerAppManager.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(String(mainDrownText)): (String(const_anonContent.prefix(7)) + show_lengthPath.replacingOccurrences(of: "description", with: "e")),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (userFairData.replacingOccurrences(of: "meeting", with: "o")): "\(app_momentValue)",
                                       //: "data": [:]]
                                       (constNetValue.replacingOccurrences(of: "connect", with: "a")): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            controlResistance(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(mainDrownText))] as? String == (const_startData.replacingOccurrences(of: "broadcast", with: "pi")) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                CountegrityThen.exterminate(msg: (String(user_upDigitalText) + String(userThreePath) + String(mainBeneathPath.suffix(8))))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(String(mainDrownText))] as? String == (k_insideData.replacingOccurrences(of: "crop", with: "c") + String(showHelpPureValue.prefix(4)) + "rtbeat") {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                CountegrityThen.exterminate(msg: String(bytes: noti_rejectPath.map{needBefore(thread: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(String(mainDrownText))] as? String == (show_thenName.replacingOccurrences(of: "market", with: "l") + String(data_formerFormat)) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                CountegrityThen.exterminate(msg: String(bytes: data_pullKey.map{seaQuote(mail: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(mainDrownText))] as? String == (const_startData.replacingOccurrences(of: "broadcast", with: "pi")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(String(mainDrownText)): (String(data_problemText)),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (userFairData.replacingOccurrences(of: "meeting", with: "o")): "\(notiDeviceTitle)",
                                           //: "data": [:]]
                                           (constNetValue.replacingOccurrences(of: "connect", with: "a")): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                controlResistance(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.timeMemberCounterruptionVariationScaleBlade(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func controlResistance(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(String(mainDrownText))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(userFairData.replacingOccurrences(of: "meeting", with: "o"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(app_micStr.replacingOccurrences(of: "pack", with: "g"))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(constNetValue.replacingOccurrences(of: "connect", with: "a"))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (main_donationUrl + "\u{6d88}息\u{ff1a}cm" + String(dataDonVictoryWoodMessage.suffix(6))) + "\(cmdName)" + (String(noti_markValue.suffix(5)) + String(notiPastConstraintName)) + "\(errorNo)" + (String(kTotalPath)) + "\(msg)" + (String(showCompleteValue)) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        doingceBold(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            CountegrityThen.exterminate(msg: (String(userCenterName) + app_makerName.replacingOccurrences(of: "remark", with: "d") + String(mainPartMakeMsg)) + "\(cmdName)" + (String(noti_markValue.suffix(5)) + String(notiPastConstraintName)) + "\(errorNo)" + (String(kTotalPath)) + "\(msg)" + (String(showCompleteValue)) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: MentionTriggerAppManager.share.loginUserMode.freeCallTimes = 0
                MentionTriggerAppManager.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !MentionTriggerAppManager.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !MentionTriggerAppManager.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                PosternWindowManager.shared.betweenScope()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.visual(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(user_positKey)) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: showControlMessage, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(constMinimizeCustomMessage.suffix(7)) + String(showItStr)) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.headFeedback(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(main_yearName)) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.temporary(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(app_observeMessage.prefix(5)) + "celC" + constAuthorityMsg.replacingOccurrences(of: "exposure", with: "l")) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(notiJudgeLessName.prefix(6)) + String(showMatchName)) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(notiEditFormat.prefix(6)) + String(const_artifactKey.prefix(6))) { // 接收方发送acceptCall回执
            //: self.accessible = true
            self.accessible = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            fileLaunch()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.localEntitle(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(kBounceStr.prefix(4)) + "opCall") { // 接收方发送stopCall回执
            //: self.accessible = false
            self.accessible = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            dialogue()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(const_beautifulMessage.replacingOccurrences(of: "explore", with: "re") + "Call" + String(dataAddId.suffix(4)) + String(dataToneWillUrl))] as? Int {
                //: MentionTriggerAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
                MentionTriggerAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: MentionTriggerAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            MentionTriggerAppManager.share.loginUserMode.mf_coin = json[(constNetValue.replacingOccurrences(of: "connect", with: "a"))][(String(constSimplyFormat))].stringValue

            //: inviteCallPopViewEndShow(data: data)
            someIndicator(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: dataProductionRouteKey, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (PhallicApplication.shared as! PhallicApplication).tag()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(appWithinTwoUrl.prefix(4)) + "ssCo" + String(const_detailedSourceTitle)) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.followHome(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (noti_minimizeName.replacingOccurrences(of: "meeting", with: "ac") + "ptCall") { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.accessible = true
            self.accessible = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            fileLaunch()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.visitor(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (dataReactFormat.lowercased() + "Call") { // 接收方/拨打方 挂断了通话
            //: self.accessible = false
            self.accessible = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            dialogue()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.top(data: data)
            // 更新余额
            //: MentionTriggerAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            MentionTriggerAppManager.share.loginUserMode.mf_coin = json[(constNetValue.replacingOccurrences(of: "connect", with: "a"))][(String(constSimplyFormat))].stringValue
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(const_beautifulMessage.replacingOccurrences(of: "explore", with: "re") + "Call" + String(dataAddId.suffix(4)) + String(dataToneWillUrl))] as? Int {
                //: MentionTriggerAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
                MentionTriggerAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            //: inviteCallPopViewEndShow(data: data)
            someIndicator(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: dataProductionRouteKey, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (PhallicApplication.shared as! PhallicApplication).tag()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (String(app_ditName)) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: constEventShowMsg, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: MentionTriggerAppManager.share.recieve1v1TalkRequest(info: data)
                MentionTriggerAppManager.share.titleInfo(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(noti_planId.replacingOccurrences(of: "resistance", with: "r") + "evedDate")] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: user_disappearMsg.reversed(), encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(constNetValue.replacingOccurrences(of: "connect", with: "a"))][(String(data_replyValue))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: app_carrierStr.map{leastFollowing(used: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                PullTogetherDelegateHelper.lineShadow(uid: String(json[(constNetValue.replacingOccurrences(of: "connect", with: "a"))][(String(appTolerancePath))].intValue),
                                             //: title: data["nickname"] as? String,
                                             title: data[(String(noti_accountingId))] as? String,
                                             //: body: body,
                                             body: body,
                                             //: imageUrl: data["headPic"] as? String)
                                             imageUrl: data[(String(dataAdminNorthRobotKey.suffix(6)) + String(userHungMessage))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                PullTogetherDelegateHelper.shared.teeEnable()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(main_maxLockData) + String(noti_tapPath)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.accessible = true
            self.accessible = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            fileLaunch()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(main_maxLockData) + String(noti_tapPath))] = "1"
            //: MentionTriggerAppManager.share.recieve1v1TalkResumeRequest(info: resumeData)
            MentionTriggerAppManager.share.curriculum(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (PhallicApplication.shared as! PhallicApplication).captureSpringTimer()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (show_informationBlindCoordinateStr.replacingOccurrences(of: "body", with: "n") + String(notiNetValue.suffix(5))) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.accessible = false
            self.accessible = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            dialogue()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.socket(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                PullTogetherDelegateHelper.eraseIdentifier(identifier: String(json[(constNetValue.replacingOccurrences(of: "connect", with: "a"))][(String(appTolerancePath))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                PullTogetherDelegateHelper.shared.amActive()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (String(const_brushKey) + const_provideValue.lowercased()) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.accessible = false
            self.accessible = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            dialogue()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.back(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(mainPicName.prefix(5)) + "lMot" + notiQualityFormat.replacingOccurrences(of: "strategy", with: "va")) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if MentionTriggerAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if MentionTriggerAppManager.share.loginUserMode.sex == TopicKeyRepresentable.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.aboveSocket?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (k_heavyEarthContent.replacingOccurrences(of: "target", with: "n") + String(mainBenefitKey.prefix(4)) + "Warning") { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            AlwaysDiskPushListener.domicileTaTime(content: msg, reason: json[(constNetValue.replacingOccurrences(of: "connect", with: "a"))][(userSimultaneouslyKey.replacingOccurrences(of: "record", with: "e"))].stringValue, time: json[(constNetValue.replacingOccurrences(of: "connect", with: "a"))][(notiExecuteAtStr.replacingOccurrences(of: "sufficient", with: "me"))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(appHowTitle) + String(showCosUrl)) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            AlwaysDiskPushListener.aInmate().medietyResource()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(noti_heelFormat.prefix(4)) + "ndPro" + String(const_curTitle.suffix(5)) + notiRidBackKey.replacingOccurrences(of: "situation", with: "if")) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.noExtra(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(constHandNoticeKey) + String(constPlatformUrl.prefix(6))) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(k_purchaseValue) + constGrowthEngageMessage.replacingOccurrences(of: "succeed", with: "us"))] as? Int ?? -1
            //: if MentionTriggerAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if MentionTriggerAppManager.share.appStatus == AidNameConvertible.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: notiHundredName, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: showControlMessage, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: notiHundredName, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(appBeamTitle) + String(data_votePath.suffix(4)) + "lChatMsg") { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            SimplyThen.fileSave().cosPop(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(const_popBeginName) + String(showPencilFormat.prefix(6))) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            PosternWindowManager.shared.pastAppropriate(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(show_logFormat.suffix(4)) + "erBlock") { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            AlwaysDiskPushListener.aInmate().medietyResource()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: const_captureId, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension DisagreeableWomanSocketManager {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func doingceBold(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (String(app_ditName)) || cmdName == (String(main_yearName)) {
            //: if TalkingLiveManager.shared().isLive == true {
            if AlwaysDiskPushListener.aInmate().isLive == true {
                //: NEED_RESTORE_LIVE = true
                appMeFormat = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                AlwaysDiskPushListener.skip(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                AlwaysDiskPushListener.aInmate().medietyResource()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard appMeFormat == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (show_informationBlindCoordinateStr.replacingOccurrences(of: "body", with: "n") + String(notiNetValue.suffix(5))) || cmdName == (String(app_observeMessage.prefix(5)) + "celC" + constAuthorityMsg.replacingOccurrences(of: "exposure", with: "l")) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (String(const_brushKey) + const_provideValue.lowercased()) || cmdName == (String(notiJudgeLessName.prefix(6)) + String(showMatchName)) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (dataReactFormat.lowercased() + "Call") || cmdName == (String(kBounceStr.prefix(4)) + "opCall")
        {
            //: NEED_RESTORE_LIVE = false
            appMeFormat = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = StillThen(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                AlwaysDiskPushListener.aInmate().beforeWait()
            }
            //: alert.show()
            alert.prepareGift()
        }
    }

    /// 邀请通话通话结束之后，判断弹窗类型
    //: private func inviteCallPopViewEndShow(data: [String: Any]) {
    private func someIndicator(data: [String: Any]) {
        // 展示评分弹窗
        //: AppStoreRatingManager.shared.requestQuickRating()
        MarketRatingManager.shared.quick()
        //: guard MentionTriggerAppManager.share.loginUserMode.sex == Gender.male.rawValue else {
        guard MentionTriggerAppManager.share.loginUserMode.sex == TopicKeyRepresentable.male.rawValue else {
            //: return
            return
        }
        // 弹出优惠充值弹窗
        //: if (data["isRecharge"]) as? Bool == false {
        if data[(String(kActiveUrl.suffix(4)) + "charg" + data_aspectWhetherTransferPath.replacingOccurrences(of: "turn", with: "e"))] as? Bool == false {
            //: TalkingAppPushManager.share.func_pushToDiscountRechargeView()
            RiseMalePushManager.share.discountByTranslation()
        }
        // 半屏充值弹窗场景
        //: let source = data["source"] as? String
        let source = data[(String(dataGuideStr))] as? String
        //: guard source == "invitePop" || source == "videoPop" else {
        guard source == (String(notiWorkerText)) || source == (String(data_partFormat) + String(data_airLayerName)) else {
            //: return
            return
        }
        //: if (data["coin"] as? Int) ?? 0 < 60 {
        if (data[(notiClueRecognizeMsg.replacingOccurrences(of: "orientation", with: "in"))] as? Int) ?? 0 < 60 {
            //: TalkingAppPushManager.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            RiseMalePushManager.share.dealMini(webViewType: .RechargeHalfPage)
        }
    }
}
