
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let constCapitalStateUrl:[UInt8] = [0xdd,0xda,0xdd,0xc0,0x9c,0xd7,0xdb,0xd0,0xd1,0xc6,0x8e,0x9d,0x94,0xdc,0xd5,0xc7,0x94,0xda,0xdb,0xc0,0x94,0xd6,0xd1,0xd1,0xda,0x94,0xdd,0xd9,0xc4,0xd8,0xd1,0xd9,0xd1,0xda,0xc0,0xd1,0xd0]

private func helpOver(require num: UInt8) -> UInt8 {
    return num ^ 180
}

/*: "Voice greeting" :*/
fileprivate let appSequenceId:[Character] = ["V","o","i","c","e"," ","g","r","e","e","t","i"]
fileprivate let dataPrimaryUrl:String = "nlevel"

/*: "Add" :*/
fileprivate let app_windUrl:[Character] = ["A","d","d"]

/*: "type" :*/
fileprivate let user_attributeData:[Character] = ["t","y","p","e"]

/*: "addStatus" :*/
fileprivate let userSizeValue:String = "regular bird yellow offaddSt"

/*: "list" :*/
fileprivate let notiSweepValue:[Character] = ["l","i","s","t"]

/*: "audio" :*/
fileprivate let notiHolderUrl:String = "audenvironmento"

/*: "length" :*/
fileprivate let mainSoundUrl:String = "lenportraitt"
fileprivate let data_usedId:[Character] = ["h"]

/*: "url" :*/
fileprivate let user_secureId:String = "urdelivery"

/*: "Successfully added, please wait for approval" :*/
fileprivate let showStuffPath:[UInt8] = [0x13,0x35,0x23,0x23,0x25,0x33,0x33,0x26,0x35,0x2c,0x2c,0x39,0x60,0x21,0x24,0x24,0x25,0x24,0x6c,0x60,0x30,0x2c,0x25,0x21,0x33,0x25,0x60,0x37,0x21,0x29,0x34,0x60,0x26,0x2f,0x32,0x60,0x21,0x30,0x30,0x32,0x2f,0x36,0x21,0x2c]

private func rushGroup(now num: UInt8) -> UInt8 {
    return num ^ 64
}

/*: "Are you sure to delete this message？" :*/
fileprivate let appNameId:[UInt8] = [0x21,0x52,0x45,0x0,0x59,0x4f,0x55,0x0,0x53,0x55,0x52,0x45,0x0,0x54,0x4f,0x0,0x44,0x45,0x4c,0x45,0x54,0x45,0x0,0x54,0x48,0x49,0x53,0x0,0x4d,0x45,0x53,0x53,0x41,0x47,0x45,0xcf,0x9c,0x7f]

fileprivate func beanSweep(suspend num: UInt8) -> UInt8 {
    let value = Int(num) - 224
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Cancel" :*/
fileprivate let app_attachRareName:[Character] = ["C","a","n","c","e","l"]

/*: "Delete" :*/
fileprivate let app_equitySessionUrl:[Character] = ["D","e","l","e","t","e"]

/*: "Please wait for the audit result" :*/
fileprivate let const_birthdayId:[UInt8] = [0x9f,0xa3,0xaa,0xae,0xbc,0xaa,0xef,0xb8,0xae,0xa6,0xbb,0xef,0xa9,0xa0,0xbd,0xef,0xbb,0xa7,0xaa,0xef,0xae,0xba,0xab,0xa6,0xbb,0xef,0xbd,0xaa,0xbc,0xba,0xa3,0xbb]

private func delayReminder(ask num: UInt8) -> UInt8 {
    return num ^ 207
}

/*: "The upper limit has been reached" :*/
fileprivate let showPropertyFormat:[UInt8] = [0x64,0x65,0x68,0x63,0x61,0x65,0x72,0x20,0x6e,0x65,0x65,0x62,0x20,0x73,0x61,0x68,0x20,0x74,0x69,0x6d,0x69,0x6c,0x20,0x72,0x65,0x70,0x70,0x75,0x20,0x65,0x68,0x54]

/*: "filePath" :*/
fileprivate let noti_factorStr:[Character] = ["f","i","l","e","P"]
fileprivate let mainGravityValue:String = "alip"

/*: "time" :*/
fileprivate let data_adId:String = "timbuilder"

/*: "#FF506D" :*/
fileprivate let user_accommodateFleetMessage:[Character] = ["#","F","F","5","0","6"]
fileprivate let dataContendValue:String = "total"

/*: "btn_message_delete_nor" :*/
fileprivate let userRushSpringUrl:String = "btn_birthday aircraft restriction over scope"
fileprivate let kWorkingFormat:String = "cap pute_de"
fileprivate let app_ehValue:String = "lete_norlink sale min shade"

/*: "Currently mute" :*/
fileprivate let showBeneathMsg:String = "Currenstatute follow"
fileprivate let appEarningsLicenseReferId:String = "murequiree"

/*: "Edit Title" :*/
fileprivate let mainRetText:String = "faculty dialogueEdit Tit"
fileprivate let showBookPath:String = "lfeed"

/*: "remark" :*/
fileprivate let dataWordFormat:String = "rpreviousar"
fileprivate let mainTransferUrl:[Character] = ["k"]

/*: "Click the button below to add\ncontent" :*/
fileprivate let showComputerLiveFormat:[UInt8] = [0xb4,0xdd,0xda,0xd4,0xdc,0x91,0xe5,0xd9,0xd6,0x91,0xd3,0xe6,0xe5,0xe5,0xe0,0xdf,0x91,0xd3,0xd6,0xdd,0xe0,0xe8,0x91,0xe5,0xe0,0x91,0xd2,0xd5,0xd5,0x7b,0xd4,0xe0,0xdf,0xe5,0xd6,0xdf,0xe5]

fileprivate func shallLay(arc num: UInt8) -> UInt8 {
    let value = Int(num) + 143
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited." :*/
fileprivate let dataFacultyName:[UInt8] = [0x2e,0x64,0x65,0x74,0x69,0x64,0x65,0x20,0x65,0x62,0x20,0x6e,0x61,0x63,0x20,0x65,0x6c,0x74,0x69,0x74,0x20,0x65,0x63,0x69,0x6f,0x76,0x20,0x65,0x68,0x74,0x20,0x64,0x6e,0x61,0x20,0x2c,0x74,0x69,0x20,0x65,0x74,0x65,0x6c,0x65,0x64,0x20,0x6f,0x74,0x20,0x61,0x65,0x72,0x61,0x20,0x65,0x63,0x69,0x6f,0x76,0x20,0x65,0x68,0x74,0x20,0x6f,0x74,0x20,0x65,0x64,0x69,0x6c,0x73,0x20,0x2c,0x65,0x63,0x69,0x6f,0x76,0x20,0x65,0x68,0x74,0x20,0x79,0x61,0x6c,0x70,0x20,0x6f,0x74,0x20,0x65,0x6c,0x62,0x62,0x75,0x62,0x20,0x65,0x68,0x74,0x20,0x6b,0x63,0x69,0x6c,0x43,0xa,0x3b,0x73,0x6e,0x72,0x75,0x74,0x20,0x79,0x62,0x20,0x73,0x65,0x67,0x61,0x73,0x73,0x65,0x6d,0x20,0x65,0x63,0x69,0x6f,0x76,0x20,0x64,0x6e,0x65,0x73,0x20,0x6c,0x6c,0x69,0x77,0x20,0x6d,0x65,0x74,0x73,0x79,0x73,0x20,0x65,0x68,0x74,0x20,0x64,0x6e,0x61,0x20,0x2c,0x73,0x67,0x6e,0x69,0x74,0x65,0x65,0x72,0x67,0x20,0x65,0x63,0x69,0x6f,0x76,0x20,0x65,0x6c,0x70,0x69,0x74,0x6c,0x75,0x6d,0x20,0x64,0x64,0x41]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  RiseThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/9.
//

//: import UIKit
import UIKit

//: class TalkingGreetAudioVC: TalkingBaseViewController {
class RiseThen: EverlastinglyThen {
    //: var addStatus = -1
    var addStatus = -1 // 1：可以添加；2：已有正在审核中得音频，不能添加；3：已有20条审核通过的音频，不能添加；4：已有20条审核通过的文字，不能添加；5：已有20条审核中或审核通过的图片，不能添加；
    //: var recordView: TalkingGreetRecordAudioView?
    var recordView: AidView?
    //: var isPlaying = false
    var isPlaying = false
    //: var playIndex: IndexPath?
    var playIndex: IndexPath?
    //: var player: TalkingAudioPlayTool?
    var player: HumanFaceForthPlayTool?
    //: var isMute = false
    var isMute = false
    //: var currenCell: TalkingGreetAudioCell?
    var currenCell: IssueOutsideAudioCell?
    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: constCapitalStateUrl.map{helpOver(require: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Voice greeting".localized
        self.title = (String(appSequenceId) + dataPrimaryUrl.replacingOccurrences(of: "level", with: "g")).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.appColor()
        //: designView()
        designViewCalled()
        //: getAudioListData()
        successfullyAccumulation()
        //: setMutedDetection()
        refuseBox()
        //: NotificationCenter.default.addObserver(self, selector: #selector(cancelPlayerAudio(notification:)), name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(audioDraw(notification:)), name: constEventShowMsg, object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(interruptionChange(notification:)), name: AVAudioSession.interruptionNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(corner(notification:)), name: AVAudioSession.interruptionNotification, object: nil)

        // 添加接收到音视频通话通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(stopPlay),
                                               selector: #selector(build),
                                               //: name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION,
                                               name: constEventShowMsg,
                                               //: object: nil)
                                               object: nil)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: if isPlaying {
        if isPlaying {
            //: self.player?.stopPlay()
            self.player?.rewardRedFun()
        }
    }

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [GateMeasurable] = {
        //: var array = [TalkingGreetModel]()
        var array = [GateMeasurable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: show_objectValue, height: constBlockIdentityContent - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(app_windUrl)).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.landEnableSize(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.tillOfCreation(colors: UIColor.spectralColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(server), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetAudioVC {
extension RiseThen {
    //: func getAudioListData() {
    func successfullyAccumulation() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(String(user_attributeData))] = 1
        //: ProgressHUD.show()
        LovelyLifetimeProgressHUD.launchSocialShow()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        MeBeThen.completionForOf(params: dict) { [weak self] succeed, result, _ in

            //: ProgressHUD.dismiss()
            LovelyLifetimeProgressHUD.arcCurDismiss()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.trail()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(userSizeValue.suffix(5)) + "atus")] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(String(notiSweepValue))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [GateMeasurable] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<GateMeasurable>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.player = nil
                self.player = nil
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func requestUploadAudioFileData(data: NSData, time: String) {
    func face(data: NSData, time: String) {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(String(user_attributeData))] = 1
        //: dict["audio"] = data
        dict[(notiHolderUrl.replacingOccurrences(of: "environment", with: "i"))] = data
        //: dict["length"] = Int(time)
        dict[(mainSoundUrl.replacingOccurrences(of: "portrait", with: "g") + String(data_usedId))] = Int(time)

        //: ProgressHUD.show()
        LovelyLifetimeProgressHUD.launchSocialShow()
        //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) {[weak self] succeed, result, errorModel in
        MeBeThen.frameBit(params: dict) { [weak self] _, result, _ in

            //: ProgressHUD.dismiss()
            LovelyLifetimeProgressHUD.arcCurDismiss()
            //: guard let self = self else {return}
            guard let self = self else { return }

            //: guard let dict = result as? Dictionary<String, Any> else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let url: String = dict["url"] as! String
            let url: String = dict[(user_secureId.replacingOccurrences(of: "delivery", with: "l"))] as! String
            //: if !url.isEmptyString {
            if !url.isEmptyString {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.noExtra(showMsg: String(bytes: showStuffPath.map{rushGroup(now: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.successfullyAccumulation()
                //: self.recordView?.dismiss()
                self.recordView?.process()
                //: self.recordView = nil
                self.recordView = nil
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func tiedUp(index: Int) {
        //: let config = ShowAlertConfig()
        let config = NumberegrityAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.landEnableSize(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        StandMarketThen.objectPresent(message: String(bytes: appNameId.map{beanSweep(suspend: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(app_attachRareName)).localized, rightBtnTitle: (String(app_equitySessionUrl)).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            StandMarketThen.alertCur()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            StandMarketThen.alertCur()
            //: self.stopPlay()
            self.build()

            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: GateMeasurable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            MeBeThen.setPer(params: dict) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.successfullyAccumulation()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetAudioVC {
extension RiseThen {
    //: @objc func cancelPlayerAudio(notification: NSNotification) -> Void {
    @objc func audioDraw(notification _: NSNotification) {
        //: stopPlay()
        build()
    }

    /// 播放过程中中断
    //: @objc func interruptionChange(notification: NSNotification) -> Void {
    @objc func corner(notification: NSNotification) {
        //: let dic = notification.userInfo as![String: AnyObject]
        let dic = notification.userInfo as! [String: AnyObject]

        //: let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        //: if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
        if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
            //: stopPlay()
            build()
        }
    }

    //: @objc func addButtonClick() {
    @objc func server() {
        //: guard TalkingSocketManager.shared.accessible == false else {
        guard DisagreeableWomanSocketManager.shared.accessible == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.outBarMsg(showMsg: main_maxPath)
            //: return
            return
        }

        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        GatePermissionTool.extent(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.outBarMsg(showMsg: dataStatusKey)
                //: return
                return
            }
            //: if self.addStatus == 2 {
            if self.addStatus == 2 {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please wait for the audit result".localized)
                self.outBarMsg(showMsg: String(bytes: const_birthdayId.map{delayReminder(ask: $0)}, encoding: .utf8)!.localized)
                //: return
                return
                    //: }else if (self.addStatus == 3) {
            } else if self.addStatus == 3 {
                //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
                self.outBarMsg(showMsg: String(bytes: showPropertyFormat.reversed(), encoding: .utf8)!.localized)
                //: return
                return
            }
            //: self.stopPlay()
            self.build()
            //: self.recordView = TalkingGreetRecordAudioView.init(frame: self.view.frame)
            self.recordView = AidView(frame: self.view.frame)
            //: self.recordView?.show()
            self.recordView?.towardDotShow()
            //: self.recordView?.audioFileUpload = {[weak self] dict in
            self.recordView?.audioFileUpload = { [weak self] dict in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let data = NSData.init(contentsOfFile: dict["filePath"] as! String)
                let data = NSData(contentsOfFile: dict[(String(noti_factorStr) + mainGravityValue.replacingOccurrences(of: "lip", with: "th"))] as! String)
                //: guard data != nil else {
                guard data != nil else {
                    //: return
                    return
                }
                //: self.requestUploadAudioFileData(data: data!, time: dict["time"] as! String)
                self.face(data: data!, time: dict[(data_adId.replacingOccurrences(of: "builder", with: "e"))] as! String)
            }
        }
    }
}

//: extension TalkingGreetAudioVC {
extension RiseThen {
    //: func setMutedDetection() {
    func refuseBox() {
        //: RBDMuteSwitch.shared.detectMuteSwitch()
        ShotThen.shared.stat()
        //: RBDMuteSwitch.shared.isMutedBlock = { [weak self] mute in
        ShotThen.shared.isMutedBlock = { [weak self] mute in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.isMute = mute
            self.isMute = mute
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetAudioVC: UITableViewDelegate, UITableViewDataSource {
extension RiseThen: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return 101
        return 101
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = TalkingGreetAudioCell.className()
        let identifier = IssueOutsideAudioCell.className()
        //: let cell: TalkingGreetAudioCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetAudioCell
        let cell: IssueOutsideAudioCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! IssueOutsideAudioCell

        //: cell.setGreetAudioCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.marshalRowAudioComputerSimulationPresentPastCompartment(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)

        //: if isPlaying && playIndex?.row == indexPath.section {
        if isPlaying && playIndex?.row == indexPath.section {
            //: cell.syncPlayState(isPlaying: true)
            cell.genitor(isPlaying: true)
            //: }else {
        } else {
            //: cell.syncPlayState(isPlaying: false)
            cell.genitor(isPlaying: false)
        }

        //: cell.audioPlayActionBlock = { [weak self] in
        cell.audioPlayActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: RBDMuteSwitch.shared.detectMuteSwitch()
            ShotThen.shared.stat()
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: self.playAudio(index: indexPath)
                self.creation(index: indexPath)
            }
        }
        //: cell.editActionBlock = { [weak self] in
        cell.editActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.stopPlay()
            self.build()
            //: self.editTitle(index: indexPath.section)
            self.join(index: indexPath.section)
        }
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(app_equitySessionUrl)).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.tiedUp(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(user_accommodateFleetMessage) + dataContendValue.replacingOccurrences(of: "total", with: "D")))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.tiedUp(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.lastTo(name: (String(userRushSpringUrl.prefix(4)) + "messag" + String(kWorkingFormat.suffix(4)) + String(app_ehValue.prefix(8)))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = LifetimeImage(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func playAudio(index: IndexPath) {
    func creation(index: IndexPath) {
        //: guard !TalkingSocketManager.shared.accessible else {
        guard !DisagreeableWomanSocketManager.shared.accessible else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.outBarMsg(showMsg: main_maxPath)
            //: return
            return
        }
        //: if isPlaying && playIndex == index {
        if isPlaying && playIndex == index {
            //: isPlaying = false
            isPlaying = false
            //: MainTable.reloadData()
            MainTable.reloadData()
            //: self.player?.stopPlay()
            self.player?.rewardRedFun()
            //: return
            return
        }
        //: if isPlaying {
        if isPlaying {
            //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
            currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? IssueOutsideAudioCell
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.genitor(isPlaying: false)
            }
        }

        //: playIndex = index
        playIndex = index
        //: let model = self.dataSourceArr[index.section]
        let model = self.dataSourceArr[index.section]

        //: if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
        if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Currently mute".localized)
            self.outBarMsg(showMsg: (String(showBeneathMsg.prefix(6)) + "tly " + appEarningsLicenseReferId.replacingOccurrences(of: "require", with: "t")).localized)
            //: return
            return
        }
        //: if self.player == nil {
        if self.player == nil {
            //: self.player = TalkingAudioPlayTool.shared
            self.player = HumanFaceForthPlayTool.shared
            //: self.player?.delegate = self
            self.player?.delegate = self
        }
        //: self.player?.stopPlay()
        self.player?.rewardRedFun()
        //: self.player?.play(urlStr: model.content)
        self.player?.route(urlStr: model.content)
        //: isPlaying = true
        isPlaying = true
        //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
        currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? IssueOutsideAudioCell
        //: if currenCell != nil {
        if currenCell != nil {
            //: currenCell?.syncPlayState(isPlaying: true)
            currenCell?.genitor(isPlaying: true)
        }
    }

    //: func editTitle(index: Int) {
    func join(index: Int) {
        //: let view = TalkingEditAlertView.init(title: "Edit Title".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, config: nil)
        let view = ProgramTipThen(title: (String(mainRetText.suffix(8)) + showBookPath.replacingOccurrences(of: "feed", with: "e")).localized, leftBtnTitle: (String(app_attachRareName)).localized, rightBtnTitle: "OK".localized, config: nil)
        //: view.show()
        view.thread()

        //: view.rightBlock = { [weak self] text in
        view.rightBlock = { [weak self] text in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: ProgressHUD.show()
            LovelyLifetimeProgressHUD.launchSocialShow()

            //: var model: TalkingGreetModel = self.dataSourceArr[index]
            var model: GateMeasurable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: dict["remark"] = text
            dict[(dataWordFormat.replacingOccurrences(of: "previous", with: "em") + String(mainTransferUrl))] = text

            //: TalkingMeRequestTool.req_UpdateAudioRemark(params: dict) { succeed, result, errorModel in
            MeBeThen.proposal(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                LovelyLifetimeProgressHUD.arcCurDismiss()
                //: if succeed {
                if succeed {
                    //: model.remark = text
                    model.remark = text
                    //: self.dataSourceArr[index] = model
                    self.dataSourceArr[index] = model
                    //: self.MainTable.reloadData()
                    self.MainTable.reloadData()
                }
            }
        }
    }

    //: @objc func stopPlay() {
    @objc func build() {
        //: if isPlaying {
        if isPlaying {
            //: isPlaying = false
            isPlaying = false
            //: self.player?.stopPlay()
            self.player?.rewardRedFun()
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.genitor(isPlaying: false)
            }
        }
    }
}

//: extension TalkingGreetAudioVC: TalkingAudioPlayToolDelegate {
extension RiseThen: AudioGuideThen {
    //: func playerStatusChange(status: AudioPlayerStatus) {
    func chessPlayerToSelfWorth(status: GrayGloryPlayerStatus) {
        //: switch status {
        switch status {
        //: case .PlayerFinish:
        case .PlayerFinish:
            //: stopPlay()
            build()
        //: break
        //: case .PlayerInterruption:
        case .PlayerInterruption:
            //: stopPlay()
            build()
        //: break
        //: default:
        default:
            //: break
            break
        }
    }
}

// MARK: - Layout

//: extension TalkingGreetAudioVC {
extension RiseThen {
    //: private func setupEmptyDataView() {
    private func trail() {
        //: var style = EmptyStyle()
        var style = StandardEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: showComputerLiveFormat.map{shallLay(arc: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .appColor()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func designViewCalled() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetAudioCell.self, forCellReuseIdentifier: TalkingGreetAudioCell.className())
        MainTable.register(IssueOutsideAudioCell.self, forCellReuseIdentifier: IssueOutsideAudioCell.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 145))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: show_objectValue, height: 145))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.modusOperandi()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.activeBeautyNative(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited.".localized
        let string = String(bytes: dataFacultyName.reversed(), encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
