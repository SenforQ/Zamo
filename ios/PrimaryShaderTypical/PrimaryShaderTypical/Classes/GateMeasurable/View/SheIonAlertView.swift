
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_whiteKey:[UInt8] = [0xcd,0xca,0xcd,0xd0,0x8c,0xc7,0xcb,0xc0,0xc1,0xd6,0x9e,0x8d,0x84,0xcc,0xc5,0xd7,0x84,0xca,0xcb,0xd0,0x84,0xc6,0xc1,0xc1,0xca,0x84,0xcd,0xc9,0xd4,0xc8,0xc1,0xc9,0xc1,0xca,0xd0,0xc1,0xc0]

/*: "Cancel" :*/
fileprivate let k_databaseMessage:[Character] = ["C","a","n","c","e","l"]

/*: "Submit" :*/
fileprivate let data_lifeMessage:[Character] = ["S","u","b","m","i"]
fileprivate let noti_whiteData:String = "enhance"

/*: "Please tell us your problems                   " :*/
fileprivate let userWaitId:[UInt8] = [0x3b,0x57,0x50,0x4c,0x5e,0x50,0xb,0x5f,0x50,0x57,0x57,0xb,0x60,0x5e,0xb,0x64,0x5a,0x60,0x5d,0xb,0x5b,0x5d,0x5a,0x4d,0x57,0x50,0x58,0x5e,0xb,0xb,0xb,0xb,0xb,0xb,0xb,0xb,0xb,0xb,0xb,0xb,0xb,0xb,0xb,0xb,0xb,0xb,0xb]

fileprivate func forEachSwitcheShared(family num: UInt8) -> UInt8 {
    let value = Int(num) + 21
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "You need to choose at least one." :*/
fileprivate let showHangStrengthContent:[UInt8] = [0xc9,0xff,0xe5,0xb0,0xfe,0xf5,0xf5,0xf4,0xb0,0xe4,0xff,0xb0,0xf3,0xf8,0xff,0xff,0xe3,0xf5,0xb0,0xf1,0xe4,0xb0,0xfc,0xf5,0xf1,0xe3,0xe4,0xb0,0xff,0xfe,0xf5,0xbe]

private func middleSchedule(slow num: UInt8) -> UInt8 {
    return num ^ 144
}

/*: "Please tell us your problems." :*/
fileprivate let data_equallyFormat:String = "Pleaone someone wait"
fileprivate let k_coatEnterFinishId:String = "l us ywe after"
fileprivate let kFireValue:String = "oblesignal"
fileprivate let mainImproveName:String = "s.report none trade forth extensive"

/*: "app/userReport" :*/
fileprivate let const_strategyStopThereTitle:String = "app/userfailure execute three manual"
fileprivate let notiMissKey:[Character] = ["R","e","p","o","r","t"]

/*: "reason" :*/
fileprivate let main_modeFormat:String = "reascommentn"

/*: "desc" :*/
fileprivate let showRequestStr:[UInt8] = [0x4e,0x4f,0x59,0x49]

private func baseballClub(allow num: UInt8) -> UInt8 {
    return num ^ 42
}

/*: "mf/moment/report" :*/
fileprivate let app_comprehensiveId:[Character] = ["m","f","/","m","o","m","e","n"]
fileprivate let user_youngName:[Character] = ["t","/","r","e","p","o","r","t"]

/*: "momentId" :*/
fileprivate let const_dismissKey:[UInt8] = [0xc,0xe,0xc,0x4,0xf,0x15,0x28,0x5]

private func earthMount(speed num: UInt8) -> UInt8 {
    return num ^ 97
}

/*: "mf/momentReply/report" :*/
fileprivate let k_numberTitle:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l"]
fileprivate let noti_propertyFieldId:String = "false view insert wonder breaky/r"
fileprivate let user_presentationStr:String = "eporshape"

/*: "app/videoReport" :*/
fileprivate let mainResponseFarOrganData:String = "tip bird him tickapp/"
fileprivate let const_toWriteTitle:String = "oReportway member"

/*: "logId" :*/
fileprivate let mainSinceId:[UInt8] = [0x2d,0x2e,0x26,0x8,0x25]

private func mobileCoat(sweet num: UInt8) -> UInt8 {
    return num ^ 65
}

/*: "Submitted successfully" :*/
fileprivate let data_lengthEhUrl:[Character] = ["S","u","b","m","i","t"]
fileprivate let kSameText:[Character] = ["t","e","d"," ","s","u","c","c","e","s","s","f","u","l","l","y"]

/*: "Nudity" :*/
fileprivate let app_decideValue:String = "interest drawingNudity"

/*: "Violence" :*/
fileprivate let const_locationData:[Character] = ["V","i","o","l","e"]
fileprivate let data_enableMsg:String = "NCE"

/*: "Harassment" :*/
fileprivate let k_peerName:String = "Harastry accelerate act"
fileprivate let constTenderOneTitle:[Character] = ["s","m","e","n","t"]

/*: "Suicide or Self-injury" :*/
fileprivate let kGroupValue:[Character] = ["S","u","i","c","i","d","e"," ","o","r"," ","S","e"]
fileprivate let user_coreContent:String = "restore artifact pretty commit parentlf-i"

/*: "False Information" :*/
fileprivate let kSecureTitle:[Character] = ["F","a","l","s","e"," ","I","n","f"]
fileprivate let data_exclusiveFormat:[Character] = ["o","r","m","a","t","i","o","n"]

/*: "Spam" :*/
fileprivate let user_bulletMenuStr:String = "Spamchoice body this continue he"

/*: "Hate Speech" :*/
fileprivate let kDenyTitle:String = "edition publish chemicalHate"
fileprivate let user_brideStr:[Character] = ["e","c","h"]

/*: "Terrorism" :*/
fileprivate let showAncientEffOkId:String = "Terrornobody wood"
fileprivate let dataSelectId:String = "isjump"

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let user_restName:[UInt8] = [0xdc,0xee,0xe3,0xea,0xaf,0xe0,0xe9,0xaf,0xc6,0xe3,0xe3,0xea,0xe8,0xee,0xe3,0xaf,0xe0,0xfd,0xaf,0xdd,0xea,0xe8,0xfa,0xe3,0xee,0xfb,0xea,0xeb,0xaf,0xc8,0xe0,0xe0,0xeb,0xfc]

/*: "Sending disgusting messages" :*/
fileprivate let data_searchText:String = "Sendimanage warning wave"
fileprivate let show_followTitle:String = "mutual meeting cigarettesting "

/*: "Using photos of nudity or drugs" :*/
fileprivate let main_norMessage:[UInt8] = [0xfb,0x19,0xf,0x14,0xd,0xc6,0x16,0xe,0x15,0x1a,0x15,0x19,0xc6,0x15,0xc,0xc6,0x14,0x1b,0xa,0xf,0x1a,0x1f,0xc6,0x15,0x18,0xc6,0xa,0x18,0x1b,0xd,0x19]

fileprivate func paraRocket(found num: UInt8) -> UInt8 {
    let value = Int(num) + 90
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Harassing or sending ads" :*/
fileprivate let appRespectPath:[Character] = ["H","a","r","a","s","s","i","n","g"," ","o","r"," ","s","e","n","d","i"]
fileprivate let noti_netName:String = "ng adsvalid video"

/*: "Defraud or using fake info" :*/
fileprivate let k_createName:String = "Defrauheavy need bind maximum"
fileprivate let data_reachStr:String = "written word harksing "
fileprivate let mainJungleName:[Character] = ["i","n","f","o"]

/*: "Others" :*/
fileprivate let constVidEyebrowName:[Character] = ["O","t","h","e","r"]
fileprivate let appGravityUrl:String = "chemical"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SheIonAlertView.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import Then
import Then
//: import UIKit
import UIKit

//: enum TalkingReportAlertType: CaseIterable {
enum AssistantCaseIterable: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class SheIonAlertView: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: AssistantCaseIterable?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [ConstraintFactoryThen] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = ConstraintFactoryThen()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: AssistantCaseIterable?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        myOn()
        //: setupSubViewsConstraint()
        ditConstraint()
        //: setupReasons()
        thanPerson()
        //: bindInteraction()
        menuTotaleraction()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_whiteKey.map{$0^164}, encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(k_databaseMessage)).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.thirdRevenue(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.landEnableSize(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.thirdRevenue().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(fragment), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(data_lifeMessage) + noti_whiteData.replacingOccurrences(of: "enhance", with: "t")).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.landEnableSize(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.tillOfCreation(colors: UIColor.spectralColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(give), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: show_objectValue - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.judgeAdvertising(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.appValueDetailColor(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = SheView(placeholder: String(bytes: userWaitId.map{forEachSwitcheShared(family: $0)}, encoding: .utf8)!.localized, placeholderColor: UIColor.modusOperandi(), frame: CGRect(x: 10, y: 10, width: show_objectValue - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .landEnableSize(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.roundChromaticColor()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .landEnableSize(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.judgeAdvertising(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(BringUpReactiveCompatible.self, forCellReuseIdentifier: BringUpReactiveCompatible.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension SheIonAlertView {
    //: @objc func func__TapGestureRecognizer() {
    @objc func rawPolicy() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func nogGo(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = EverlastinglyReactiveCompatible.getWindow()
            let window = EverlastinglyReactiveCompatible.insideWindow()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func sound(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func fragment() {
        //: self.dismissReportView(succeed: false)
        self.sound(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func give() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.outBarMsg(showMsg: String(bytes: showHangStrengthContent.map{middleSchedule(slow: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.outBarMsg(showMsg: (String(data_equallyFormat.prefix(4)) + "se tel" + String(k_coatEnterFinishId.prefix(6)) + "our pr" + kFireValue.replacingOccurrences(of: "signal", with: "m") + String(mainImproveName.prefix(2))).localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(const_strategyStopThereTitle.prefix(8)) + String(notiMissKey))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (main_modeFormat.replacingOccurrences(of: "comment", with: "o")): selectedModel.type, String(bytes: showRequestStr.map{baseballClub(allow: $0)}, encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(app_comprehensiveId) + String(user_youngName))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: const_dismissKey.map{earthMount(speed: $0)}, encoding: .utf8)!: tempId, (main_modeFormat.replacingOccurrences(of: "comment", with: "o")): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(k_numberTitle) + String(noti_propertyFieldId.suffix(3)) + user_presentationStr.replacingOccurrences(of: "shape", with: "t"))
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (main_modeFormat.replacingOccurrences(of: "comment", with: "o")): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(mainResponseFarOrganData.suffix(4)) + "vide" + String(const_toWriteTitle.prefix(7)))
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: mainSinceId.map{mobileCoat(sweet: $0)}, encoding: .utf8)!: tempId, (main_modeFormat.replacingOccurrences(of: "comment", with: "o")): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        BoundRequestManager.ban(reqPath: path, reqParams: param) { succeed, _, _ in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.noExtra(showMsg: (String(data_lengthEhUrl) + String(kSameText)).localized)
                //: self.dismissReportView(succeed: true)
                self.sound(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension SheIonAlertView: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: BringUpReactiveCompatible.className()) as! BringUpReactiveCompatible
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.media(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension SheIonAlertView {
    /// 添加视图
    //: private func setupSubviews() {
    private func myOn() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func ditConstraint() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func thanPerson() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(app_decideValue.suffix(6))).localized,
                           //: "Violence".localized,
                           (String(const_locationData) + data_enableMsg.lowercased()).localized,
                           //: "Harassment".localized,
                           (String(k_peerName.prefix(5)) + String(constTenderOneTitle)).localized,
                           //: "Suicide or Self-injury".localized,
                           (String(kGroupValue) + String(user_coreContent.suffix(4)) + "njury").localized,
                           //: "False Information".localized,
                           (String(kSecureTitle) + String(data_exclusiveFormat)).localized,
                           //: "Spam".localized,
                           (String(user_bulletMenuStr.prefix(4))).localized,
                           //: "Hate Speech".localized,
                           (String(kDenyTitle.suffix(4)) + " Spe" + String(user_brideStr)).localized,
                           //: "Terrorism".localized,
                           (String(showAncientEffOkId.prefix(6)) + dataSelectId.replacingOccurrences(of: "jump", with: "m")).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: user_restName.map{$0^143}, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(data_searchText.prefix(5)) + "ng disgu" + String(show_followTitle.suffix(6)) + "messages").localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: main_norMessage.map{paraRocket(found: $0)}, encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(appRespectPath) + String(noti_netName.prefix(6))).localized,
                           //: "Defraud or using fake info".localized,
                           (String(k_createName.prefix(6)) + "d or u" + String(data_reachStr.suffix(5)) + "fake " + String(mainJungleName)).localized,
                           //: "Others".localized]
                           (String(constVidEyebrowName) + appGravityUrl.replacingOccurrences(of: "chemical", with: "s")).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = MentionTriggerAppManager.share.appConfigMode.videoReport["reason"] {
            if let reason = MentionTriggerAppManager.share.appConfigMode.videoReport[(main_modeFormat.replacingOccurrences(of: "comment", with: "o"))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = ConstraintFactoryThen()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func menuTotaleraction() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
