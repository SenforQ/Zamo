
//: Declare String Begin

/*: "http://" :*/
fileprivate let k_cycleName:String = "http://uniform digital headline"

/*: "https://" :*/
fileprivate let kEasyFleetName:String = "httpow"

/*: "http:// :*/
fileprivate let noti_forwardMessage:[Character] = ["h"]
fileprivate let dataPromptMessage:String = "ttp://off blade"

/*: "serverSelctIndex =  :*/
fileprivate let k_duringUrl:[Character] = ["s","e","r","v","e","r","S","e","l","c","t","I","n","d","e","x"," ","="]
fileprivate let showAddedMsg:[Character] = [" "]

/*: "serverUrlStr =  :*/
fileprivate let kMidData:[Character] = ["s","e","r","v","e","r","U","r","l","S","t","r"," ","="," "]

/*: "app. :*/
fileprivate let appCounterruptId:[Character] = ["a","p","p","."]

/*: .com" :*/
fileprivate let constRemainFormat:[Character] = [".","c","o","m"]

/*: "mf/chatRoom/sendGift" :*/
fileprivate let data_folkUrl:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","s","e","n","d","G"]
fileprivate let app_modelAppealValue:[Character] = ["i","f","t"]

/*: "recharge/applePayNotify" :*/
fileprivate let appTaUrl:String = "rsignchar"
fileprivate let mainBeatMessage:String = "simply hazard calendar creaturepplePa"

/*: "user/appleLogin" :*/
fileprivate let userMicFormat:[Character] = ["u","s","e","r","/","a","p","p"]
fileprivate let kLicenseFormat:[Character] = ["l","e","L","o","g","i","n"]

/*: "IM/getMyBlackList" :*/
fileprivate let data_effId:String = "IM/gettiming rein origin priority"
fileprivate let userCarryName:[Character] = ["L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let constOkName:[Character] = ["m","f","/","b","u","s","i","n","e"]
fileprivate let constCiteUsTitle:String = "SS"
fileprivate let showCellFormat:String = "extent lead country calculation hi/sendMsg"

/*: "mf/business/sendGift" :*/
fileprivate let app_evenValue:[Character] = ["m","f","/","b","u","s","i","n"]
fileprivate let mainAbsStr:String = "esuccessfully"
fileprivate let constCounterruptQuickUrl:String = "very care document/sen"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let main_showTitle:String = "posit collect headmf/cha"
fileprivate let user_formTitle:String = "m/senhelp deadline club accommodate history"
fileprivate let notiPadLabText:[Character] = ["d","M","s","g"]

/*: "GJ21FB" :*/
fileprivate let userLicenseMessage:[Character] = ["G","J","2","1","F","B"]

/*: "完成了检测,请求状态正常" :*/
fileprivate let appHearFormat:String = "完成\u{4e86}检\u{6d4b},"
fileprivate let user_pleasePath:[Character] = ["正","常"]

/*: "接口请求失败" :*/
fileprivate let constCheckId:[Character] = ["接","口","请","\u{6c42}","失","败"]

/*: "完成了检测,需要更换server url" :*/
fileprivate let main_followUrl:String = "\u{5b8c}成了"
fileprivate let main_pendingFormat:String = "检测,需要更"
fileprivate let data_detailValue:String = "fraction render identifyver url"

/*: "HTTPProxy" :*/
fileprivate let const_bringTitle:String = "HTTPProxyman female least pack pick"

/*: "__SCOPED__" :*/
fileprivate let app_conText:[Character] = ["_","_","S","C","O","P","E"]
fileprivate let app_chiefEhData:[Character] = ["D","_","_"]

/*: "tap" :*/
fileprivate let notiTowardConfirmData:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let userJoinName:String = "curveun"

/*: "ipsec" :*/
fileprivate let data_computeCoreTitle:[Character] = ["i","p","s","e","c"]

/*: "ppp" :*/
fileprivate let app_aName:String = "strongp"

/*: "func__updateTalkingCookies =  :*/
fileprivate let kWhetherData:[Character] = ["f","u","n","c","_","_","u","p","d","a","t","e","T","a","l","k","i","n","g","C","o","o","k","i","e","s"," ","="," "]

/*: "https:// :*/
fileprivate let constShiftGenderFormat:String = "overlook heart key importhttps://"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let app_promptName:[UInt8] = [0x20,0x3d,0x20,0x6c,0x72,0x55,0x68,0x74,0x69,0x57,0x73,0x65,0x69,0x6b,0x6f,0x6f,0x43,0x65,0x6d,0x61,0x53,0x65,0x74,0x65,0x6c,0x65,0x64,0x5f,0x5f,0x63,0x6e,0x75,0x66]

/*: "UID" :*/
fileprivate let noti_signatureContent:String = "Uorigin"

/*: "PHPSESSID" :*/
fileprivate let dataOutputTitleDefinitionFormat:String = "descriptionHdescriptionSESSID"

/*: "func__saveAllCookies =  :*/
fileprivate let kDitText:String = "score user playfunc__"
fileprivate let noti_myStr:String = "psychological surelCoo"
fileprivate let noti_haveFormat:String = " strike "

/*: "func__saveAllCookies  error" :*/
fileprivate let showPencilName:String = "accuracyn"
fileprivate let dataNearKey:String = "c__savcondition straight team"
fileprivate let k_styleEntityName:String = "ooktechnology"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ProjectionThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum ExtensiveLangType: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class ProjectionThen: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = ProjectionThen()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        upwardSecond()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(k_cycleName.prefix(7)))) || newValue.hasPrefix((kEasyFleetName.replacingOccurrences(of: "pow", with: "p") + "s://")) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if k_homeContent && !k_coverFormat {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(noti_forwardMessage) + String(dataPromptMessage.prefix(6))) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(noti_forwardMessage) + String(dataPromptMessage.prefix(6))) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    talkAnswer(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func upwardSecond() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = showProduceLastTitle.integer(forKey: mainHundredIdentityTitle)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (String(k_duringUrl) + String(showAddedMsg)) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = awakeUrl()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(k_cycleName.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if k_homeContent && !k_coverFormat {
            //: serverPrefix = "http://"
            serverPrefix = (String(k_cycleName.prefix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (String(kMidData)) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func awakeUrl() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func part(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(appCounterruptId)) + "\(dataCancelMessage)" + (String(constRemainFormat)), (String(appCounterruptId)) + "\(dataCancelMessage)" + (String(constRemainFormat))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(data_folkUrl) + String(app_modelAppealValue)),
         //: "recharge/applePayNotify",
         (appTaUrl.replacingOccurrences(of: "sign", with: "e") + "ge/a" + String(mainBeatMessage.suffix(6)) + "yNotify"),
         //: "user/appleLogin"] + slowPathArr1
         (String(userMicFormat) + String(kLicenseFormat))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(data_effId.prefix(6)) + "MyBlack" + String(userCarryName)),
         //: "mf/business/sendMsg",
         (String(constOkName) + constCiteUsTitle.lowercased() + String(showCellFormat.suffix(8))),
         //: "mf/business/sendGift",
         (String(app_evenValue) + mainAbsStr.replacingOccurrences(of: "successfully", with: "ss") + String(constCounterruptQuickUrl.suffix(4)) + "dGift"),
         //: "mf/chatRoom/sendMsg"]
         (String(main_showTitle.suffix(6)) + "tRoo" + String(user_formTitle.prefix(5)) + String(notiPadLabText))]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = EverlastinglyReactiveCompatible.getSystemLangCode()
        let lang = EverlastinglyReactiveCompatible.feedAnimal()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .windowDimension(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (String(userLicenseMessage)) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension ProjectionThen {
    //: func checkServerUrl() {
    func cornerAttach() {
        //: if MentionTriggerAppManager.share.networkStatus != .Unavailable {
        if MentionTriggerAppManager.share.networkStatus != .Unavailable {
            //: func_requestHost()
            alongRequest()
            //: } else {
        } else {
            //: func_starCodeTime()
            funAnimal()
        }
    }

    //: func func_starCodeTime() {
    func funAnimal() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.alongRequest()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func goBefore() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func alongRequest() {
        //: if MentionTriggerAppManager.share.networkStatus == .Unavailable {
        if MentionTriggerAppManager.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        goBefore()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        SubjectMatterManagerRequest.placeResistanceMatch { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (appHearFormat + "请求状态" + String(user_pleasePath)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (String(constCheckId)))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.funAnimal()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (main_followUrl + main_pendingFormat.capitalized + "\u{6362}ser" + String(data_detailValue.suffix(7))))
                    //: self.func__changeHostUrl()
                    self.purchaseStage()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func purchaseStage() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        goBefore()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        showProduceLastTitle.set(serverSelctIndex, forKey: mainHundredIdentityTitle)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(k_cycleName.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if k_homeContent, !k_coverFormat {
            //: serverPrefix = "http://"
            serverPrefix = (String(k_cycleName.prefix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension ProjectionThen {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func proxy() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(String(const_bringTitle.prefix(9)))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func nextAcross() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(app_conText) + String(app_chiefEhData))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((String(notiTowardConfirmData))) || keyStr.contains((userJoinName.replacingOccurrences(of: "curve", with: "t"))) || keyStr.contains((String(data_computeCoreTitle))) || keyStr.contains((app_aName.replacingOccurrences(of: "strong", with: "pp"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension ProjectionThen {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func talkAnswer(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(kWhetherData)) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = birthdayOwn()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = MentionTriggerAppManager.share.appConfigMode.urlH5Domain
        var urlDomain: String = MentionTriggerAppManager.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(k_cycleName.prefix(7))), with: (kEasyFleetName.replacingOccurrences(of: "pow", with: "p") + "s://"))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((kEasyFleetName.replacingOccurrences(of: "pow", with: "p") + "s://")) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(constShiftGenderFormat.suffix(8))) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            self.cookies(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        colour()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func cookies(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: app_promptName.reversed(), encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func birthdayOwn() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(k_cycleName.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if k_homeContent, !k_coverFormat {
            //: serverPrefix = "https://"
            serverPrefix = (kEasyFleetName.replacingOccurrences(of: "pow", with: "p") + "s://")
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(appCounterruptId)) + "\(dataCancelMessage)" + (String(constRemainFormat)))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func notWhoNote(note _: Notification) {
        //: if MentionTriggerAppManager.share.loginUid.isEmptyString {
        if MentionTriggerAppManager.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            flagNotice()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            sourceListener()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func flagNotice() {
        //: var htmlDomain: String = MentionTriggerAppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = MentionTriggerAppManager.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (noti_signatureContent.replacingOccurrences(of: "origin", with: "ID")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(noti_signatureContent.replacingOccurrences(of: "origin", with: "ID"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (dataOutputTitleDefinitionFormat.replacingOccurrences(of: "description", with: "P")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(dataOutputTitleDefinitionFormat.replacingOccurrences(of: "description", with: "P"))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (noti_signatureContent.replacingOccurrences(of: "origin", with: "ID")) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(noti_signatureContent.replacingOccurrences(of: "origin", with: "ID"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (dataOutputTitleDefinitionFormat.replacingOccurrences(of: "description", with: "P")) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(dataOutputTitleDefinitionFormat.replacingOccurrences(of: "description", with: "P"))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((noti_signatureContent.replacingOccurrences(of: "origin", with: "ID"))), h5Cookie.keys.contains((noti_signatureContent.replacingOccurrences(of: "origin", with: "ID"))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(noti_signatureContent.replacingOccurrences(of: "origin", with: "ID"))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(noti_signatureContent.replacingOccurrences(of: "origin", with: "ID"))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((dataOutputTitleDefinitionFormat.replacingOccurrences(of: "description", with: "P"))), h5Cookie.keys.contains((dataOutputTitleDefinitionFormat.replacingOccurrences(of: "description", with: "P"))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(dataOutputTitleDefinitionFormat.replacingOccurrences(of: "description", with: "P"))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(dataOutputTitleDefinitionFormat.replacingOccurrences(of: "description", with: "P"))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func sourceListener() {
        //: let htmlDomain: String = MentionTriggerAppManager.share.appConfigMode.urlH5Domain
        let htmlDomain: String = MentionTriggerAppManager.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == MentionTriggerAppManager.share.loginUserMode.userID {
            if cookie.name == (noti_signatureContent.replacingOccurrences(of: "origin", with: "ID")), cookie.value == MentionTriggerAppManager.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (dataOutputTitleDefinitionFormat.replacingOccurrences(of: "description", with: "P")) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (noti_signatureContent.replacingOccurrences(of: "origin", with: "ID")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(noti_signatureContent.replacingOccurrences(of: "origin", with: "ID"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (dataOutputTitleDefinitionFormat.replacingOccurrences(of: "description", with: "P")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(dataOutputTitleDefinitionFormat.replacingOccurrences(of: "description", with: "P"))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((noti_signatureContent.replacingOccurrences(of: "origin", with: "ID"))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(noti_signatureContent.replacingOccurrences(of: "origin", with: "ID"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((dataOutputTitleDefinitionFormat.replacingOccurrences(of: "description", with: "P"))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(dataOutputTitleDefinitionFormat.replacingOccurrences(of: "description", with: "P"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func colour() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(kDitText.suffix(6)) + "saveAl" + String(noti_myStr.suffix(4)) + "kies" + noti_haveFormat.replacingOccurrences(of: "strike", with: "=")) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            showProduceLastTitle.set(cookieData, forKey: k_conversationWindowKey)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (showPencilName.replacingOccurrences(of: "accuracy", with: "fu") + String(dataNearKey.prefix(6)) + "eAllC" + k_styleEntityName.replacingOccurrences(of: "technology", with: "ie") + "s  error"))
        }
    }

    //: func func__setAppCookies() {
    func bridleCookies() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = showProduceLastTitle.object(forKey: k_conversationWindowKey)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func bagAnnouncement() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        showProduceLastTitle.removeObject(forKey: k_conversationWindowKey)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
