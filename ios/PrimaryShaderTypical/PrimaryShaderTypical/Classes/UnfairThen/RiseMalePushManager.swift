
//: Declare String Begin

/*: "fee" :*/
fileprivate let main_writerCollectMsg:String = "combineee"

/*: "VIPFee" :*/
fileprivate let userOpValue:[Character] = ["V","I","P","F","e","e"]

/*: "chattingNum" :*/
fileprivate let user_againData:String = "chmerelyti"
fileprivate let showToleranceFormat:[Character] = ["n","g","N","u","m"]

/*: "You're already in her live room" :*/
fileprivate let appUnknownKey:[UInt8] = [0x5e,0x68,0x72,0x20,0x75,0x62,0x27,0x66,0x6b,0x75,0x62,0x66,0x63,0x7e,0x27,0x6e,0x69,0x27,0x6f,0x62,0x75,0x27,0x6b,0x6e,0x71,0x62,0x27,0x75,0x68,0x68,0x6a]

private func ingredientExport(invite num: UInt8) -> UInt8 {
    return num ^ 7
}

/*: "toUid" :*/
fileprivate let data_lengthPath:[UInt8] = [0x64,0x69,0x55,0x6f,0x74]

/*: "momentId" :*/
fileprivate let kPanMemberTeamText:[UInt8] = [0xef,0xed,0xef,0xe7,0xec,0xf6,0xcb,0xe6]

private func mainBoth(three num: UInt8) -> UInt8 {
    return num ^ 130
}

/*: "extra" :*/
fileprivate let constAreaValue:[UInt8] = [0x12,0xf,0x3,0x5,0x16]

private func thatFaculty(audience num: UInt8) -> UInt8 {
    return num ^ 119
}

/*: "uid" :*/
fileprivate let noti_braceTitle:[UInt8] = [0xfe,0xe2,0xef]

private func afterThe(gesture num: UInt8) -> UInt8 {
    return num ^ 139
}

/*: "onlineStatus" :*/
fileprivate let show_fieldKey:[UInt8] = [0x97,0x96,0x94,0x91,0x96,0x9d,0xab,0x8c,0x99,0x8c,0x8d,0x8b]

private func columnUnknown(assist num: UInt8) -> UInt8 {
    return num ^ 248
}

/*: "isNewUser" :*/
fileprivate let notiWaitMessage:[UInt8] = [0x73,0x7d,0x58,0x6f,0x81,0x5f,0x7d,0x6f,0x7c]

fileprivate func revenueJaw(resource num: UInt8) -> UInt8 {
    let value = Int(num) + 246
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "userStatus" :*/
fileprivate let kLifestyleMessage:[UInt8] = [0xcb,0xc9,0xbb,0xc8,0xa9,0xca,0xb7,0xca,0xcb,0xc9]

fileprivate func beanAnyone(effect num: UInt8) -> UInt8 {
    let value = Int(num) + 170
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Account is restricted！" :*/
fileprivate let noti_routeDitPath:[Character] = ["A","c","c","o","u","n","t"," ","i","s"," ","r","e","s","t","r","i","c","t","e","d"]
fileprivate let userQueryData:[Character] = ["！"]

/*: "Failed to enter the chat room. Please try again later" :*/
fileprivate let data_shadeCuteFormat:[UInt8] = [0x72,0x65,0x74,0x61,0x6c,0x20,0x6e,0x69,0x61,0x67,0x61,0x20,0x79,0x72,0x74,0x20,0x65,0x73,0x61,0x65,0x6c,0x50,0x20,0x2e,0x6d,0x6f,0x6f,0x72,0x20,0x74,0x61,0x68,0x63,0x20,0x65,0x68,0x74,0x20,0x72,0x65,0x74,0x6e,0x65,0x20,0x6f,0x74,0x20,0x64,0x65,0x6c,0x69,0x61,0x46]

/*: "url" :*/
fileprivate let data_surgeryUrl:String = "urreader"

/*: "userDetail" :*/
fileprivate let mainFillName:String = "userDetbulb sort"
fileprivate let constHistoryName:String = "airefer"

/*: "&type=6" :*/
fileprivate let k_studyName:String = "tar span&type="
fileprivate let k_portraitKey:String = "moment"

/*: "&type=7" :*/
fileprivate let showGiveKey:String = "ingredient least&type=7"

/*: "amount" :*/
fileprivate let kTermsContainData:[UInt8] = [0x70,0x7c,0x7e,0x84,0x7d,0x83]

fileprivate func wealthyDaily(refuse num: UInt8) -> UInt8 {
    let value = Int(num) + 241
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "https://" :*/
fileprivate let mainPowData:String = "https://cycle bar lab"

/*: "http://" :*/
fileprivate let k_estimatedText:String = "http://replace approximate statute bind manual"

/*: "transparency" :*/
fileprivate let const_heelPath:[Character] = ["t","r","a","n","s","p","a","r","e","n"]
fileprivate let user_mayId:String = "cperform"

/*: "version= :*/
fileprivate let show_plainPath:[Character] = ["v","e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let user_betweenApplicationTitle:String = "&packrespect recognize"
fileprivate let k_portName:[Character] = ["a"]
fileprivate let dataCommunicateStr:String = "remove like map original approvalgeId="

/*: &bundleId= :*/
fileprivate let const_creaturePath:[Character] = ["&","b","u","n","d","l","e","I","d"]
fileprivate let kMissFormat:String = "lack"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  RiseMalePushManager.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/31.
//

//: import UIKit
import UIKit

//: public class TalkingAppPushManager: NSObject {
public class RiseMalePushManager: NSObject {
    //: @objc static public let share = TalkingAppPushManager()
    @objc public static let share = RiseMalePushManager()
    //: private override init() {}
    override private init() {}
}

//: extension TalkingAppPushManager {
extension RiseMalePushManager {
    // MARK: - 随机视频入口

    //: func func__pushToRandomVideoVC(isBeginRand: Bool) {
    func headRand(isBeginRand: Bool) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickInRandomvideo)
        const_failureData.comprehensiveStr(eventID: noti_askKey)
        //: guard TalkingSocketManager.shared.accessible == false else {
        guard DisagreeableWomanSocketManager.shared.accessible == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.outBarMsg(showMsg: main_maxPath)
            //: return
            return
        }
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        GatePermissionTool.fitStop { isOpen in
            //: guard isOpen == true else { return }
            guard isOpen == true else { return }
            //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
            GunfireAlwaysJurisdictionManageressReactiveCompatible.throughByCompletion { succeed, result, _ in
                //: guard succeed else { return }
                guard succeed else { return }
                //: let json = JSON(result ?? [String: Any]())
                let json = JSON(result ?? [String: Any]())
                //: let vc = TalkingRandomVideoViewController()
                let vc = CoreErrorDelegate()
                //: vc.fee = json["fee"].intValue
                vc.fee = json[(main_writerCollectMsg.replacingOccurrences(of: "combine", with: "f"))].intValue
                //: vc.vipFee = json["VIPFee"].intValue
                vc.vipFee = json[(String(userOpValue))].intValue
                //: vc.chattingNum = json["chattingNum"].intValue
                vc.chattingNum = json[(user_againData.replacingOccurrences(of: "merely", with: "at") + String(showToleranceFormat))].intValue
                //: vc.isBeginRand = isBeginRand
                vc.isBeginRand = isBeginRand
                //: vc.isFirstRandow = isBeginRand
                vc.isFirstRandow = isBeginRand
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.transplant()?.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }

    // MARK: - 完善资料

    //: func func__pushToFillUserInfoVC() {
    func fillFore() {
        //: MentionTriggerAppManager.share.userFillInfoMode = UserFillInfoModel.init()
        MentionTriggerAppManager.share.userFillInfoMode = SimulationWouldInfoModel()
        //: let VC = TalkingLoginGenderVC()
        let VC = PublicallyReactiveCompatible()
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = rf()
        //: if currentVC != nil {
        if currentVC != nil {
            //: currentVC?.navigationController?.pushViewController(VC, animated: true)
            currentVC?.navigationController?.pushViewController(VC, animated: true)
        }
    }

    // MARK: - 绑定邮箱

    /// - Parameters:
    ///   - type: 类型
    ///   - isShowBack: 是否显示返回按钮
    //: func func__pushToLockUserEmailVC(isShowBack: Bool = true) {
    func styleBack(isShowBack: Bool = true) {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = rf()
        //: if currentVC != nil {
        if currentVC != nil {
            //: if currentVC!.isKind(of: TalkingLoginBindEmailVC.self) ||
            if currentVC!.isKind(of: AutomaticEmailVc.self) ||
                //: currentVC!.isKind(of: TalkingLoginMainViewController.self) {
                currentVC!.isKind(of: StandThen.self)
            {
                //: return
                return
            }
            //: let vc = TalkingLoginBindEmailVC()
            let vc = AutomaticEmailVc()
            //: vc.isBack = isShowBack
            vc.isBack = isShowBack
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
    }

    // MARK: - 跳转个人资料

    //: func func__pushToUserDetailVC(uid: String?) {
    func nudge(uid: String?) {
        //: guard let currentVC = self.currentViewController() else { return }
        guard let currentVC = self.transplant() else { return }
        //: if let liveVC = currentVC as? TalkingLiveBeautifyViewController, TalkingLiveManager.shared().isLive {
        if let liveVC = currentVC as? SectionLiveOrganBePositionViewDelegate, AlwaysDiskPushListener.aInmate().isLive {
            // 从主播页跳转个人资料时，切小屏模式
            //: liveVC.topView.miniButtonClick()
            liveVC.topView.chief()

            //: } else if let profileVC = currentVC as? TalkingUserDetailViewController {
        } else if let profileVC = currentVC as? TotalerViewController {
            // 同一用户的资料页已经在顶部，只刷新
            //: if profileVC.uid == uid {
            if profileVC.uid == uid {
                //: profileVC.requestAllData()
                profileVC.accountingData()
                //: return
                return
            }
        }

        //: let vc = TalkingUserDetailViewController(tempUid: uid)
        let vc = TotalerViewController(tempUid: uid)
        //: getNavigationController()?.pushViewController(vc, animated: true)
        rootController()?.pushViewController(vc, animated: true)
    }

    // MARK: - 观众侧进入直播间

    /// 观众侧跳转到直播间
    /// - Parameters:
    ///   - uid: 主播id
    ///   - enterType: 入口
    //: func func_audiencePushToLiveRoomVC(uid: String, enterType: LiveEnterType) {
    func aboveSpace(uid: String, enterType: TreeEnterType) {
        //: guard TalkingPermissionTool.isLiveOrPartyActive() == false else { return }
        guard GatePermissionTool.netFor() == false else { return }

        // 同一直播间只能进一次
        //: guard String(TalkingLiveManager.shared().liveRoomModel.streamerInfo.uid) != uid else {
        guard String(AlwaysDiskPushListener.aInmate().liveRoomModel.streamerInfo.uid) != uid else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You're already in her live room".localized)
            self.outBarMsg(showMsg: String(bytes: appUnknownKey.map{ingredientExport(invite: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        // 校验接口，通过后进入直播间
        //: TalkingAudienceManager().req_enterLiveRoom(streamerUid: uid) { succeed, result, errorModel in
        ManageressThen().readyCommon(streamerUid: uid) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: guard let livemodel = TalkingLiveRoomModel.deserialize(from: result as? Dictionary) else { return }
            guard let livemodel = SlowModel.deserialize(from: result as? Dictionary) else { return }
            // 退出之前的直播间
            //: let currentVC = self.func__getCurrentActivityVC()
            let currentVC = self.rf()
            //: if let vcArr = currentVC?.navigationController?.viewControllers {
            if let vcArr = currentVC?.navigationController?.viewControllers {
                //: for index in 0..<(vcArr.count) {
                for index in 0 ..< (vcArr.count) {
                    //: let vc = vcArr[index]
                    let vc = vcArr[index]
                    //: if vc.isKind(of: TalkingLivePullStreamsViewController.self) == true {
                    if vc.isKind(of: DrawViewController.self) == true {
                        //: (vc as! TalkingLivePullStreamsViewController).popCurrentViewController()
                        (vc as! DrawViewController).cigaretteHolder()
                    }
                }
            }

            // 进入新直播间
            //: TalkingLiveManager.shared().liveRoomModel = livemodel
            AlwaysDiskPushListener.aInmate().liveRoomModel = livemodel
            //: let liveVc = TalkingLivePullStreamsViewController()
            let liveVc = DrawViewController()
            //: liveVc.type = enterType
            liveVc.type = enterType
            //: currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            // 埋点
            //: switch enterType {
            switch enterType {
            //: case .match:
            case .match:
                //: uploadRecord.uploadRecordEvent(eventID: ClickMatchLiveWindowNoP, toUid: uid)
                const_failureData.comprehensiveStr(eventID: mainIndexStr, toUid: uid)
            //: case .userDetail:
            case .userDetail:
                //: uploadRecord.uploadRecordEvent(eventID: ClickProfileLiveWindowNoP, toUid: uid)
                const_failureData.comprehensiveStr(eventID: showRankUrl, toUid: uid)
            //: case .unknown: break
            case .unknown: break
            }
        }
    }

    // MARK: - 跳转聊天

    /// 跳转到消息列表页
    /// - Parameter isHalf: 是否半屏展示
    //: func func__pushToChatListVC(isHalfView: Bool = false) {
    func visualList(isHalfView: Bool = false) {
        //: let vc = TalkingChatListViewController(isHalfView: isHalfView)
        let vc = DataConverterViewController(isHalfView: isHalfView)
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = rf()
        //: if isHalfView == false {
        if isHalfView == false {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
            //: } else {
        } else {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
        }
    }

    /// 跳转到消息聊天页
    /// - Parameters:
    ///   - chatID: 对方Id
    ///   - isFrom: 私聊页入口
    /// - Returns: 消息聊天页

    //: func func__pushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, completion: ((_ vc: TalkingPrivateChatController) -> Void)? = nil ) {
    func bubbleAdjust(chatID: String, isFrom: PackFromEnum = .Normal, completion: ((_ vc: AutomaticViewController) -> Void)? = nil) {
        //: guard chatID.count > 0 else { return }
        guard chatID.count > 0 else { return }

        //: if chatID != EverlastinglyReactiveCompatible.getXiaoMiID() {
        if chatID != EverlastinglyReactiveCompatible.layChild() { // 系统消息
            //: ProgressHUD.show()
            LovelyLifetimeProgressHUD.launchSocialShow()
            //: let param = ["toUid": chatID, "momentId": "0", "extra": "1"]
            let param = [String(bytes: data_lengthPath.reversed(), encoding: .utf8)!: chatID, String(bytes: kPanMemberTeamText.map{mainBoth(three: $0)}, encoding: .utf8)!: "0", String(bytes: constAreaValue.map{thatFaculty(audience: $0)}, encoding: .utf8)!: "1"]
            //: TalkingChatRequestTool.req_refreshUserChatInfo(param: param) { [weak self] succeed, result, errorModel in
            GoUpReactiveCompatible.statusGoCompletion(param: param) { [weak self] succeed, result, _ in
                //: ProgressHUD.dismiss()
                LovelyLifetimeProgressHUD.arcCurDismiss()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: guard succeed else { return }
                guard succeed else { return }

                //: let userWrap = PronkCacheModel.func__transformedChatinfo(userDic: result as! Dictionary<String, Any>)
                let userWrap = PronkCacheModel.toAGreaterExtentDecision(userDic: result as! [String: Any])
                //: DBUserInfoManager.cache_updateMsgUserInfoAndPostNotif(with: userWrap)
                BoundPackInfoManager.sameExit(with: userWrap)
                // 更新消息列表用户在线状态
                //: let userInfo = ["uid": chatID,
                let userInfo = [String(bytes: noti_braceTitle.map{afterThe(gesture: $0)}, encoding: .utf8)!: chatID,
                                //: "onlineStatus": userWrap.onlineStatus,
                                String(bytes: show_fieldKey.map{columnUnknown(assist: $0)}, encoding: .utf8)!: userWrap.onlineStatus,
                                //: "isNewUser":userWrap.isNewUser,
                                String(bytes: notiWaitMessage.map{revenueJaw(resource: $0)}, encoding: .utf8)!: userWrap.isNewUser,
                                //: "userStatus": userWrap.userStatus]
                                String(bytes: kLifestyleMessage.map{beanAnyone(effect: $0)}, encoding: .utf8)!: userWrap.userStatus]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: userBackgroundMessage,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
                //: if userWrap.userStatus == 3 {
                if userWrap.userStatus == 3 { /// 被封号
                    //: self.func__showStatusBarErrorMsg(showMsg: "Account is restricted！".localized)
                    self.outBarMsg(showMsg: (String(noti_routeDitPath) + String(userQueryData)).localized)
                    //: return
                    return
                }
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    let chatVC = self.timing(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    //: completion?(chatVC)
                    completion?(chatVC)
                }
            }
            //: }else{
        } else {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: PronkCacheModel())
                let chatVC = self.timing(chatID: chatID, isFrom: isFrom, userWrap: PronkCacheModel())
                //: completion?(chatVC)
                completion?(chatVC)
            }
        }
    }

    //: func func__canPushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: PronkCacheModel ) -> TalkingPrivateChatController {
    func timing(chatID: String, isFrom: PackFromEnum = .Normal, userWrap: PronkCacheModel) -> AutomaticViewController {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = rf()
        //: if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
        if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index] .isKind(of: TalkingPrivateChatController.self) == true {
                if vcArr[index].isKind(of: AutomaticViewController.self) == true {
                    //: let vc = vcArr[index] as! TalkingPrivateChatController
                    let vc = vcArr[index] as! AutomaticViewController
                    //: if vc.targetId == chatID {
                    if vc.targetId == chatID {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return vc
                        return vc
                    }
                }
            }
        }

        //: let vc = TalkingPrivateChatController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        let vc = AutomaticViewController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        //: if isFrom == .LiveRoom {
        if isFrom == .LiveRoom {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
            //: } else {
        } else {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
        //: return vc
        return vc
    }

    // MARK: - 跳转群聊

    //: func func__pushToGroupChat(groupId: String) {
    func ticking(groupId: String) {
        //: guard groupId.count > 0 else { return }
        guard groupId.count > 0 else { return }
        //: ProgressHUD.show()
        LovelyLifetimeProgressHUD.launchSocialShow()
        //: TalkingChatRequestTool.req_checkChatRoom(roomId: groupId) { succeed, result, errorModel in
        GoUpReactiveCompatible.nameWith(roomId: groupId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            LovelyLifetimeProgressHUD.arcCurDismiss()
            //: guard succeed == true else {
            guard succeed == true else {
                //: if errorModel?.errorCode == 106 {
                if errorModel?.errorCode == 106 {
                    //: } else {
                } else {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                    self.outBarMsg(showMsg: errorModel?.errorMsg ?? "")
                    //: if errorModel?.errorCode == 103 {
                    if errorModel?.errorCode == 103 { // 不在家族里
                    }
                }

                //: return
                return
            }

            //: guard let data = result as? [String: Any] else {
            guard let data = result as? [String: Any] else {
                //: return
                return
            }

            //: if EverlastinglyReactiveCompatible.isGroupChat(groupId) {
            if EverlastinglyReactiveCompatible.viewFeature(groupId) {
                //: } else {
            } else { // 公共聊天室
                //: V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                    //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                    guard self.enableShareStatus(roomId: groupId) == false else { return }
                    //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                    let vc = StillChatController(groupType: GroupType_Meeting, infoData: data)
                    //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                    self.rf()?.navigationController?.pushViewController(vc, animated: true)

                    //: } fail: { code, desc in
                } fail: { code, _ in
                    //: if code == 10013 {
                    if code == 10013 { // 程序异常退出，未退出聊天室
                        //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                        guard self.enableShareStatus(roomId: groupId) == false else { return }
                        //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                        let vc = StillChatController(groupType: GroupType_Meeting, infoData: data)
                        //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        self.rf()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Failed to enter the chat room. Please try again later".localized)
                        self.outBarMsg(showMsg: String(bytes: data_shadeCuteFormat.reversed(), encoding: .utf8)!.localized)
                    }
                }
            }
        }
    }

    /// 检测堆栈中是否有群聊控制器，有则跳转到群聊页
    /// - Parameter roomId: 房间Id
    /// - Returns: 检测结果（true: 有; false: 无）
    //: private func func__checkIsGroupChatVC(roomId: String) -> Bool {
    private func enableShareStatus(roomId: String) -> Bool {
        //: let currentVC = self.func__getCurrentActivityVC()
        let currentVC = self.rf()
        //: if let vcArr = currentVC?.navigationController?.viewControllers {
        if let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index].isKind(of: TalkingGroupChatController.self) == true {
                if vcArr[index].isKind(of: StillChatController.self) == true {
                    //: let vc = vcArr[index] as! TalkingGroupChatController
                    let vc = vcArr[index] as! StillChatController
                    //: if vc.infoModel.roomId == roomId {
                    if vc.infoModel.roomId == roomId {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return true
                        return true
                    }
                }
            }
        }

        //: return false
        return false
    }

    // MARK: - 真人认证

    //: func func__pushUserVerifyController(toast: String?) {
    func turnDownRowToast(toast _: String?) {
        //: let tpAuth = TPUserAuth(rawValue: MentionTriggerAppManager.share.loginUserMode.isTPAuth)
        let tpAuth = StandardPinColumnConvertible(rawValue: MentionTriggerAppManager.share.loginUserMode.isTPAuth)
        //: switch(tpAuth) {
        switch tpAuth {
        //: case .unknown, .isRefused:
        case .unknown, .isRefused:
            //: let vc = TalkingStatementEquityVC()
            let vc = MisguideViewController()
//            vc.toastStr = toast ?? ""
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = rf()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: case .isOnGoing:
        case .isOnGoing:
            //: let vc = TalkingEditProfilesVC()
            let vc = SectionViewDelegate()
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = rf()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: default:
        default:
            //: break
            break
        }
    }

    // MARK: - 任意用户的动态列表

    //: func func__pushUserPostListWithUid(uid: String) {
    func factorPress(uid _: String) {}

    // MARK: - 反馈feebook

    //: func func__pushFeedbackVC() {
    func wonderDestroy() {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = rf()
        //: let vc = TalkingFeedbackVC.init()
        let vc = RidFieldDelegate()
        //: currentVC?.navigationController?.pushViewController(vc, animated: true)
        currentVC?.navigationController?.pushViewController(vc, animated: true)
    }

    // MARK: - 消息跳转

    //: func func__actionPushForMessage(jumpModel: TalkingMessageJumpModel) {
    func counterest(jumpModel: SweatshopMeasurable) {
        //: if jumpModel.jumpKey == "url" {
        if jumpModel.jumpKey == (data_surgeryUrl.replacingOccurrences(of: "reader", with: "l")) {
            //: func__pushToWebVC(urlStr: jumpModel.url)
            errConfig(urlStr: jumpModel.url)
            //: } else if jumpModel.jumpKey == "userDetail"{
        } else if jumpModel.jumpKey == (String(mainFillName.prefix(7)) + constHistoryName.replacingOccurrences(of: "refer", with: "l")) {}
    }

    // MARK: - 会员订阅网页

    //: @objc public func func__pushToSubscribePageWebVC() {
    @objc public func behindHairFill() {
        //: TalkingAppPushManager.share.func__pushToWebVC(webViewType: .SubscribePage)
        RiseMalePushManager.share.monthType(webViewType: .SubscribePage)
    }

    // MARK: - 设置密码页面

    /// 跳转设置密码页面
    /// - Parameters:
    ///   - type: 手机号/邮箱
    ///   - content: 展示内容
    ///   - isNewPwd: 是否新设置密码（修改需要验证码）
    //: func func__pushToSetNewPasswordVC(type: LoginType, content: String, isNewPwd: Bool) {
    func pinpoint(type: LoginExpressionConvertible, content: String, isNewPwd: Bool) {
        //: let vc = TalkingSetNewPasswordVC()
        let vc = RugRecognizerDelegate()
        //: vc.type = type
        vc.type = type
        //: vc.content = content
        vc.content = content
        //: vc.isNewPwd = isNewPwd
        vc.isNewPwd = isNewPwd
        //: func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
        rf()?.navigationController?.pushViewController(vc, animated: true)
    }
}

// MARK: - 跳转H5

//: extension TalkingAppPushManager {
extension RiseMalePushManager {
    // 跳转全屏H5入口
    //: func func__pushToWebVC(webViewType: WebUrlType) {
    func monthType(webViewType: SpeakForeverCustomReflectable) {
        //: func__pushToWebVC(webViewType: webViewType, webConfig: TalkingWebConfig())
        spectrograph(webViewType: webViewType, webConfig: RidConfig())
    }

    /// 系统主动拨打弹窗，余额不足跳转充值/订阅页
    //: func func__jumpVideoCallToWebRecharge(popupCallIndex: Int, appendParams: String = "&type=6") {
    func easy(popupCallIndex: Int, appendParams: String = (String(k_studyName.suffix(6)) + k_portraitKey.replacingOccurrences(of: "moment", with: "6"))) {
        //: let view = TalkingInsufficientBalanceView.init()
        let view = HemisphereBalanceView()
        //: view.show()
        view.oppositeShow()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if popupCallIndex == 1 {
            if popupCallIndex == 1 { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                self.dealMini(webViewType: .RechargeHalfPage)
                //: } else if popupCallIndex == 2 {
            } else if popupCallIndex == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                taListener(appendParams: appendParams)
            }
        }
    }

    /// 余额不足跳转充值/订阅页
    /// - Parameters:
    ///   - clickEvent: 埋点事件
    ///   - sufficient: 余额是否充足（默认true）
    //: func func__jumpToWebRecharge(clickEvent: String = "", sufficient: Bool = true, appendParams: String = "&type=7") {
    func primed(clickEvent: String = "", sufficient: Bool = true, appendParams: String = (String(showGiveKey.suffix(7)))) {
        //: let payWinType = MentionTriggerAppManager.share.appUserConfigMode.payWinType
        let payWinType = MentionTriggerAppManager.share.appUserConfigMode.payWinType
        // 样式：半屏充值页 || (已订阅 && 余额不足)

        //: let view = TalkingInsufficientBalanceView.init()
        let view = HemisphereBalanceView()
        //: view.show()
        view.oppositeShow()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if payWinType == 1 || (MentionTriggerAppManager.share.loginUserMode.loungePlus && sufficient == false) {
            if payWinType == 1 || (MentionTriggerAppManager.share.loginUserMode.loungePlus && sufficient == false) { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                self.dealMini(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                //: } else if payWinType == 2 {
            } else if payWinType == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                taListener(appendParams: appendParams)
            }
        }
    }

    /// 跳转会员订阅弹窗
    /// - Parameter appendParams: url拼接参数（假视频流程拼接："&type=6；默认流程："&type=7"）
    //: func func__pushToSubscribeAlert(appendParams: String = "&type=7") {
    func taListener(appendParams: String = (String(showGiveKey.suffix(7)))) {
        //: var urlStr = TalkingWebManager.getFullUrl(urlType: .SubscribeAlert)
        var urlStr = ProvideThen.north(urlType: .SubscribeAlert)
        //: urlStr.append(appendParams)
        urlStr.append(appendParams)
        //: TalkingAppPushManager.share.func__pushToWebVC(urlStr: urlStr)
        RiseMalePushManager.share.errConfig(urlStr: urlStr)
        // 曝光用户数埋点
        //: uploadRecord.uploadRecordEvent(eventID: subscribtionRecordButton, parameterStr: ["amount": 0])
        const_failureData.engagement(eventID: userListLastId, parameterStr: [String(bytes: kTermsContainData.map{wealthyDaily(refuse: $0)}, encoding: .utf8)!: 0])
    }

    /// 跳转半屏H5入口
    /// - Parameters:
    ///   - webViewType: 网页URL
    ///   - clickEvent: 埋点事件
    //: func func__pushToHalfWebVC(webViewType: WebUrlType, clickEvent: String = "") {
    func dealMini(webViewType: SpeakForeverCustomReflectable, clickEvent: String = "") {
        //: var config = TalkingWebConfig.init()
        var config = RidConfig()
        //: config.showLoadingView = false
        config.showLoadingView = false
        //: config.isHalfView = true
        config.isHalfView = true
        //: func__pushToWebVC(webViewType: webViewType, webConfig: config)
        spectrograph(webViewType: webViewType, webConfig: config)
        // 充值埋点
        //: if webViewType == .RechargeHalfPage {
        if webViewType == .RechargeHalfPage {
            //: RECHARGE_CLICK_EVENT = clickEvent
            main_titleGameData = clickEvent
            //: if clickEvent.count > 0 {
            if clickEvent.count > 0 {
                //: uploadRecord.uploadRecordEvent(eventID: clickEvent, parameterStr: ["amount": 0])
                const_failureData.engagement(eventID: clickEvent, parameterStr: [String(bytes: kTermsContainData.map{wealthyDaily(refuse: $0)}, encoding: .utf8)!: 0])
            }
        }
    }

    /// 跳转到充值优惠弹窗
    //: func func_pushToDiscountRechargeView() {
    func discountByTranslation() {
        //: guard !MentionTriggerAppManager.share.appUserConfigMode.discountUrl.isEmpty else { return }
        guard !MentionTriggerAppManager.share.appUserConfigMode.discountUrl.isEmpty else { return }
        //: let httpsUrl = MentionTriggerAppManager.share.appConfigMode.urlH5Domain + MentionTriggerAppManager.share.appUserConfigMode.discountUrl
        let httpsUrl = MentionTriggerAppManager.share.appConfigMode.urlH5Domain + MentionTriggerAppManager.share.appUserConfigMode.discountUrl
        //: let httpUrl = httpsUrl.replacingOccurrences(of: "https://", with: "http://")
        let httpUrl = httpsUrl.replacingOccurrences(of: (String(mainPowData.prefix(8))), with: (String(k_estimatedText.prefix(7))))
        //: TalkingAppPushManager.share.func__pushToWebVC(urlStr: httpUrl)
        RiseMalePushManager.share.errConfig(urlStr: httpUrl)
    }

    //: func func__pushToWebVC(webViewType: WebUrlType, webConfig: TalkingWebConfig) {
    func spectrograph(webViewType: SpeakForeverCustomReflectable, webConfig: RidConfig) {
        //: let urlStr = TalkingWebManager.getFullUrl(urlType: webViewType)
        let urlStr = ProvideThen.north(urlType: webViewType)
        //: func__pushToWebView(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
        airCurrentDoing(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
    }

    //: func func__pushToWebVC(urlStr: String?, webConfig: TalkingWebConfig? = nil) {
    func errConfig(urlStr: String?, webConfig: RidConfig? = nil) {
        //: let config = (webConfig == nil) ? TalkingWebConfig():webConfig
        let config = (webConfig == nil) ? RidConfig() : webConfig
        //: func__pushToWebView(urlStr: urlStr, webViewType: nil, webConfig: config!)
        airCurrentDoing(urlStr: urlStr, webViewType: nil, webConfig: config!)
    }

    //: func func__pushToWebView(urlStr: String?, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    func airCurrentDoing(urlStr: String?, webViewType: SpeakForeverCustomReflectable?, webConfig: RidConfig) {
        //: guard urlStr != nil && urlStr != "" else { return }
        guard urlStr != nil, urlStr != "" else { return }
        //: var config = webConfig
        var config = webConfig

        // 有值且大于0，半屏按照比例展示
        //: if config.widthHeight ?? 0 > 0 {
        if config.widthHeight ?? 0 > 0 {
            //: config.isHalfView = true
            config.isHalfView = true
        }
        // 解析url中带的参数
        //: let urlParams = JSON(urlStr!.urlParameters ?? [:])
        let urlParams = JSON(urlStr!.urlParameters ?? [:])
        //: if urlParams["transparency"].stringValue == "1" { // 透明背景
        if urlParams[(String(const_heelPath) + user_mayId.replacingOccurrences(of: "perform", with: "y"))].stringValue == "1" { // 透明背景
            //: config.clearBgColor = true
            config.clearBgColor = true
        }

        // url拼接参数
        //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "`%^{}\"[]|\\<>").inverted
        let allowCharSet = CharacterSet(charactersIn: "`%^{}" + "\"" + "[]|\\<>").inverted
        //: var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        //: let otherParams = "version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)"
        let otherParams = (String(show_plainPath)) + "\(userDoingId)" + (String(user_betweenApplicationTitle.prefix(5)) + String(k_portName) + String(dataCommunicateStr.suffix(5))) + "\(user_underFormat)" + (String(const_creaturePath) + kMissFormat.replacingOccurrences(of: "lack", with: "=")) + "\(mainAgeData)"
        //: if urlStr.contains("?") {
        if urlStr.contains("?") {
            //: urlStr = "\(urlStr)&\(otherParams)"
            urlStr = "\(urlStr)&\(otherParams)"
            //: } else {
        } else {
            //: urlStr = "\(urlStr)?\(otherParams)"
            urlStr = "\(urlStr)?\(otherParams)"
        }

        //: let vc = TalkingWebViewController.init(urlString: urlStr, webViewType: webViewType, webConfig: config)
        let vc = ViewAssistantMessageHandler(urlString: urlStr, webViewType: webViewType, webConfig: config)

        // 展示网页
        //: if let currentVC = func__getCurrentActivityVC() {
        if let currentVC = rf() {
            // 如果当前视图是模态上来的，跳转到下级时只能使用模态效果跳转
            //: var currentVCIsPresent = false
            var currentVCIsPresent = false
            //: if currentVC.isKind(of: TalkingWebViewController.self) &&
            if currentVC.isKind(of: ViewAssistantMessageHandler.self) &&
                //: (currentVC as! TalkingWebViewController).isModal == true {
                (currentVC as! ViewAssistantMessageHandler).isModal == true
            {
                //: currentVCIsPresent = true
                currentVCIsPresent = true
            }

            //: if config.isHalfView {
            if config.isHalfView { // 半屏使用present
                //: if config.widthHeight == nil {
                if config.widthHeight == nil {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight*2/3)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: show_objectValue, height: constBlockIdentityContent * 2 / 3)
                    //: vc.tranConfig.configCorner(corners: [.topLeft, .topRight], radius: 18)
                    vc.tranConfig.sharedRadius(corners: [.topLeft, .topRight], radius: 18)
                    //: } else {
                } else {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenWidth/config.widthHeight!)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: show_objectValue, height: show_objectValue / config.widthHeight!)
                }
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else if config.clearBgColor || currentVCIsPresent {
            } else if config.clearBgColor || currentVCIsPresent { // 透明背景只能用present
                //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
                vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: show_objectValue, height: constBlockIdentityContent)
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else {
            } else {
                //: vc.isModal = false
                vc.isModal = false
                //: currentVC.navigationController?.pushViewController(vc, animated: true)
                currentVC.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }
}

// MARK: - 获取当前视图、导航

//: extension TalkingAppPushManager {
public extension RiseMalePushManager {
    //: @objc public func func__getCurrentActivityVC() -> UIViewController? {
    @objc func rf() -> UIViewController? {
        //: return currentViewController()
        return transplant()
    }
}

//: struct TalkingMessageJumpModel: HandyJSON {
struct SweatshopMeasurable: HandyJSON {
    //: var jumpKey: String?
    var jumpKey: String? // 评论的id
    //: var jumpUid: String?
    var jumpUid: String?
    //: var url: String?
    var url: String?
}
