
//: Declare String Begin

/*: ; build: :*/
fileprivate let showSmartConstraintValue:[Character] = [";"," "]
fileprivate let showBlueValue:String = "build:language discount province gravity administrative"

/*: ; iOS  :*/
fileprivate let k_judgeMsg:String = "zz purchase income adjustment decide; iOS "

/*: "audio" :*/
fileprivate let mainUpperUrl:String = "auconveyio"

/*: .wav" :*/
fileprivate let dataItMobileContent:String = "invisible go.wav"

/*: "audio/wav" :*/
fileprivate let dataFragmentTitle:String = "thanks window keepaudio/"
fileprivate let constLocalName:[Character] = ["w","a","v"]

/*: .jpg" :*/
fileprivate let notiMentallyMsg:[Character] = [".","j","p","g"]

/*: "image/jpeg" :*/
fileprivate let app_supportMessage:String = "go contrast gravityimage/"
fileprivate let mainApplicationId:String = "fleetpeg"

/*: "://app." :*/
fileprivate let k_modestId:[Character] = [":","/","/","a","p","p","."]

/*: "data" :*/
fileprivate let appHandleId:String = "dacreaturea"

/*: ":null" :*/
fileprivate let kFineTitle:String = ":nullvolume nothing"

/*: "Request failed, Try again later" :*/
fileprivate let mainLabListenerTitle:[UInt8] = [0x72,0x65,0x74,0x61,0x6c,0x20,0x6e,0x69,0x61,0x67,0x61,0x20,0x79,0x72,0x54,0x20,0x2c,0x64,0x65,0x6c,0x69,0x61,0x66,0x20,0x74,0x73,0x65,0x75,0x71,0x65,0x52]

/*: "json error" :*/
fileprivate let user_missUrl:[Character] = ["j","s","o","n"," ","e","r","r","o","r"]

/*: "platform=iphone&version= :*/
fileprivate let userComputerValue:String = "PLATFO"
fileprivate let notiWaitId:String = "hone&vextra guidance conduct cross republic"
fileprivate let data_brideBrushUrl:[Character] = ["e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let kMaximumId:[Character] = ["&","p","a","c","k"]
fileprivate let constGolfText:String = "ageId=motion later writer contend growth"

/*: &bundleId= :*/
fileprivate let main_hourValue:[Character] = ["&","b","u"]
fileprivate let const_workerKey:String = "minute own appear sensendleId="

/*: &lang= :*/
fileprivate let notiBehindValue:[Character] = ["&","l","a","n","g"]
fileprivate let app_characterizeValue:String = "="

/*: "/route/slowindex" :*/
fileprivate let data_literalMessage:String = "/"
fileprivate let user_manageUrl:String = "route/extent key tab delivery"

/*: "/route/index" :*/
fileprivate let appBelowData:String = "combine snap nut may/rout"
fileprivate let data_soundValue:String = "advert soon draft compound woode/index"

/*: "?path= :*/
fileprivate let data_adCentralUrl:[Character] = ["?","p","a","t","h","="]

/*: "&GJNonceStr= :*/
fileprivate let user_wouldProduceSlideMessage:[Character] = ["&","G","J","N","o","n","c","e","S","t"]
fileprivate let const_heartStr:String = "document putr="

/*: "&params= :*/
fileprivate let const_trustValue:[Character] = ["&","p","a"]
fileprivate let noti_arrowText:String = "issue clearly whichrams="

/*: "token" :*/
fileprivate let show_tabMoveFormat:[Character] = ["t","o","k","e","n"]

/*: "请求成功 :*/
fileprivate let noti_exhibitContent:String = "\u{8bf7}求成功"

/*: "AAAAAAAA" :*/
fileprivate let mainSendContent:[Character] = ["A"]
fileprivate let const_morePath:[Character] = ["A","A","A","A","A","A","A"]

/*: "下载完成 -  :*/
fileprivate let data_profileId:String = "inmate"
fileprivate let kManualEffectData:String = "\u{8f7d}完成 - "

/*: "/Documents/" :*/
fileprivate let user_gestureUrl:[Character] = ["/","D","o","c","u","m","e","n","t","s","/"]

/*: ".mp4" :*/
fileprivate let dataAliveId:[Character] = [".","m","p","4"]

/*: "移动地址: :*/
fileprivate let mainUnitPostContent:[Character] = ["移","动","地","址",":"]

/*: "后台任务下载回来" :*/
fileprivate let main_rankCoreKey:String = "后\u{53f0}\u{4efb}\u{52a1}下\u{8f7d}回"
fileprivate let k_fadeDisappearData:[Character] = ["来"]

/*: "下载成功 : :*/
fileprivate let mainBarName:String = "\u{4e0b}载\u{6210}功 :"

/*: "Net Error" :*/
fileprivate let show_benefitPath:String = "post paperNet Error"

/*: "下载失败 : :*/
fileprivate let data_visitorName:String = "\u{4e0b}载失败 :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TipToolTaskDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let noti_tabMsg = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let noti_qualityPlayMsg = "\(appAttributeStr)/\(show_foundData) (\(mainAgeData)" + (String(showSmartConstraintValue) + String(showBlueValue.prefix(6))) + "\(const_lineContent)" + (String(k_judgeMsg.suffix(6))) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: RiseErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let userIdentityContent = TipToolTaskDelegate()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class TipToolTaskDelegate: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func downMid(model: ConfinementRequestModel, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.fleck() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.setAboutWithCompletion(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.parentCounteraction(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func setAboutWithCompletion(model: ConfinementRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.fitShrink(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.fleet(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    subjectMatterCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, RiseErrorResponse(errorCode: GrayAdditiveArithmetic.NetError.rawValue, errorMsg: showLicenseOutputName))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        outBarMsg(showMsg: showLicenseOutputName)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (mainUpperUrl.replacingOccurrences(of: "convey", with: "d")) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.changeoverStamp())+\(user_underFormat)" + (String(dataItMobileContent.suffix(4))), mimeType: (String(dataFragmentTitle.suffix(6)) + String(constLocalName)))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(notiMentallyMsg)), mimeType: (String(app_supportMessage.suffix(6)) + mainApplicationId.replacingOccurrences(of: "fleet", with: "j")))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    subjectMatterCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, RiseErrorResponse(errorCode: GrayAdditiveArithmetic.NetError.rawValue, errorMsg: showLicenseOutputName))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        outBarMsg(showMsg: showLicenseOutputName)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func parentCounteraction(model: ConfinementRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.fitShrink(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.fleet(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: k_coverFormat ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = noti_tabMsg }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    subjectMatterCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, RiseErrorResponse(errorCode: GrayAdditiveArithmetic.NetError.rawValue, errorMsg: showLicenseOutputName))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        outBarMsg(showMsg: showLicenseOutputName)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = noti_tabMsg }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    subjectMatterCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, RiseErrorResponse(errorCode: GrayAdditiveArithmetic.NetError.rawValue, errorMsg: showLicenseOutputName))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        outBarMsg(showMsg: showLicenseOutputName)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func subjectMatterCompletion(model: ConfinementRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            ProjectionThen.share.talkAnswer(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if k_coverFormat, model.requestServer.contains((String(k_modestId))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.sampleAnima(key: ProjectionThen.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (appHandleId.replacingOccurrences(of: "creature", with: "t")) + "\"" + (String(kFineTitle.prefix(5))), with: "" + "\"" + (appHandleId.replacingOccurrences(of: "creature", with: "t")) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !k_coverFormat {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(kFineTitle.prefix(5))), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<TopicBaseResponse>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == GrayAdditiveArithmetic.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, RiseErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    outBarMsg(showMsg: responseModel.msg ?? String(bytes: mainLabListenerTitle.reversed(), encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case GrayAdditiveArithmetic.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: const_captureId, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, RiseErrorResponse(errorCode: GrayAdditiveArithmetic.NetError.rawValue, errorMsg: (String(user_missUrl)).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func fitShrink(model: ConfinementRequestModel) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (userComputerValue.lowercased() + "rm=ip" + String(notiWaitId.prefix(6)) + String(data_brideBrushUrl)) + "\(userDoingId)" + (String(kMaximumId) + String(constGolfText.prefix(6))) + "\(user_underFormat)" + (String(main_hourValue) + String(const_workerKey.suffix(7))) + "\(mainAgeData)" + (String(notiBehindValue) + app_characterizeValue.capitalized) + "\(ProjectionThen.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if k_coverFormat, serverUrl.contains((String(k_modestId))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if ProjectionThen.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((data_literalMessage.capitalized + String(user_manageUrl.prefix(6)) + "slowindex"))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(appBelowData.suffix(5)) + String(data_soundValue.suffix(7)))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(data_adCentralUrl)) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(user_wouldProduceSlideMessage) + String(const_heartStr.suffix(2))) + "\(ProjectionThen.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.contestant(key: ProjectionThen.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(const_trustValue) + String(noti_arrowText.suffix(5))) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func fleet(model: ConfinementRequestModel) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(noti_qualityPlayMsg)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (String(show_tabMoveFormat)), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func limitModel(model: ConfinementRequestModel) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.fitShrink(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (noti_exhibitContent) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (String(mainSendContent) + String(const_morePath))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (data_profileId.replacingOccurrences(of: "inmate", with: "下") + kManualEffectData) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(user_gestureUrl)) + NSDate.changeoverStamp() + (String(dataAliveId))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (String(mainUnitPostContent)) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (main_rankCoreKey + String(k_fadeDisappearData)))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = PullTogetherDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func centralCityCompletion(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: appTurnMessage.birthOrShape(), with: dataCancelMessage)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (mainBarName) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, RiseErrorResponse(errorCode: GrayAdditiveArithmetic.Normal.rawValue, errorMsg: (String(show_benefitPath.suffix(9))).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, RiseErrorResponse(errorCode: GrayAdditiveArithmetic.NetError.rawValue, errorMsg: (String(show_benefitPath.suffix(9))).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (data_visitorName) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func enableArray(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: appTurnMessage.birthOrShape(), with: dataCancelMessage)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (mainBarName) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, RiseErrorResponse(errorCode: GrayAdditiveArithmetic.Normal.rawValue, errorMsg: (String(show_benefitPath.suffix(9))).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, RiseErrorResponse(errorCode: GrayAdditiveArithmetic.NetError.rawValue, errorMsg: (String(show_benefitPath.suffix(9))).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (data_visitorName) + "\(errMsg)")
            }
        }
    }
}
