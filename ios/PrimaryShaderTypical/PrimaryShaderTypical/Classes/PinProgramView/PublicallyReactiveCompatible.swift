
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kHouseMsg:[UInt8] = [0x2c,0x31,0x2c,0x37,0xeb,0x26,0x32,0x27,0x28,0x35,0xfd,0xec,0xe3,0x2b,0x24,0x36,0xe3,0x31,0x32,0x37,0xe3,0x25,0x28,0x28,0x31,0xe3,0x2c,0x30,0x33,0x2f,0x28,0x30,0x28,0x31,0x37,0x28,0x27]

fileprivate func militaryQuarters(absolute num: UInt8) -> UInt8 {
    let value = Int(num) + 61
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Personal information" :*/
fileprivate let userInsideUrl:String = "destination asPerson"
fileprivate let show_temporaryFormat:String = "formaaccept"
fileprivate let constProgramStr:String = "otelevision"

/*: "TopicKeyRepresentable" :*/
fileprivate let userUpdatelyName:[Character] = ["G","e","n","d","e"]
fileprivate let k_browStr:String = "actual"

/*: "Once gender is set, it Cannot be changed." :*/
fileprivate let noti_partnerPath:[UInt8] = [0xf2,0xd3,0xde,0xd8,0x9d,0xda,0xd8,0xd3,0xd9,0xd8,0xcf,0x9d,0xd4,0xce,0x9d,0xce,0xd8,0xc9,0x91,0x9d,0xd4,0xc9,0x9d,0xfe,0xdc,0xd3,0xd3,0xd2,0xc9,0x9d,0xdf,0xd8,0x9d,0xde,0xd5,0xdc,0xd3,0xda,0xd8,0xd9,0x93]

private func profitCome(story num: UInt8) -> UInt8 {
    return num ^ 189
}

/*: "#2ABBFF" :*/
fileprivate let data_animalDevicePath:[Character] = ["#"]
fileprivate let data_medalText:[Character] = ["2","A","B","B","F","F"]

/*: "btn_male_nv_nor" :*/
fileprivate let data_stockHideName:String = "btn_normal operate crop shrink care"
fileprivate let user_extentData:String = "_norcharacterize pending contrast worldwide"

/*: "btn_male_nv_sel" :*/
fileprivate let const_strokeMsg:[Character] = ["b","t","n","_","m","a","l","e","_","n","v","_"]
fileprivate let constGoingBeApplicationContent:[Character] = ["s","e","l"]

/*: "Male" :*/
fileprivate let appPageMessage:String = "Malepack score"

/*: "#FF5372" :*/
fileprivate let kEditMainId:String = "#FF5372calendar react content signal golf"

/*: "btn_female_nv_nor" :*/
fileprivate let user_inviteMessage:String = "btn_feshould global zz bubble"
fileprivate let show_voteUrl:String = "oturn"

/*: "btn_female_nv_sel" :*/
fileprivate let showContactFormat:[Character] = ["b","t","n","_","f","e","m","a","l"]
fileprivate let show_restrictionMsg:String = "e_nv_selother bit dialogue"

/*: "Female" :*/
fileprivate let app_incomeDistanceMessage:[Character] = ["F"]
fileprivate let const_attributeTitle:String = "emapower"

/*: "Next" :*/
fileprivate let kEntryData:String = "Nextdescribe fair pending security elect"

/*: "sex" :*/
fileprivate let k_pageEqualName:[UInt8] = [0xd0,0xc6,0xdb]

private func averageMargin(value num: UInt8) -> UInt8 {
    return num ^ 163
}

/*: "RegisterSuccess" :*/
fileprivate let app_lightGesturePath:[Character] = ["R","e","g","i","s","t","e","r"]
fileprivate let userSpeedName:[Character] = ["S","u","c","c","e","s","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PublicallyReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/30.
//

//: import UIKit
import UIKit

//: class TalkingLoginGenderVC: TalkingBaseViewController {
class PublicallyReactiveCompatible: EverlastinglyThen {
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kHouseMsg.map{militaryQuarters(absolute: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: popGesture(isOpen: false)
        extreme(isOpen: false)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Personal information".localized
        self.title = (String(userInsideUrl.suffix(6)) + "al in" + show_temporaryFormat.replacingOccurrences(of: "accept", with: "ti") + constProgramStr.replacingOccurrences(of: "television", with: "n")).localized
        //: MentionTriggerAppManager.share.userFillInfoMode.sex = "1"
        MentionTriggerAppManager.share.userFillInfoMode.sex = "1"
        //: self.setupSubviews()
        self.hair()
        //: self.setupSubViewsConstraint()
        self.awakeConstraint()
        //: self.bindInteraction()
        self.maxQuantityeractionPer()
        //: func__checkNextBtnState()
        wall()
    }

    //: deinit {
    deinit {}

    //: private lazy var logoTitleLabel: UILabel = {
    private lazy var logoTitleLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 17)
        label.font = .landEnableSize(type: .Semibold, fontSize: 17)
        //: label.textColor = .appTitleColor()
        label.textColor = .roundChromaticColor()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.text = "Gender".localized
        label.text = (String(userUpdatelyName) + k_browStr.replacingOccurrences(of: "actual", with: "r")).localized
        //: return label
        return label
        //: }()
    }()

    //: private lazy var genderTipLabel: UILabel = {
    private lazy var genderTipLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 15)
        label.font = .landEnableSize(type: .Regular, fontSize: 15)
        //: label.textColor = .appValueColor()
        label.textColor = .todaySkinColor()
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: label.text = "Once gender is set, it Cannot be changed.".localized
        label.text = String(bytes: noti_partnerPath.map{profitCome(story: $0)}, encoding: .utf8)!.localized
        //: return label
        return label
        //: }()
    }()

    //: private lazy var maleButton: TalkingButton = {
    private lazy var maleButton: EarthThen = {
        //: let btn = TalkingButton(type: .custom)
        let btn = EarthThen(type: .custom)
        //: btn.isSelected = false
        btn.isSelected = false
        //: btn.spaceBetweenTitleAndImage = 20
        btn.spaceBetweenTitleAndImage = 20
        //: btn.setBackgroundColor(color: UIColor.init(red: 244/255.0, green: 244/255.0, blue: 244/255.0, alpha: 1.0), forState: .normal)
        btn.pastAccess(color: UIColor(red: 244 / 255.0, green: 244 / 255.0, blue: 244 / 255.0, alpha: 1.0), forState: .normal)
        //: btn.setBackgroundColor(color: UIColor.init(hex: "#2ABBFF")!, forState: .selected)
        btn.pastAccess(color: UIColor(hex: (String(data_animalDevicePath) + String(data_medalText)))!, forState: .selected)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_male_nv_nor"), for: .normal)
        btn.setImage(UIImage.lastTo(name: (String(data_stockHideName.prefix(4)) + "male_nv" + String(user_extentData.prefix(4)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_male_nv_sel"), for: .selected)
        btn.setImage(UIImage.lastTo(name: (String(const_strokeMsg) + String(constGoingBeApplicationContent))), for: .selected)
        //: btn.setTitle("Male".localized, for: .normal)
        btn.setTitle((String(appPageMessage.prefix(4))).localized, for: .normal)
        //: btn.setTitle("Male".localized, for: .selected)
        btn.setTitle((String(appPageMessage.prefix(4))).localized, for: .selected)
        //: btn.setTitleColor(.black, for: .normal)
        btn.setTitleColor(.black, for: .normal)
        //: btn.setTitleColor(.white, for: .selected)
        btn.setTitleColor(.white, for: .selected)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 17)
        btn.titleLabel?.font = UIFont.activeBeautyNative(fontSize: 17)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var femaleButton: TalkingButton = {
    private lazy var femaleButton: EarthThen = {
        //: let btn = TalkingButton(type: .custom)
        let btn = EarthThen(type: .custom)
        //: btn.isSelected = false
        btn.isSelected = false
        //: btn.spaceBetweenTitleAndImage = 20
        btn.spaceBetweenTitleAndImage = 20
        //: btn.setBackgroundColor(color: UIColor.init(red: 244/255.0, green: 244/255.0, blue: 244/255.0, alpha: 1.0), forState: .normal)
        btn.pastAccess(color: UIColor(red: 244 / 255.0, green: 244 / 255.0, blue: 244 / 255.0, alpha: 1.0), forState: .normal)
        //: btn.setBackgroundColor(color: UIColor.init(hex: "#FF5372")!, forState: .selected)
        btn.pastAccess(color: UIColor(hex: (String(kEditMainId.prefix(7))))!, forState: .selected)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_female_nv_nor"), for: .normal)
        btn.setImage(UIImage.lastTo(name: (String(user_inviteMessage.prefix(6)) + "male_nv_n" + show_voteUrl.replacingOccurrences(of: "turn", with: "r"))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_female_nv_sel"), for: .selected)
        btn.setImage(UIImage.lastTo(name: (String(showContactFormat) + String(show_restrictionMsg.prefix(8)))), for: .selected)
        //: btn.setTitle("Female".localized, for: .normal)
        btn.setTitle((String(app_incomeDistanceMessage) + const_attributeTitle.replacingOccurrences(of: "power", with: "le")).localized, for: .normal)
        //: btn.setTitle("Female".localized, for: .selected)
        btn.setTitle((String(app_incomeDistanceMessage) + const_attributeTitle.replacingOccurrences(of: "power", with: "le")).localized, for: .selected)
        //: btn.setTitleColor(.black, for: .normal)
        btn.setTitleColor(.black, for: .normal)
        //: btn.setTitleColor(.white, for: .selected)
        btn.setTitleColor(.white, for: .selected)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 17)
        btn.titleLabel?.font = UIFont.activeBeautyNative(fontSize: 17)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var commitButton: UIButton = {
    private lazy var commitButton: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        btn.titleLabel?.font = UIFont.landEnableSize(type: .Medium, fontSize: 17)
        //: btn.setTitle("Next".localized, for: .normal)
        btn.setTitle((String(kEntryData.prefix(4))).localized, for: .normal)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: ScreenWidth-60, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.tillOfCreation(colors: UIColor.spectralColor(), size: CGSize(width: show_objectValue - 60, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Public Event

//: extension TalkingLoginGenderVC {
extension PublicallyReactiveCompatible {
    //: private func func__checkNextBtnState() {
    private func wall() {
        //: if MentionTriggerAppManager.share.userFillInfoMode.sex == "1" {
        if MentionTriggerAppManager.share.userFillInfoMode.sex == "1" {
            //: self.commitButton.isEnabled = true
            self.commitButton.isEnabled = true
            //: self.maleButton.isSelected = true
            self.maleButton.isSelected = true
            //: self.femaleButton.isSelected = false
            self.femaleButton.isSelected = false
            //: } else if MentionTriggerAppManager.share.userFillInfoMode.sex == "2"{
        } else if MentionTriggerAppManager.share.userFillInfoMode.sex == "2" {
            //: self.commitButton.isEnabled = true
            self.commitButton.isEnabled = true
            //: self.maleButton.isSelected = false
            self.maleButton.isSelected = false
            //: self.femaleButton.isSelected = true
            self.femaleButton.isSelected = true
            //: } else {
        } else {
            //: self.commitButton.isEnabled = false
            self.commitButton.isEnabled = false
            //: self.maleButton.isSelected = false
            self.maleButton.isSelected = false
            //: self.femaleButton.isSelected = false
            self.femaleButton.isSelected = false
        }
    }

    //: private func func__pushToNextProgress() {
    private func still() {
        //        // 1.3 判断用户设备SIM卡归属地是否来自被剔除的国家或地区
        //        // 1.4 判断用户设备是否开启VPN
//        if MentionTriggerAppManager.share.appConfigMode.vpnSimCardCheck {
//
//            guard  !ProjectionThen.isUsedProxy() && !ProjectionThen.isUsedVPN() else {
//                self.func__showStatusBarErrorMsg(showMsg: "Network error. Check it and try again.".localized)
//                self.navigationController?.popToRootViewController(animated: true)
//                NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
//                SubjectMatterManagerRequest.func__reportDeviceID()
//                return
//            }
//        }
        /// 清除后面已填写过资料
//        let sex = MentionTriggerAppManager.share.userFillInfoMode.sex
//        MentionTriggerAppManager.share.userFillInfoMode = SimulationWouldInfoModel.init()
//        MentionTriggerAppManager.share.userFillInfoMode.sex = sex
//        let VC = CurvetViewController()
//        self.navigationController?.pushViewController(VC, animated: true)

        //: view.endEditing(true)
        view.endEditing(true)
        //: ProgressHUD.show()
        LovelyLifetimeProgressHUD.launchSocialShow()
        //: TalkingLoginRequestTool.req_updateUserInfo(params: ["sex":MentionTriggerAppManager.share.userFillInfoMode.sex]) { succeed, result, errorModel in
        SpeakRequestTool.tillNearby(params: [String(bytes: k_pageEqualName.map{averageMargin(value: $0)}, encoding: .utf8)!: MentionTriggerAppManager.share.userFillInfoMode.sex]) { succeed, result, _ in
            //: ProgressHUD.dismiss()
            LovelyLifetimeProgressHUD.arcCurDismiss()
            //: if succeed {
            if succeed {
                // 发送通知
                //: NotificationCenter.default.post(name: DID_LOGIN_SUCCESS_NOTIFICATION, object: nil, userInfo: result as! Dictionary<String, Any>)
                NotificationCenter.default.post(name: userPageId, object: nil, userInfo: result as! [String: Any])
                // 注册成功埋点
                //: TalkingAdjustManager.share.addEvent(key: "RegisterSuccess")
                BorderThen.share.shareFill(key: (String(app_lightGesturePath) + String(userSpeedName)))
                //: FirebaseAnalyticsManager.share.logEvent(name: "RegisterSuccess")
                GrayAnalyticsManager.share.offMethod(name: (String(app_lightGesturePath) + String(userSpeedName)))
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingLoginGenderVC {
extension PublicallyReactiveCompatible {
    // 添加视图
    //: private func setupSubviews() {
    private func hair() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: view.addSubview(logoTitleLabel)
        view.addSubview(logoTitleLabel)
        //: view.addSubview(genderTipLabel)
        view.addSubview(genderTipLabel)
        //: view.addSubview(maleButton)
        view.addSubview(maleButton)
        //: view.addSubview(femaleButton)
        view.addSubview(femaleButton)
        //: view.addSubview(commitButton)
        view.addSubview(commitButton)
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func awakeConstraint() {
        //: logoTitleLabel.snp.makeConstraints { make in
        logoTitleLabel.snp.makeConstraints { make in
            //: make.top.equalTo(34)
            make.top.equalTo(34)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
        }
        //: genderTipLabel.snp.makeConstraints { make in
        genderTipLabel.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.width.equalTo(ScreenWidth-60)
            make.width.equalTo(show_objectValue - 60)
            //: make.top.equalTo(logoTitleLabel.snp.bottom).offset(6)
            make.top.equalTo(logoTitleLabel.snp.bottom).offset(6)
        }
        //: maleButton.snp.makeConstraints { make in
        maleButton.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.width.equalTo(actualWidth(w: 140))
            make.width.equalTo(actualWidth(w: 140))
            //: make.height.equalTo(actualWidth(w: 50))
            make.height.equalTo(actualWidth(w: 50))
            //: make.top.equalTo(genderTipLabel.snp.bottom).offset(20)
            make.top.equalTo(genderTipLabel.snp.bottom).offset(20)
        }
        //: femaleButton.snp.makeConstraints { make in
        femaleButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.width.equalTo(actualWidth(w: 140))
            make.width.equalTo(actualWidth(w: 140))
            //: make.height.equalTo(actualWidth(w: 50))
            make.height.equalTo(actualWidth(w: 50))
            //: make.top.equalTo(genderTipLabel.snp.bottom).offset(20)
            make.top.equalTo(genderTipLabel.snp.bottom).offset(20)
        }
        //: commitButton.snp.makeConstraints { make in
        commitButton.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.width.equalTo(ScreenWidth-60)
            make.width.equalTo(show_objectValue - 60)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
            //: make.top.equalTo(maleButton.snp.bottom).offset(40)
            make.top.equalTo(maleButton.snp.bottom).offset(40)
        }
    }

    // 添加事件
    //: private func bindInteraction() {
    private func maxQuantityeractionPer() {
        //: commitButton.rx.tap
        commitButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func__pushToNextProgress()
                self.still()

                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: femaleButton.rx.tap
        femaleButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: MentionTriggerAppManager.share.userFillInfoMode.sex = "2"
                MentionTriggerAppManager.share.userFillInfoMode.sex = "2"
                //: self.func__checkNextBtnState()
                self.wall()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: maleButton.rx.tap
        maleButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: MentionTriggerAppManager.share.userFillInfoMode.sex = "1"
                MentionTriggerAppManager.share.userFillInfoMode.sex = "1"
                //: self.func__checkNextBtnState()
                self.wall()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
