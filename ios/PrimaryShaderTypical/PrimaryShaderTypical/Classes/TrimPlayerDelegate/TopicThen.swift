
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let showFieldMessage:[Character] = ["m","f","/","m","o"]
fileprivate let user_interruptName:String = "ment/listfine flag trust"

/*: "mf/moment/moments" :*/
fileprivate let constSeekName:[Character] = ["m","f","/","m","o","m","e","n","t","/","m","o","m","e","n","t"]
fileprivate let mainTechniqueStr:String = "lay"

/*: "mf/moment/like" :*/
fileprivate let mainDestinationText:String = "mf/momtable wood burn disable contrast"
fileprivate let showDialValue:String = "issueke"

/*: "mf/crush/send" :*/
fileprivate let kExposureUrl:[Character] = ["m","f","/","c","r","u","s","h","/","s"]
fileprivate let k_appearanceSharedCreatureData:[Character] = ["e","n","d"]

/*: "mf/moment/view" :*/
fileprivate let userYaValue:String = "mf/momupper cycle block"
fileprivate let app_distanceFormat:String = "vierefer"

/*: "mf/momentReply/list" :*/
fileprivate let main_tableKey:[Character] = ["m","f","/","m","o","m"]
fileprivate let mainSequenceUrl:String = "entRsuccessful favorite shadow hormone"
fileprivate let userWallFirmName:String = "lipolicy"

/*: "mf/momentReply/save" :*/
fileprivate let k_restrictionMiddlePath:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l","y","/","s"]
fileprivate let data_numbId:String = "AVE"

/*: "mf/moment/videoSign" :*/
fileprivate let user_insidePowerTitle:[Character] = ["m","f","/","m","o"]
fileprivate let noti_warningData:String = "mcontendnt"
fileprivate let show_subjectValue:String = "standard this premiumoSign"

/*: "dynamicPic.zip" :*/
fileprivate let show_volumeFormat:String = "dyreasonmi"
fileprivate let user_advertisingMessage:[Character] = [".","z","i","p"]

/*: "pic" :*/
fileprivate let user_farUrl:[UInt8] = [0x63,0x69,0x70]

/*: "content" :*/
fileprivate let constConsumerStr:String = "contconnectionn"
fileprivate let mainAccessKey:[Character] = ["t"]

/*: "mf/moment/save" :*/
fileprivate let mainRetMsg:[Character] = ["m","f","/","m","o","m","e","n","t"]
fileprivate let const_recommendationDisplayTitle:[Character] = ["/","s","a","v","e"]

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let kDisableSuspendValue:[UInt8] = [0x31,0x56,0x43,0x4d,0x4a,0x54,0x49,0x1,0x34,0x56,0x44,0x44,0x46,0x54,0x54,0x47,0x56,0x4d,0xd,0x1,0x28,0x50,0x1,0x55,0x50,0x1,0x2e,0x5a,0x1,0x25,0x5a,0x4f,0x42,0x4e,0x4a,0x44,0x54,0x1,0x55,0x50,0x1,0x57,0x4a,0x46,0x58]

fileprivate func implementWonder(feature num: UInt8) -> UInt8 {
    let value = Int(num) + 31
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/moment/saveVideo" :*/
fileprivate let notiMannerKey:String = "sweet foundationmf/mom"
fileprivate let mainProposeHundredContent:String = "proposal clipveVideo"

/*: .png" :*/
fileprivate let showPurchaseName:String = ".pngwait control"

/*: "dynamicPic" :*/
fileprivate let kErrorPath:String = "dynamwelcome"
fileprivate let k_sureValue:[Character] = ["P","i","c"]

/*: "freeImage" :*/
fileprivate let noti_combineValue:String = "frmerely"
fileprivate let const_interestedName:String = "Imagediscount own"

/*: "mf/momentReply/replylist" :*/
fileprivate let constClickHerePath:String = "elect star phonemf/mom"
fileprivate let constCheckMsg:String = "ply/rreply mutual compute"
fileprivate let show_receiveMessage:String = "eplsegmentlist"

/*: "mf/moment/delete" :*/
fileprivate let constCoverTitle:[Character] = ["m","f","/","m","o","m","e","n","t","/","d","e","l","e","t"]
fileprivate let notiAdditionStr:[Character] = ["e"]

/*: "mf/momentReply/delete" :*/
fileprivate let data_plainFormat:String = "index restriction following sea maybemf/m"
fileprivate let noti_profilePath:String = "tReplyfloor two"

/*: "mf/moment/pin" :*/
fileprivate let app_saveMapListenValue:String = "mf/momegift last"
fileprivate let kFeeFormat:String = "context assist squarent/pin"

/*: "mf/moment/remindNum" :*/
fileprivate let const_favoriteFormat:[Character] = ["m","f","/","m","o","m","e","n","t","/","r","e","m","i","n","d","N","u","m"]

/*: "mf/moment/remindList" :*/
fileprivate let kPurchaseAspectBaseballStr:String = "impression nothing bound agentmf/mo"
fileprivate let noti_judgeContent:String = "menad"
fileprivate let const_equallyData:String = "shift pagendList"

/*: "page" :*/
fileprivate let const_forwardManualAnswerTitle:[UInt8] = [0xac,0x9d,0xa3,0xa1]

fileprivate func segmentMutualCalled(privacy num: UInt8) -> UInt8 {
    let value = Int(num) - 60
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cos/getCredential" :*/
fileprivate let user_challengeNearbyText:[Character] = ["c","o","s"]
fileprivate let const_soundText:String = "/getcentral short dynamics meeting"
fileprivate let notiCombinedValue:String = "ticurvel"

/*: "type" :*/
fileprivate let userRouteTransmissionMsg:String = "TYPE"

/*: "user/reportUserInfo" :*/
fileprivate let constPowHelpKey:[Character] = ["u","s","e","r","/","r","e","p"]
fileprivate let kSmallStr:[Character] = ["o","r","t","U","s","e","r","I","n","f","o"]

/*: "uid" :*/
fileprivate let data_dialogName:[Character] = ["u","i","d"]

/*: "deviceId" :*/
fileprivate let main_quickScienceName:String = "ddecide"
fileprivate let userSeeStr:String = "viceIdcurve period"

/*: "deviceName" :*/
fileprivate let notiResponseId:[Character] = ["d","e","v","i","c","e","N","a"]
fileprivate let kYearData:String = "sea"

/*: "iphone" :*/
fileprivate let data_certainId:String = "ipsimplyone"

/*: "fileUrl" :*/
fileprivate let constChallengeName:String = "animal maybe harvest providerfileUrl"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TopicThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import Adjust
import Adjust
//: import UIKit
import UIKit

//: class TalkingMomentRequestTool: NSObject {
class TopicThen: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func lay(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(showFieldMessage) + String(user_interruptName.prefix(9)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func adminCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(constSeekName) + mainTechniqueStr.replacingOccurrences(of: "lay", with: "s"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func pendingCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(mainDestinationText.prefix(6)) + "ent/" + showDialValue.replacingOccurrences(of: "issue", with: "li"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func paramsDestroy(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(kExposureUrl) + String(k_appearanceSharedCreatureData))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func message(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(userYaValue.prefix(6)) + "ent/" + app_distanceFormat.replacingOccurrences(of: "refer", with: "w"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func outsideDrown(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(main_tableKey) + String(mainSequenceUrl.prefix(4)) + "eply/" + userWallFirmName.replacingOccurrences(of: "policy", with: "st"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func nightcap(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(k_restrictionMiddlePath) + data_numbId.lowercased())
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func alive(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(user_insidePowerTitle) + noti_warningData.replacingOccurrences(of: "contend", with: "e") + "/vide" + String(show_subjectValue.suffix(5)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func eavesdropLive(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        decision()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            get(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((show_volumeFormat.replacingOccurrences(of: "reason", with: "na") + "cPic" + String(user_advertisingMessage)))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: user_farUrl.reversed(), encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(constConsumerStr.replacingOccurrences(of: "connection", with: "e") + String(mainAccessKey))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = ConfinementRequestModel()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(mainRetMsg) + String(const_recommendationDisplayTitle))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.noExtra(showMsg: String(bytes: kDisableSuspendValue.map{implementWonder(feature: $0)}, encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.decision()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func viaLicense(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(notiMannerKey.suffix(6)) + "ent/sa" + String(mainProposeHundredContent.suffix(7)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func get(index: Int, image: UIImage) {
        //: let data = image.compressedImageData()
        let data = image.beyondQuote()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(showPurchaseName.prefix(4)))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((kErrorPath.replacingOccurrences(of: "welcome", with: "ic") + String(k_sureValue)))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func decision() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((noti_combineValue.replacingOccurrences(of: "merely", with: "ee") + String(const_interestedName.prefix(5))))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func one(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(constClickHerePath.suffix(6)) + "entRe" + String(constCheckMsg.prefix(5)) + show_receiveMessage.replacingOccurrences(of: "segment", with: "y"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func descriptionCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(constCoverTitle) + String(notiAdditionStr))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func visitorCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(data_plainFormat.suffix(4)) + "omen" + String(noti_profilePath.prefix(6)) + "/delete")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func featureIt(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(app_saveMapListenValue.prefix(7)) + String(kFeeFormat.suffix(6)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func charm(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(const_favoriteFormat))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func resourceCompletion(page: Int, completion: @escaping (_ data: [TipHandyJSON]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(kPurchaseAspectBaseballStr.suffix(5)) + noti_judgeContent.replacingOccurrences(of: "ad", with: "t") + "/remi" + String(const_equallyData.suffix(6)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: const_forwardManualAnswerTitle.map{segmentMutualCalled(privacy: $0)}, encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { _, result, _ in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [TipHandyJSON] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<TipHandyJSON>.deserialize(from: arr as? Array) as? [TipHandyJSON] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func editionReply(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(user_challengeNearbyText) + String(const_soundText.prefix(4)) + "Creden" + notiCombinedValue.replacingOccurrences(of: "curve", with: "a"))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(userRouteTransmissionMsg.lowercased()): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func multiWall(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestServer = MentionTriggerAppManager.share.appConfigMode.reportDomain
        reqModel.requestServer = MentionTriggerAppManager.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (String(constPowHelpKey) + String(kSmallStr))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = MentionTriggerAppManager.share.loginUserMode.userID
        params[(String(data_dialogName))] = MentionTriggerAppManager.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(main_quickScienceName.replacingOccurrences(of: "decide", with: "e") + String(userSeeStr.prefix(6)))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(String(notiResponseId) + kYearData.replacingOccurrences(of: "sea", with: "me"))] = (data_certainId.replacingOccurrences(of: "simply", with: "h"))
        //: params["type"] = 1
        params[(userRouteTransmissionMsg.lowercased())] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(constChallengeName.suffix(7)))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
