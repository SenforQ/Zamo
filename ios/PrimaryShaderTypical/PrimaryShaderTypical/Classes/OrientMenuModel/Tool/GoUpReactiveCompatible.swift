
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let constMaximumToneMsg:[Character] = ["m","f","/","b","u","s","i","n","e","s","s"]
fileprivate let user_exhibitBeneathWarnKey:[Character] = ["/","g","i","f","t","L","i","s","t"]

/*: "type" :*/
fileprivate let appChooseWriteMessage:[UInt8] = [0xcc,0xc1,0xc8,0xdd]

private func pencilHear(strong num: UInt8) -> UInt8 {
    return num ^ 184
}

/*: "mf/chatRoom/giftList" :*/
fileprivate let constTranslationMaleStr:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","g","i","f"]
fileprivate let kCutContent:String = "it just calculate nut editortList"

/*: "live/giftList" :*/
fileprivate let dataCompositionId:[Character] = ["l","i","v","e","/"]
fileprivate let k_highlightTitle:[Character] = ["g","i","f","t","L","i","s","t"]

/*: "IM/getMessageList" :*/
fileprivate let k_playUrl:[Character] = ["I","M","/","g","e","t","M","e","s","s","a","g","e","L","i","s"]
fileprivate let showProductUrl:String = "what"

/*: "mf/business/sendMsg" :*/
fileprivate let main_entitleText:[Character] = ["m","f","/","b","u","s","i","n"]
fileprivate let k_beyondUrl:[Character] = ["e","s","s","/","s","e","n","d","M","s","g"]

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let data_apartmentName:String = "beatf"
fileprivate let userCoatMessage:String = "/chatroute scene display vote describe"
fileprivate let notiEntryName:String = "put start injury print interaction/sen"

/*: "mf/business/sendGift" :*/
fileprivate let main_resolutionFormat:String = "mf/bucamera contain off rein"
fileprivate let show_markName:String = "partner party drag rank/send"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let noti_outputUrl:String = "age feemf/ch"
fileprivate let showBlankMsg:String = "m/semain related shape"
fileprivate let appOrientationName:[Character] = ["f","t"]

/*: "live/sendGift" :*/
fileprivate let app_numberData:[Character] = ["l","i","v","e","/","s","e","n","d","G","i","f","t"]

/*: "mf/user/getChatInfo" :*/
fileprivate let showMutualName:[Character] = ["m","f","/","u","s","e","r","/","g","e","t","C","h","a","t","I","n","f","o"]

/*: "IM/getMessageUserInfoList" :*/
fileprivate let noti_imageUrl:String = "IM/geblack possible design ex"
fileprivate let kRevenueOccurKey:String = "write force list announcementgeUserIn"
fileprivate let constMemoryTitle:[Character] = ["f","o","L","i","s","t"]

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let notiNetworkData:[Character] = ["m","f","/","c","r","u","s","h","/","w","h","o","L","i","k","e","M","e","L","i","s","t"]

/*: "mf/crush/ignore" :*/
fileprivate let dataAutomaticallyStr:String = "mf/cruliteral block staff"
fileprivate let main_mightName:String = "NORE"

/*: "mf/crush/like" :*/
fileprivate let dataRejectKey:[Character] = ["m","f","/","c"]
fileprivate let const_closedUrl:[Character] = ["r","u","s","h","/","l","i","k","e"]

/*: "mf/user/getRelation" :*/
fileprivate let dataOverlookValue:[Character] = ["m","f","/","u","s","e","r","/","g"]
fileprivate let data_triggerMsg:[Character] = ["e","t","R","e","l","a","t","i","o","n"]

/*: "user/attention" :*/
fileprivate let kAddValue:[UInt8] = [0x6a,0x68,0x5a,0x67,0x24,0x56,0x69,0x69,0x5a,0x63,0x69,0x5e,0x64,0x63]

fileprivate func missSale(po num: UInt8) -> UInt8 {
    let value = Int(num) + 11
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "user/removeAttention" :*/
fileprivate let app_headlineId:[Character] = ["u","s","e","r","/"]
fileprivate let userWorkMessage:String = "remochin"
fileprivate let appSendUrl:[Character] = ["e","n","t","i","o","n"]

/*: "Can't earn points if you follow each other~" :*/
fileprivate let data_bringTitle:[UInt8] = [0x4d,0x6f,0x60,0x29,0x7a,0x2e,0x6b,0x6f,0x7c,0x60,0x2e,0x7e,0x61,0x67,0x60,0x7a,0x7d,0x2e,0x67,0x68,0x2e,0x77,0x61,0x7b,0x2e,0x68,0x61,0x62,0x62,0x61,0x79,0x2e,0x6b,0x6f,0x6d,0x66,0x2e,0x61,0x7a,0x66,0x6b,0x7c,0x70]

private func sparkWhen(narrow num: UInt8) -> UInt8 {
    return num ^ 14
}

/*: "follow Success" :*/
fileprivate let constStrategyValue:[Character] = ["f","o","l","l","o","w"," ","S","u","c"]
fileprivate let data_poseKey:String = "streetss"

/*: "unfollow Success" :*/
fileprivate let user_errTitle:[Character] = ["u","n","f","o","l","l"]
fileprivate let const_labelData:String = "ow Sucnever forehead thumb"

/*: "translate/msg" :*/
fileprivate let noti_indexFormat:String = "trahoney"
fileprivate let dataDescriptionStr:String = "going mention aide/msg"

/*: "msgId" :*/
fileprivate let constProcessorFormat:[UInt8] = [0x49,0x57,0x43,0x6d,0x40]

private func problemAverageMaximum(nearby num: UInt8) -> UInt8 {
    return num ^ 36
}

/*: "targetLng" :*/
fileprivate let kHappyMessage:[UInt8] = [0xfe,0xeb,0xf8,0xed,0xef,0xfe,0xc6,0xe4,0xed]

private func admixtureIntervalerest(north num: UInt8) -> UInt8 {
    return num ^ 138
}

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let appThemeId:String = "dynamic column wood appointmentmf/gr"
fileprivate let mainDecreaseTitle:String = "etAutoGreflection platform conduct disappear reader"
fileprivate let data_hundredId:String = "reetCred green select product"

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let data_multiStr:String = "strength found application futuremf/g"
fileprivate let main_routeFormat:String = "getIclass discount problem resistance"
fileprivate let showScanMsg:String = "otranslationo"

/*: "toUid" :*/
fileprivate let show_requireMerelyMsg:[UInt8] = [0x44,0x5f,0x65,0x59,0x54]

/*: "mf/chatRoom/info" :*/
fileprivate let dataExceptQuantityId:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","i","n","f","o"]

/*: "mf/chatRoom/list" :*/
fileprivate let k_estimateMessage:[Character] = ["m","f","/","c","h","a","t","R"]
fileprivate let notiBirthdayFormat:String = "O"
fileprivate let user_describeName:[Character] = ["o","m","/","l","i","s","t"]

/*: "roomId" :*/
fileprivate let appRatingTitle:[UInt8] = [0x20,0x3d,0x3d,0x3f,0x1b,0x36]

/*: "needExtraInfo" :*/
fileprivate let dataFailTitle:[UInt8] = [0x38,0x33,0x33,0x32,0x13,0x2e,0x22,0x24,0x37,0x1f,0x38,0x30,0x39]

private func transmissionFace(outlet num: UInt8) -> UInt8 {
    return num ^ 86
}

/*: "mf/chatRoom/check" :*/
fileprivate let k_engageId:String = "photo holder detectmf/c"
fileprivate let userPhysicalFormat:[Character] = ["c","h","e","c","k"]

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let user_goodValue:String = "net sum into curvemf/ch"
fileprivate let app_toleranceName:String = "atRoselect doc fill valid"
fileprivate let userProviderMessage:String = "book case record awake outsideinOrQuit"

/*: "mf/chatRoom/members" :*/
fileprivate let user_quantityFormat:[Character] = ["m","f","/","c","h","a","t","R","o","o","m"]
fileprivate let notiAnonRequireMsg:[Character] = ["/","m","e","m","b","e","r","s"]

/*: "translate/text" :*/
fileprivate let noti_introWaitMessage:String = "trhungnslhung"
fileprivate let data_resolutionMsg:String = "te/textagency low hello"

/*: "text" :*/
fileprivate let data_cropValue:[UInt8] = [0x58,0x49,0x5c,0x58]

fileprivate func whichTab(convert num: UInt8) -> UInt8 {
    let value = Int(num) - 228
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/user/queryStatus" :*/
fileprivate let k_nobodyTitle:[Character] = ["m","f","/","u","s","e","r","/","q","u","e","r","y","S","t","a","t","u","s"]

/*: "toUids" :*/
fileprivate let const_volumePath:[UInt8] = [0xf4,0xef,0xd5,0xe9,0xe4,0xf3]

private func threeLose(margin num: UInt8) -> UInt8 {
    return num ^ 128
}

/*: "call/getCallTab" :*/
fileprivate let k_maxMaybeLocationUrl:[Character] = ["c","a","l","l","/","g"]
fileprivate let dataFollowMessage:[Character] = ["e","t","C","a","l","l","T","a","b"]

/*: "mf/user/remark" :*/
fileprivate let notiTransmissionBigName:[Character] = ["m","f","/","u","s","e","r","/","r","e","m"]
fileprivate let userDarkName:String = "ask"

/*: "content" :*/
fileprivate let user_currentBridgeStr:[UInt8] = [0x85,0x89,0x88,0x92,0x83,0x88,0x92]

private func partnerHere(bar num: UInt8) -> UInt8 {
    return num ^ 230
}

/*: "top" :*/
fileprivate let notiReplaceId:[UInt8] = [0x5b,0x40,0x5f]

private func overCapable(depth num: UInt8) -> UInt8 {
    return num ^ 47
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  GoUpReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class TalkingChatRequestTool: NSObject {
public class GoUpReactiveCompatible: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [PronkCacheModel]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [PronkCacheModel]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func man(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(constMaximumToneMsg) + String(user_exhibitBeneathWarnKey))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: appChooseWriteMessage.map{pencilHear(strong: $0)}, encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func endComponent(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(constTranslationMaleStr) + String(kCutContent.suffix(5)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func visualizer(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (String(dataCompositionId) + String(k_highlightTitle))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func randomCompletion(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(k_playUrl) + showProductUrl.replacingOccurrences(of: "what", with: "t"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func equal(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (String(main_entitleText) + String(k_beyondUrl))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func aboveExtremeCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (data_apartmentName.replacingOccurrences(of: "beat", with: "m") + String(userCoatMessage.prefix(5)) + "Room" + String(notiEntryName.suffix(4)) + "dMsg")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func routeEffectForward(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(main_resolutionFormat.prefix(5)) + "siness" + String(show_markName.suffix(5)) + "Gift")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func worker(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(noti_outputUrl.suffix(5)) + "atRoo" + String(showBlankMsg.prefix(4)) + "ndGi" + String(appOrientationName))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func paramCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (String(app_numberData))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func statusGoCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(showMutualName))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func fieldFromCompletion(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(noti_imageUrl.prefix(5)) + "tMessa" + String(kRevenueOccurKey.suffix(8)) + String(constMemoryTitle))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, _ in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<PronkCacheModel>.deserialize(from: result as? Array) {
            if let datas = Array<PronkCacheModel>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [PronkCacheModel])
                completion(succeed, datas as! [PronkCacheModel])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func sizeMaterial(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        userIdentityContent.centralCityCompletion(giftUrl: giftUrl, path: path) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func serverRes(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (String(notiNetworkData))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func weeklyCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(dataAutomaticallyStr.prefix(6)) + "sh/ig" + main_mightName.lowercased())
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func writeLive(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(dataRejectKey) + String(const_closedUrl))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func substructure(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(dataOverlookValue) + String(data_triggerMsg))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func adCompletion(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: kAddValue.map{missSale(po: $0)}, encoding: .utf8)! : (String(app_headlineId) + userWorkMessage.replacingOccurrences(of: "chin", with: "v") + "eAtt" + String(appSendUrl))
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: if MentionTriggerAppManager.share.loginUserMode.sex == Gender.female.rawValue {
                    if MentionTriggerAppManager.share.loginUserMode.sex == TopicKeyRepresentable.female.rawValue { // 女性
                        //: object.func__showStatusBarSuccessMsg(showMsg: "Can't earn points if you follow each other~".localized)
                        const_viewTitle.noExtra(showMsg: String(bytes: data_bringTitle.map{sparkWhen(narrow: $0)}, encoding: .utf8)!.localized)
                        //: } else {
                    } else {
                        //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                        const_viewTitle.noExtra(showMsg: (String(constStrategyValue) + data_poseKey.replacingOccurrences(of: "street", with: "ce")).localized)
                    }
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    const_viewTitle.noExtra(showMsg: (String(user_errTitle) + String(const_labelData.prefix(6)) + "cess").localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                const_viewTitle.outBarMsg(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func toALesserExtent(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (noti_indexFormat.replacingOccurrences(of: "honey", with: "n") + "slat" + String(dataDescriptionStr.suffix(5)))
        //: reqModel.params = ["msgId": msgId, "targetLng": EverlastinglyReactiveCompatible.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = [String(bytes: constProcessorFormat.map{problemAverageMaximum(nearby: $0)}, encoding: .utf8)!: msgId, String(bytes: kHappyMessage.map{admixtureIntervalerest(north: $0)}, encoding: .utf8)!: EverlastinglyReactiveCompatible.feedAnimal(), String(bytes: appChooseWriteMessage.map{pencilHear(strong: $0)}, encoding: .utf8)!: "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func textCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(appThemeId.suffix(5)) + "eet/g" + String(mainDecreaseTitle.prefix(7)) + String(data_hundredId.prefix(5)) + "ontent")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func gestureSituation(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(data_multiStr.suffix(4)) + "reet/" + String(main_routeFormat.prefix(4)) + "ntimatePh" + showScanMsg.replacingOccurrences(of: "translation", with: "t"))
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = [String(bytes: show_requireMerelyMsg.map{$0^48}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func currency(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (String(dataExceptQuantityId))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func twentyFourHoursCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(k_estimateMessage) + notiBirthdayFormat.lowercased() + String(user_describeName))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func nameWith(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: appRatingTitle.map{$0^82}, encoding: .utf8)!: roomId, String(bytes: dataFailTitle.map{transmissionFace(outlet: $0)}, encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(k_engageId.suffix(4)) + "hatRoom/" + String(userPhysicalFormat))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func miniskirt(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (String(user_goodValue.suffix(5)) + String(app_toleranceName.prefix(4)) + "om/jo" + String(userProviderMessage.suffix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func groupGetCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (String(user_quantityFormat) + String(notiAnonRequireMsg))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - text: 文本
    ///   - completion: 回调
    //: class func req_translateText(text: String, completion: @escaping FinishBlock) {
    class func repeatLikeCompletion(text: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "translate/text"
        reqModel.requestPath = (noti_introWaitMessage.replacingOccurrences(of: "hung", with: "a") + String(data_resolutionMsg.prefix(7)))
        //: reqModel.params = ["text": text]
        reqModel.params = [String(bytes: data_cropValue.map{whichTab(convert: $0)}, encoding: .utf8)!: text]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func lessInsert(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(k_nobodyTitle))
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: const_volumePath.map{threeLose(margin: $0)}, encoding: .utf8)!: toUids, String(bytes: appChooseWriteMessage.map{pencilHear(strong: $0)}, encoding: .utf8)!: 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, _ in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func strengthSomeone(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (String(k_maxMaybeLocationUrl) + String(dataFollowMessage))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func afterRe(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ConfinementRequestModel()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (String(notiTransmissionBigName) + userDarkName.replacingOccurrences(of: "s", with: "r"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: user_currentBridgeStr.map{partnerHere(bar: $0)}, encoding: .utf8)!: content, String(bytes: notiReplaceId.map{overCapable(depth: $0)}, encoding: .utf8)!: isTop, String(bytes: show_requireMerelyMsg.map{$0^48}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userIdentityContent.downMid(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
