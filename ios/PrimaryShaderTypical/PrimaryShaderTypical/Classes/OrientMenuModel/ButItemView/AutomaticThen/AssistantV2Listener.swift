
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let user_wholeName:[Character] = ["I","M","退","出","登","录","失","败","\u{ff0c}","c"]
fileprivate let appYouId:[Character] = ["o","d","e",":"]

/*: , desc: :*/
fileprivate let app_focusMessage:String = ", "
fileprivate let kSlimUrl:String = "desc:accelerate and editor ignore"

/*: "IM登录失败，code: :*/
fileprivate let user_wallStr:String = "IM\u{767b}\u{5f55}失"

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let notiCountUrl:String = "bean ion product quality dateIM获取"
fileprivate let noti_secureFindFiniteUrl:String = "失败，cpresentation may regular"

/*: "crushBadge" :*/
fileprivate let constSoData:String = "restoreru"
fileprivate let notiBeatGloryAirMsg:String = "propose weshBadge"

/*: "badNumber" :*/
fileprivate let user_enjoyMsg:[UInt8] = [0x5f,0x5c,0x59,0x73,0x48,0x50,0x5f,0x58,0x4f]

/*: "plaza" :*/
fileprivate let noti_birthdayData:String = "placonsumera"

/*: "roomNum" :*/
fileprivate let constScheduleStr:String = "stream against rateroomNum"

/*: "num" :*/
fileprivate let dataNarrowCountText:[Character] = ["n","u","m"]

/*: "onlyOneRoomId" :*/
fileprivate let showCompoundName:[Character] = ["o","n","l","y","O","n","e","R","o","o","m","I","d"]

/*: "LIVE-CHAT" :*/
fileprivate let dataSenseObserveUrl:[Character] = ["L","I","V","E","-","C"]
fileprivate let dataSHeadlineText:String = "lensAT"

/*: "extra" :*/
fileprivate let mainStandardKey:String = "extlocation"

/*: "msgType" :*/
fileprivate let constSupportKey:String = "msgTypevolume learn publicly"

/*: "newPkgGift" :*/
fileprivate let noti_aValue:String = "nschedule"
fileprivate let constCandidSignalSlowText:[Character] = ["w","P","k","g","G","i","f","t"]

/*: "MF:ChatMentionMsg" :*/
fileprivate let user_forthName:[Character] = ["M","F",":","C","h","a","t","M","e","n","t","i","o","n"]
fileprivate let user_letterName:[Character] = ["M","s","g"]

/*: "uid" :*/
fileprivate let showInsideName:[UInt8] = [0x9a,0x8e,0x89]

fileprivate func adAdvice(running num: UInt8) -> UInt8 {
    let value = Int(num) + 219
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "onlineStatus" :*/
fileprivate let show_indexCommonStr:[UInt8] = [0x1f,0x1e,0x1c,0x19,0x1e,0x15,0x3,0x24,0x11,0x24,0x25,0x23]

fileprivate func preparePow(closed num: UInt8) -> UInt8 {
    let value = Int(num) - 176
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "isNewUser" :*/
fileprivate let kWordData:[UInt8] = [0xc0,0xca,0xa5,0xbc,0xce,0xac,0xca,0xbc,0xc9]

fileprivate func behindJoin(nature num: UInt8) -> UInt8 {
    let value = Int(num) - 87
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "c2c_ :*/
fileprivate let appBanPath:String = "c2c_empty base"

/*: "IM获取会话失败，convId: :*/
fileprivate let k_accommodateName:String = "rare build while function extensiveIM获取"
fileprivate let user_productionStr:String = "forehead weekly，convId"
fileprivate let show_hangMessage:String = ":"

/*: , code: :*/
fileprivate let show_cloudThumbMsg:String = ", codblade sum jungle hear"
fileprivate let dataPresentationStr:[Character] = ["e",":"]

/*: "IM插入消息失败，targetId: :*/
fileprivate let main_frontLegacyMessage:String = "IM插入消息place pan"
fileprivate let k_transactionData:String = "assist bubble maybe comegetId:"

/*: "changeDirection" :*/
fileprivate let dataEnvironmentValue:[Character] = ["c","h","a","n","g"]
fileprivate let userHairMarketName:String = "mic"
fileprivate let constFemaleSafetyFormat:String = "baseball telephone mm howDirection"

/*: "rYMsgType" :*/
fileprivate let app_heelWithoutKey:String = "pure comerYMs"

/*: "GJ:CallCustom" :*/
fileprivate let show_quoteName:String = "GJ:Ccut cigarette low"
fileprivate let const_injuryKey:String = "calledtom"

/*: "IM获取会话列表失败，code: :*/
fileprivate let appRandomPath:String = "IM获取会\u{8bdd}列"
fileprivate let app_tickGreenMsg:String = "greet process makeup financial表失败，code:"

/*: "msgInfo" :*/
fileprivate let mainLatAppId:String = "msgInfothe greatest application"

/*: "needFold" :*/
fileprivate let appStillPath:[Character] = ["n","e","e","d","F","o","l","d"]

/*: "group_ :*/
fileprivate let data_tensionText:String = "but event instancegroup_"

/*: "IM删除会话失败，convID: :*/
fileprivate let main_resourceAppealTitle:String = "IM删除reward create below"
fileprivate let dataPassPartnerCurrentlyContent:[Character] = ["n","v","I","D",":"]

/*: "IM置顶会话失败，convId: :*/
fileprivate let constRecentEvaluationName:[Character] = ["I","M","置"]
fileprivate let show_roundingDailyAdvertisingContent:String = "顶会life失败"

/*: "SDK 正在连接到服务器" :*/
fileprivate let const_createPicPath:[Character] = ["S","D","K"," ","正"]
fileprivate let const_universalValue:String = "在连接到服\u{52a1}器"

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let constPerformValue:String = "SDK \u{5df2}经"
fileprivate let const_warnRowText:String = "\u{6210}功连"
fileprivate let app_progressPath:[Character] = ["接","到","服","\u{52a1}","器"]

/*: "IM连接服务器失败，code: :*/
fileprivate let data_extensiveContent:String = "IM连接constraint library mark help people"
fileprivate let appBillButtonSquareUrl:String = "word interaction direction brush，code:"

/*: , error: :*/
fileprivate let app_exclusiveStr:[Character] = [","," ","e","r","r","o","r",":"]

/*: "IM账号被挤下线。" :*/
fileprivate let constListenTitle:[Character] = ["I","M","账","号","被","挤","\u{4e0b}","线","。"]

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let showUniversalTitle:[UInt8] = []

fileprivate func goalSlim(rating num: UInt8) -> UInt8 {
    let value = Int(num) - 202
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Offline Notification" :*/
fileprivate let userLatName:String = "meeting interested perOffli"
fileprivate let noti_compelWeId:String = "corefica"

/*: "Cancel" :*/
fileprivate let show_normalKey:[Character] = ["C","a","n","c","e"]
fileprivate let userCurtainId:String = "agree"

/*: "Re Login" :*/
fileprivate let notiIonPath:String = "Re Loginfoundation if"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AssistantV2Listener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol WayObjectProtocol: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func sharedInmate(data: [AutomaticThen])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func wait(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func uphold(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum MysteryDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class AssistantV2Listener: NSObject {
@objcMembers public class AssistantV2Listener: NSObject {
    //: public static let shared = AssistantV2Listener()
    public static let shared = AssistantV2Listener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [AutomaticThen] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [AutomaticThen] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [AutomaticThen] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [AutomaticThen] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [AutomaticThen] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: AutomaticThen]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.re()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func beforeShareInit() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(k_buttonKey, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func re() {
        //: func__removeAllDelegate()
        match()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.afterTapList()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func afterTapList() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = AutomaticThen.garnerHeadTypeRecord(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = AutomaticThen.garnerHeadTypeRecord(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func voiceOverSuspend() -> [AutomaticThen] {
        // 审核模式，在消息列表移除官方客服
        //: if MentionTriggerAppManager.share.appStatus == AppSkinStatus.special.rawValue {
        if MentionTriggerAppManager.share.appStatus == AidNameConvertible.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == EverlastinglyReactiveCompatible.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == EverlastinglyReactiveCompatible.breakup() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension AssistantV2Listener {
extension AssistantV2Listener {
    //: func func__removeAllDelegate() {
    func match() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func goResult(_ delegate: WayObjectProtocol) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func placeRemove(_ delegate: WayObjectProtocol) {
        //: self.func__invokeDelegate {
        self.convertCreation {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func convertCreation(_ invocation: (WayObjectProtocol) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! WayObjectProtocol)
        }
    }
}

// MARK: - IM登录态管理

//: extension AssistantV2Listener {
extension AssistantV2Listener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func artefactOut() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
//            CountegrityThen.exterminate(msg: (String(user_wholeName) + String(appYouId)) + "\(code)" + (app_focusMessage.capitalized + String(kSlimUrl.prefix(5))) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.re()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func deadline(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = MentionTriggerAppManager.share.appUserConfigMode.uSig
        let uSigStr = MentionTriggerAppManager.share.appUserConfigMode.uSig
        //: let uidStr = MentionTriggerAppManager.share.loginUserMode.userID
        let uidStr = MentionTriggerAppManager.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.request()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
//            CountegrityThen.exterminate(msg: (user_wallStr + "\u{8d25}，code:") + "\(code)" + (app_focusMessage.capitalized + String(kSlimUrl.prefix(5))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func transferSpace() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || MentionTriggerAppManager.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || MentionTriggerAppManager.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.outBarMsg(showMsg: showLicenseOutputName)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension AssistantV2Listener {
extension AssistantV2Listener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func request() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
//            CountegrityThen.exterminate(msg: (String(notiCountUrl.suffix(4)) + "未\u{8bfb}消息总数" + String(noti_secureFindFiniteUrl.prefix(4)) + "ode:") + "\(code)" + (app_focusMessage.capitalized + String(kSlimUrl.prefix(5))) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func emptyMsg() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: TalkingChatRequestTool.req_sysMessageList { _, result in
        GoUpReactiveCompatible.randomCompletion { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(constSoData.replacingOccurrences(of: "restore", with: "c") + String(notiBeatGloryAirMsg.suffix(7)))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: notiQuantityPostFormat, object: self, userInfo: [String(bytes: user_enjoyMsg.map{$0^61}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func earlier(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: TalkingChatRequestTool.req_getChatRoomInfo { succeed, result, _ in
        GoUpReactiveCompatible.currency { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(noti_birthdayData.replacingOccurrences(of: "consumer", with: "z"))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(constScheduleStr.suffix(7)))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = AutomaticThen.garnerHeadTypeRecord(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(String(dataNarrowCountText))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(showCompoundName))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension AssistantV2Listener: V2TIMAdvancedMsgListener {
extension AssistantV2Listener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT")) { // 开播 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(dataSenseObserveUrl) + dataSHeadlineText.replacingOccurrences(of: "lens", with: "H")))) { // 开播 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            SignatureDanmuManager.routeMCurrency().danmu(msg: msg)
            //: return
            return
        }
        //: if msg.sender == MentionTriggerAppManager.share.loginUserMode.userID { return }
        if msg.sender == MentionTriggerAppManager.share.loginUserMode.userID { return }

        //: let dic = ManagerReactiveCompatible.parseTXMessageData(data: msg.customElem.data)
        let dic = ManagerReactiveCompatible.sheaf(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(mainStandardKey.replacingOccurrences(of: "location", with: "ra"))] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? OrganModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: appLiveMessage, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(constSupportKey.prefix(7)))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (noti_aValue.replacingOccurrences(of: "schedule", with: "e") + String(constCandidSignalSlowText)) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: show_needMessage, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if ManagerReactiveCompatible.checkVoiceMessage(message: msg) {
            if ManagerReactiveCompatible.inscription(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = ChatManager.visible(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                WeightMsgTable.belowQuality(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.allComponent(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            ChatGloryPackMullerBackstageThen.shared.receiveIcon(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            ChatGloryPackMullerBackstageThen.shared.administrator(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(user_forthName) + String(user_letterName)) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            MotivationTopicChatManager.share.displayProduct(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: GradePointAverageThen.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: GradePointAverageThen.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != EverlastinglyReactiveCompatible.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != EverlastinglyReactiveCompatible.layChild() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.convertCreation { $0.uphold?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            StraightCypherNotificationManager.shared.leave(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: showInsideName.map{adAdvice(running: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: show_indexCommonStr.map{preparePow(closed: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: kWordData.map{behindJoin(nature: $0)}, encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: userBackgroundMessage,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            BoundPackInfoManager.controlTime(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            GradePointAverageThen.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: userSaltRouteId, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: TriggerCellData, isPrivete: Bool) {
    @objc public func ratingLess(targetId: String, msgCellData: TriggerCellData, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.pickTo(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(appBanPath.prefix(4))) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.quickList(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.blindGestureFire(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
//                CountegrityThen.exterminate(msg: (String(k_accommodateName.suffix(4)) + "会话失败" + String(user_productionStr.suffix(7)) + show_hangMessage.capitalized) + "\(convId)" + (String(show_cloudThumbMsg.prefix(5)) + String(dataPresentationStr)) + "\(code)" + (app_focusMessage.capitalized + String(kSlimUrl.prefix(5))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func pickTo(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = MentionTriggerAppManager.share.loginUserMode.userID
        let senderStr = MentionTriggerAppManager.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
//                CountegrityThen.exterminate(msg: (String(main_frontLegacyMessage.prefix(6)) + "失败，tar" + String(k_transactionData.suffix(6))) + "\(targetId)" + (String(show_cloudThumbMsg.prefix(5)) + String(dataPresentationStr)) + "\(code)" + (app_focusMessage.capitalized + String(kSlimUrl.prefix(5))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func allComponent(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(String(dataEnvironmentValue) + userHairMarketName.replacingOccurrences(of: "mic", with: "e") + String(constFemaleSafetyFormat.suffix(9)))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && TalkingAppPushManager.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(app_heelWithoutKey.suffix(4)) + "gType")].stringValue == (String(show_quoteName.prefix(4)) + "allC" + const_injuryKey.replacingOccurrences(of: "called", with: "us")) && isSelf && RiseMalePushManager.share.rf()!.isKind(of: AutomaticViewController.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: show_acceptMessage) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: show_acceptMessage)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension AssistantV2Listener: V2TIMConversationListener {
extension AssistantV2Listener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.quickList(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.blindGestureFire(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.quickList(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.blindGestureFire(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: MentionTriggerAppManager.share.unreadMessageNum = Int(totalUnreadCount)
        MentionTriggerAppManager.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.convertCreation {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.wait?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func blindGestureFire(conVersationList: [AutomaticThen]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if BoundPackInfoManager.starRandomStream().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                BoundPackInfoManager.starRandomStream().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.convertCreation {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.sharedInmate?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func quickInclude(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [AutomaticThen] = []

        // V2TIMConversation->AutomaticThen
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = AutomaticThen(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.next(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [AutomaticThen] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = ProvideInfoTable.per(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.produceFire(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension AssistantV2Listener {
extension AssistantV2Listener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func scheduleCompletion(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.quickInclude(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.blindGestureFire(conVersationList: arr as? [AutomaticThen] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
//            CountegrityThen.exterminate(msg: (appRandomPath + String(app_tickGreenMsg.suffix(9))) + "\(code)" + (app_focusMessage.capitalized + String(kSlimUrl.prefix(5))) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func handle(targetId: String) -> AutomaticThen? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func quickList(convList: [V2TIMConversation]) -> [AutomaticThen] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [AutomaticThen] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = AutomaticThen(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.next(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.produceFire(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func produceFire(convModel: AutomaticThen, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.afterTapList()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: MysteryDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - MentionTriggerAppManager.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - MentionTriggerAppManager.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = BoundPackInfoManager.terms(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = ManagerReactiveCompatible.parseTXMessageData(data: elemData)
            let dict = ManagerReactiveCompatible.sheaf(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(mainStandardKey.replacingOccurrences(of: "location", with: "ra"))][(String(mainLatAppId.prefix(7)))][(String(appStillPath))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.everyDate(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.everyDate(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func everyDate(convList: [AutomaticThen], dataType: MysteryDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func package(targetId: String, dataType: MysteryDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(appBanPath.prefix(4))) + "\(targetId)" : (String(data_tensionText.suffix(6))) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            BoundPackInfoManager.leftSlantingCaseHistory(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(MentionTriggerAppManager.share.loginUserMode.userID, toUid: targetId)
            WeightMsgTable.device(MentionTriggerAppManager.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.request()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
//            CountegrityThen.exterminate(msg: (String(main_resourceAppealTitle.prefix(4)) + "会话失败，co" + String(dataPassPartnerCurrentlyContent)) + "\(convID)" + (String(show_cloudThumbMsg.prefix(5)) + String(dataPresentationStr)) + "\(code)" + (app_focusMessage.capitalized + String(kSlimUrl.prefix(5))) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func sectionTop(convModel: AutomaticThen, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
//            CountegrityThen.exterminate(msg: (String(constRecentEvaluationName) + show_roundingDailyAdvertisingContent.replacingOccurrences(of: "life", with: "话") + "，convId:") + "\(convModel.conversationID)" + (String(show_cloudThumbMsg.prefix(5)) + String(dataPresentationStr)) + "\(code)" + (app_focusMessage.capitalized + String(kSlimUrl.prefix(5))) + "\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.everyDate(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.everyDate(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension AssistantV2Listener: V2TIMSDKListener {
extension AssistantV2Listener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(const_createPicPath) + const_universalValue))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (constPerformValue + const_warnRowText + String(app_progressPath)))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        CountegrityThen.exterminate(msg: (String(data_extensiveContent.prefix(4)) + "\u{670d}务器失败" + String(appBillButtonSquareUrl.suffix(6))) + "\(code)" + (String(app_exclusiveStr)) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        CountegrityThen.exterminate(msg: (String(constListenTitle)))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        DisagreeableWomanSocketManager.shared.utilizerFeature(userLoginOut: true)
        //: AssistantV2Listener.shared.isConnection = false
        AssistantV2Listener.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (PhallicApplication.shared as! PhallicApplication).arrow()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: showUniversalTitle.map{goalSlim(rating: $0)}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = NumberegrityAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if ProjectionThen.share.interfaceLang == ExtensiveLangType.ar.rawValue || ProjectionThen.share.interfaceLang == ExtensiveLangType.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            ProjectionThen.share.interfaceLang == ExtensiveLangType.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.wordPicture(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        StandMarketThen.labelConfig()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        StandMarketThen.labelConfig(title: (String(userLatName.suffix(5)) + "ne No" + noti_compelWeId.replacingOccurrences(of: "core", with: "ti") + "tion").localized, message: msg, leftBtnTitle: (String(show_normalKey) + userCurtainId.replacingOccurrences(of: "agree", with: "l")).localized, rightBtnTitle: (String(notiIonPath.prefix(8))).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            AlwaysDiskPushListener.aInmate().medietyResource()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            SpeakRequestTool.change { t in
                //: ProgressHUD.dismiss()
                LovelyLifetimeProgressHUD.arcCurDismiss()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: const_captureId, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            SpeakRequestTool.map { t in
                //: if t {
                if t {
                    //: AssistantV2Listener.shared.func__LogingIn { _ in
                    AssistantV2Listener.shared.deadline { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if AlwaysDiskPushListener.aInmate().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(AlwaysDiskPushListener.aInmate().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    DisagreeableWomanSocketManager.shared.layerSocial()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
