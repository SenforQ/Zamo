
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let data_airId:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "F5F5F5" :*/
fileprivate let k_automaticPath:[Character] = ["F","5","F","5","F","5"]

/*: "#128CFF" :*/
fileprivate let showLayerValue:String = "cite resume#128CFF"

/*: "Feedback" :*/
fileprivate let dataGenderMessage:String = "when source thumbFe"
fileprivate let data_stickName:String = "etoleranceack"

/*: "#8370FF" :*/
fileprivate let dataCornerId:String = "#8370FFedit meet able"

/*: "Quick Greeting" :*/
fileprivate let kSendMessage:String = "do lengthQuic"
fileprivate let kExpectedWhiteFormat:[Character] = ["t","i","n","g"]

/*: "Beauty Photos" :*/
fileprivate let mainRemainTaskMsg:[Character] = ["B","e","a","u","t","y"," "]
fileprivate let main_toolValue:String = "origin exPhotos"

/*: "url" :*/
fileprivate let mainMoreMsg:String = "urhouse"

/*: "unlockGift" :*/
fileprivate let main_thanksMessage:String = "uexplain"
fileprivate let mainCurrencyId:String = "type centralockGift"

/*: "isBurn" :*/
fileprivate let main_illegalCarrierValue:[UInt8] = [0x88,0x92,0xa3,0x94,0x93,0x8f]

private func employLaunch(system num: UInt8) -> UInt8 {
    return num ^ 225
}

/*: "giftId" :*/
fileprivate let data_readyPencilId:[Character] = ["g","i","f","t","I","d"]

/*: "rYMsgType" :*/
fileprivate let userResMsg:String = "rYMsgTypround tension"
fileprivate let mainRewardUrl:String = "previous"

/*: "GJ:CallCustom" :*/
fileprivate let dataTreatMessage:[Character] = ["G","J",":","C","a","l","l","C","u","s","t","o","m"]

/*: "changeDirection" :*/
fileprivate let const_noseName:String = "CHANG"
fileprivate let const_leadingData:[Character] = ["t","i","o","n"]

/*: "extra" :*/
fileprivate let dataCapacityName:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let main_profitTitle:[Character] = ["m","s","g","I","n"]
fileprivate let noti_conversationMsg:[Character] = ["f","o"]

/*: "toUid" :*/
fileprivate let showDomesticValue:[Character] = ["t","o","U","i","d"]

/*: "wantGift" :*/
fileprivate let app_searchName:[Character] = ["w","a","n","t","G","i","f","t"]

/*: "jumpKey" :*/
fileprivate let constSpyUrl:[Character] = ["j","u","m","p","K","e","y"]

/*: "truePersonAuth" :*/
fileprivate let data_momentSafetyStr:[Character] = ["t","r","u","e","P","e","r","s"]
fileprivate let constDownCountervalStr:[Character] = ["o"]
fileprivate let const_exploreData:[Character] = ["n","A","u","t","h"]

/*: "logType" :*/
fileprivate let app_windowQueryData:[Character] = ["l","o","g","T","y","p","e"]

/*: "msgType" :*/
fileprivate let appStretchContainFormat:[UInt8] = [0xf3,0xed,0xf9,0xca,0xe7,0xee,0xfb]

private func videoFirm(mic num: UInt8) -> UInt8 {
    return num ^ 158
}

/*: "txt" :*/
fileprivate let data_exploreStickFirstPath:String = "offt"

/*: "type" :*/
fileprivate let kSenseData:String = "tclearlye"

/*: "QuickMsg" :*/
fileprivate let noti_legacyFormat:String = "QuickMsunable comprehensive"
fileprivate let k_suspendName:String = "thin"

/*: "params" :*/
fileprivate let userBoardFormat:[UInt8] = [0x65,0x74,0x67,0x74,0x78,0x66]

private func informationTechnology(ride num: UInt8) -> UInt8 {
    return num ^ 21
}

/*: "content" :*/
fileprivate let data_meetConductMessage:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "txtId" :*/
fileprivate let k_domesticMsg:[Character] = ["t","x","t","I","d"]

/*: "message" :*/
fileprivate let userPencilName:[UInt8] = [0x65,0x67,0x61,0x73,0x73,0x65,0x6d]

/*: "TxtMsg" :*/
fileprivate let noti_costUrl:String = "finite fastTxtMsg"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let user_insertFormat:[UInt8] = [0xba,0xd3,0xcd,0xc7,0xc9,0x84,0xcc,0xc9,0xd0,0xd0,0xd3,0x84,0xc8,0xd3,0xdb,0xd2,0xd0,0xd3,0xc5,0xc8,0x84,0xca,0xc5,0xcd,0xd0,0xc9,0xc8,0x92,0x84,0xb4,0xd6,0xd3,0xc7,0xc9,0xc8,0xd9,0xd6,0xc9]

fileprivate func spreadCreature(design num: UInt8) -> UInt8 {
    let value = Int(num) + 156
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "length" :*/
fileprivate let mainShrinkElectHugePath:[Character] = ["l","e","n","g","t","h"]

/*: "audio" :*/
fileprivate let const_sweetUrl:String = "audbear"

/*: "audioId" :*/
fileprivate let notiAwayPath:String = "clue big processingaudioId"

/*: "audioLength" :*/
fileprivate let userMerelyMsg:[UInt8] = [0xb8,0xcc,0xbb,0xc0,0xc6,0xa3,0xbc,0xc5,0xbe,0xcb,0xbf]

fileprivate func aspectActivity(sit num: UInt8) -> UInt8 {
    let value = Int(num) + 169
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "audioSandbox" :*/
fileprivate let const_wallUrl:String = "audioSanprice greet class cap"
fileprivate let noti_backStr:[Character] = ["d","b","o","x"]

/*: "senderId" :*/
fileprivate let constSituationData:[UInt8] = [0x2b,0x3d,0x36,0x3c,0x3d,0x2a,0x11,0x3c]

/*: "AudioMsg" :*/
fileprivate let constScopeData:String = "zz"
fileprivate let const_particleStr:[Character] = ["u","d","i","o","M","s","g"]

/*: "Can't send blank message" :*/
fileprivate let kReasonName:String = "Can\'t"
fileprivate let main_canPath:String = "mp"
fileprivate let appInjuryValue:String = "ank meyes bean design"

/*: "quoteMsgId" :*/
fileprivate let notiPrimaryPath:String = "quoexamine"

/*: "quoteMsgInfo" :*/
fileprivate let main_lessTitle:[Character] = ["q","u","o","t","e","M","s","g","I","n","f","o"]

/*: "contentType" :*/
fileprivate let mainServerValue:[UInt8] = [0xc3,0xcf,0xce,0xd4,0xc5,0xce,0xd4,0xf4,0xd9,0xd0,0xc5]

private func cellIt(movie num: UInt8) -> UInt8 {
    return num ^ 160
}

/*: "ImgMsg" :*/
fileprivate let mainReplacementMessage:String = "ImgMsgtexture sound slight"

/*: "img" :*/
fileprivate let app_playMatchParentMsg:String = "immp"

/*: "imageUri" :*/
fileprivate let userSeeName:[UInt8] = [0xab,0xaf,0xa3,0xa5,0xa7,0x97,0xb0,0xab]

private func mistakeTar(announcement num: UInt8) -> UInt8 {
    return num ^ 194
}

/*: "customSend" :*/
fileprivate let notiCanvasKey:[UInt8] = [0x4,0x12,0x14,0x13,0x8,0xa,0x34,0x2,0x9,0x3]

private func stayApproval(team num: UInt8) -> UInt8 {
    return num ^ 103
}

/*: "pic" :*/
fileprivate let userProvidePath:String = "plessc"

/*: "VideoMsg" :*/
fileprivate let k_clearlyOutletText:String = "preserve restriction yet finiteVide"

/*: "video" :*/
fileprivate let notiAppearanceValue:String = "videflexible"

/*: "coverImg" :*/
fileprivate let kActText:[UInt8] = [0x30,0x3c,0x43,0x32,0x3f,0x16,0x3a,0x34]

fileprivate func worldwideWord(gravity num: UInt8) -> UInt8 {
    let value = Int(num) - 205
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "videoUrl" :*/
fileprivate let notiArtifactData:[UInt8] = [0x3,0xf6,0xf1,0xf2,0xfc,0xe2,0xff,0xf9]

fileprivate func goalHuge(house num: UInt8) -> UInt8 {
    let value = Int(num) + 115
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "duration" :*/
fileprivate let user_stockStr:[UInt8] = [0x6e,0x6f,0x69,0x74,0x61,0x72,0x75,0x64]

/*: "len" :*/
fileprivate let app_raftKey:[UInt8] = [0x6f,0x68,0x71]

fileprivate func paintTheLily(bit num: UInt8) -> UInt8 {
    let value = Int(num) - 3
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Documents/User/voice/" :*/
fileprivate let userResPullId:[Character] = ["D","o","c","u"]
fileprivate let data_pressurePresentMsg:String = "menpics"
fileprivate let kFactorKey:String = "/voice/conversation information chance act"

/*: "Documents/User/record/" :*/
fileprivate let appWithStr:String = "Documtitle permission"
fileprivate let k_psychologicalPath:String = "domestic fire justser/"
fileprivate let const_lovelyIconFormat:String = "sense gesture mannerrecord/"

/*: "msgIncome" :*/
fileprivate let noti_tailDifferenceTitle:[Character] = ["m","s","g","I","n","c","o","m","e"]

/*: "mfBean" :*/
fileprivate let k_pencilResignIndexData:[UInt8] = [0x81,0x8a,0xae,0x89,0x8d,0x82]

private func liteSort(ya num: UInt8) -> UInt8 {
    return num ^ 236
}

/*: "messageType" :*/
fileprivate let dataCigaretteKey:String = "MESSAG"

/*: "WantGift" :*/
fileprivate let user_dueName:[Character] = ["W","a","n","t","G","i","f","t"]

/*: _PrivateChatTipsTitle_ :*/
fileprivate let showLandData:[Character] = ["_","P","r","i","v","a","t","e","C","h","a","t","T","i","p","s"]
fileprivate let notiFatalValue:[Character] = ["T","i","t","l","e","_"]

/*: "msgId" :*/
fileprivate let notiProduceCostStr:String = "placement motivation business huntingmsgId"

/*: "totalMfCoin" :*/
fileprivate let main_goingMessage:[Character] = ["t","o","t","a","l","M","f","C","o","i","n"]

/*: "attachSysMsg" :*/
fileprivate let kSightId:String = "animatach"

/*: "intimatePhoto" :*/
fileprivate let userExtentFormat:String = "inclick"
fileprivate let showCreateerKey:[Character] = ["h","o","t","o"]

/*: "totalIntimate" :*/
fileprivate let main_cornerStr:String = "TOT"
fileprivate let mainTickText:String = "alIntrelated able join combined none"

/*: "uid" :*/
fileprivate let appRequestTarName:[UInt8] = [0xa9,0xb5,0xb8]

/*: "intimate" :*/
fileprivate let data_uniformMsg:[UInt8] = [0x65,0x74,0x61,0x6d,0x69,0x74,0x6e,0x69]

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let main_privacyContent:[UInt8] = [0xdb,0xf9,0x6,0xbf,0xc,0xb8,0xd,0xb,0xfd,0xb8,0xc,0x0,0xfd,0xb8,0xfe,0xd,0x6,0xfb,0xc,0x1,0x7,0x6,0xb8,0xfc,0xd,0xfd,0xb8,0xc,0x7,0xb8,0x11,0x7,0xd,0xa,0xb8,0x4,0xf9,0xfb,0x3,0xb8,0x7,0xfe,0xb8,0x1,0x6,0xc,0x1,0x5,0xf9,0xfb,0x11,0xb8,0x4,0xfd,0xe,0xfd,0x4,0xc6]

fileprivate func coordinateFire(king num: UInt8) -> UInt8 {
    let value = Int(num) + 104
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftNum" :*/
fileprivate let notiContextAbsStr:[Character] = ["g","i","f","t","N","u","m"]

/*: "pkgItemsetId" :*/
fileprivate let noti_clubPanelTitle:[Character] = ["p","k","g","I","t","e","m"]
fileprivate let main_placementData:[Character] = ["s","e","t","I","d"]

/*: "from" :*/
fileprivate let constStockText:String = "fstrengthom"

/*: "wantGiftId" :*/
fileprivate let showVeryClearNeedText:[Character] = ["w","a"]
fileprivate let appLackName:String = "ntGiftIdcondition little reference bottom following"

/*: "WantGiftMsg" :*/
fileprivate let mainRetainStr:String = "WantGweekly trigger"

/*: "gift" :*/
fileprivate let user_operateName:[Character] = ["g","i","f","t"]

/*: "View info" :*/
fileprivate let showFitStr:String = "time effect ample processorView "

/*: "Notes" :*/
fileprivate let const_executivePath:String = "Notesproduct collection one feed provide"

/*: "Report" :*/
fileprivate let showAppearStr:String = "Reporshared attention provider"
fileprivate let k_earthTitle:[Character] = ["t"]

/*: "View read receipts?" :*/
fileprivate let k_sureHourValue:String = "discount three delivery historyView "
fileprivate let const_pencilData:String = "rconnectionei"
fileprivate let show_artifactName:String = "expected public stealpts?"

/*: "She" :*/
fileprivate let notiCompareUrl:String = "a analyze commentShe"

/*: "%@ is using too low a version" :*/
fileprivate let data_tableUrl:[Character] = ["%","@"," ","i","s"," ","u","s","i","n","g"," ","t","o","o"," ","l","o","w"," ","a"," ","v","e"]
fileprivate let appMpContent:String = "rsiocoordinator"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let k_kindValue:[Character] = ["S","h","o","o","t"," ","v","i","d","e","o"," ","a","t"," ","l","e","a"]
fileprivate let dataGestureProperlyFormat:String = "st 5 version message hidden to"

/*: "Upload failed" :*/
fileprivate let k_birthValue:String = "Uploprogram cell young me ancient"
fileprivate let data_boxLaunchUrl:[Character] = ["i","l","e","d"]

/*: "播放状态: :*/
fileprivate let kHiPath:String = "播放\u{72b6}\u{6001}:"

/*: "c2c_" :*/
fileprivate let user_readStr:[Character] = ["c","2","c","_"]

/*: "msg" :*/
fileprivate let constTotaloAlterValue:String = "titleg"

/*: "tips" :*/
fileprivate let notiAreaText:String = "tloops"

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let noti_ignoreId:[UInt8] = [0x11,0x27,0x3d,0x68,0x2b,0x29,0x26,0x68,0x3d,0x3b,0x2d,0x68,0x3c,0x20,0x2d,0x68,0x3e,0x21,0x2c,0x2d,0x27,0x68,0x2b,0x20,0x29,0x3c,0x68,0x2e,0x2d,0x29,0x3c,0x3d,0x3a,0x2d,0x68,0x3c,0x27,0x68,0x2f,0x21,0x3e,0x2d,0x68,0x6d,0x8,0x68,0x29,0x68,0x2b,0x29,0x24,0x24]

private func renderCharm(hormone num: UInt8) -> UInt8 {
    return num ^ 72
}

/*: "cellData" :*/
fileprivate let appGirlFormat:String = "cellDastage scope jungle"
fileprivate let showMannerId:[Character] = ["t","a"]

/*: "System notification" :*/
fileprivate let data_firmPath:[Character] = ["S","y","s","t","e","m"," ","n","o","t","i","f","i","c","a","t"]
fileprivate let noti_accessId:String = "earthn"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AutomaticViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum PackFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class AutomaticViewController: EverlastinglyThen {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var needShowRating = false
    var needShowRating = false // 是否展示app store评分
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = PronkCacheModel()
    private var userWrap = PronkCacheModel() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = PackFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: PronkCacheModel = PronkCacheModel()) {
    init(chatID: String, isFrom: PackFromEnum = .Normal, userWrap: PronkCacheModel = PronkCacheModel()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        AutomaticViewController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        GradePointAverageThen.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == EverlastinglyReactiveCompatible.getXiaoMiID() {
        if targetId == EverlastinglyReactiveCompatible.layChild() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == EverlastinglyReactiveCompatible.getCustomerServiceID() {
        } else if targetId == EverlastinglyReactiveCompatible.breakup() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: data_airId.reversed(), encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if MentionTriggerAppManager.share.appConfigMode.disableShootScreen {
            if MentionTriggerAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = RateShieldView(frame: CGRect(x: 0, y: 0, width: show_objectValue, height: constBlockIdentityContent))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        actuality()
        //: createUI()
        forEachEnablee()
        //: createUIConstraints()
        country()
        //: bindInteraction()
        stickInMagnitudeeraction()
        //: addTapGestureRecognizer()
        whiteLate()
        //: func__requestChatInfo()
        beforeVertical()
        //: func__getFillDraftText()
        protrude()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        PhallicThen.shared.delegate()
        //: func__addVoiceNotification()
        funcGalleryLab()
        //: addReadPeceiptNotification()
        rubyNotification()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            dataCachInitEffect()
            //: func__TipsNotification()
            constraintFee()
            //: if MentionTriggerAppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if MentionTriggerAppManager.share.loginUserMode.sex == TopicKeyRepresentable.male.rawValue {
                //: func__setReplyTipsView()
                withalPing()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        text(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        text(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            recentOfTitle()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        // 展示App Store评分
        //: if needShowRating {
        if needShowRating {
            //: AppStoreRatingManager.shared.requestQuickRating()
            MarketRatingManager.shared.quick()
        }
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: ActuallyTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = ActuallyTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (String(k_automaticPath)))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(FrequencyViewCell.self, forCellReuseIdentifier: FrequencyViewCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(PubliclyViewCell.self, forCellReuseIdentifier: PubliclyViewCell.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(PinMessageCell.self, forCellReuseIdentifier: PinMessageCell.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(AccessibleViewCell.self, forCellReuseIdentifier: AccessibleViewCell.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(StillViewCell.self, forCellReuseIdentifier: StillViewCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(QuantityerMsgCell.self, forCellReuseIdentifier: QuantityerMsgCell.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(MaleView.self, forCellReuseIdentifier: MaleView.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(AliveDriveMsgCell.self, forCellReuseIdentifier: AliveDriveMsgCell.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(ToReactiveCompatible.self, forCellReuseIdentifier: ToReactiveCompatible.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: EyebrowReactiveCompatible.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(AuthorizeViewCell.self, forCellReuseIdentifier: EyebrowReactiveCompatible.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: OverObjectProtocol = {
        //: let m = TalkingCallMenuManager()
        let m = OverObjectProtocol()
        //: m.uid = targetId
        m.uid = targetId
        //: m.onReqestCallErrorBlock = { [weak self] errNo in
        m.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self,errNo == 101 else { return }
            guard let self = self, errNo == 101 else { return }
            //: self.msgInputView.setHidenFreeMsgTimesView()
            self.msgInputView.sitDownSecureView()
        }
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: SheViewDelegate = {
        //: var type: InputViewType = .PrivateChat
        var type: ReferViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = SheViewDelegate(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(showLayerValue.suffix(7))))!, .font: UIFont.landEnableSize(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(dataGenderMessage.suffix(2)) + data_stickName.replacingOccurrences(of: "tolerance", with: "db")).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(presentationClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: DisableAnimatView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = DisableAnimatView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: MotivationView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = MotivationView(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.landEnableSize(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(dataCornerId.prefix(7)))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(kSendMessage.suffix(4)) + "k Gree" + String(kExpectedWhiteFormat)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.thirdRevenue().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(exposure), for: .touchUpInside)
        //: btn.isHidden = !MentionTriggerAppManager.share.appUserConfigMode.showDirectGreet || MentionTriggerAppManager.share.loginUserMode.sex == Gender.male.rawValue || MentionTriggerAppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !MentionTriggerAppManager.share.appUserConfigMode.showDirectGreet || MentionTriggerAppManager.share.loginUserMode.sex == TopicKeyRepresentable.male.rawValue || MentionTriggerAppManager.share.appStatus == AidNameConvertible.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: HePinReactiveCompatible? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = HePinReactiveCompatible()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: BoundView = {
        //: let v = TalkingSelectTranslationView()
        let v = BoundView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.generate(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: HarvestResultView = {
        //: let v = TalkingTranslationResultView()
        let v = HarvestResultView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.stochasticProcess(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.landEnableSize(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(dataCornerId.prefix(7)))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(mainRemainTaskMsg) + String(main_toolValue.suffix(6))).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.thirdRevenue().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(betwixt), for: .touchUpInside)
        //: btn.isHidden = MentionTriggerAppManager.share.loginUserMode.sex == Gender.male.rawValue || MentionTriggerAppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = MentionTriggerAppManager.share.loginUserMode.sex == TopicKeyRepresentable.male.rawValue || MentionTriggerAppManager.share.appStatus == AidNameConvertible.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: DiskThen = {
        //: let v = TalkingLiveRoomGamesView()
        let v = DiskThen()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: HeliumBannerView = {
        //: let view = TalkingPrivateBannerView.init()
        let view = HeliumBannerView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: TipMentionView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = TipMentionView()
        //: v.isHidden = (MentionTriggerAppManager.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (MentionTriggerAppManager.share.appStatus == AidNameConvertible.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: ChatView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = ChatView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.straddle()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension AutomaticViewController: FloorCoveringBannerDelegate {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func mp(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if MentionTriggerAppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if MentionTriggerAppManager.share.loginUserMode.sex == TopicKeyRepresentable.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if MentionTriggerAppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if MentionTriggerAppManager.share.loginUserMode.sex == TopicKeyRepresentable.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.translateView(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension AutomaticViewController: TrimDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func exposure() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        GradePointAverageThen.item(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.outBarMsg(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.sourceRequest(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func betwixt() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        GradePointAverageThen.atTheSameTime(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = WouldDataSource()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().choiceRest(urlStr: dict?[(mainMoreMsg.replacingOccurrences(of: "house", with: "l"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(main_thanksMessage.replacingOccurrences(of: "explain", with: "nl") + String(mainCurrencyId.suffix(7)))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = TrimRuleModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: main_illegalCarrierValue.map{employLaunch(system: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(data_readyPencilId))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.evenSoTransfer(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TriggerCellData! {
    func toFilter(_: ActuallyTableViewController!, running data: V2TIMMessage!) -> TriggerCellData! {
        //: let dic = ManagerReactiveCompatible.parseTXMessageData(data: data.customElem.data)
        let dic = ManagerReactiveCompatible.sheaf(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = ChatManager.transferAccess(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: AriseCellData.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(userResMsg.prefix(8)) + mainRewardUrl.replacingOccurrences(of: "previous", with: "e"))].stringValue == (String(dataTreatMessage)) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(const_noseName.lowercased() + "eDirec" + String(const_leadingData))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(String(dataCapacityName))][(String(main_profitTitle) + String(noti_conversationMsg))][(String(showDomesticValue))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = ValuationThen(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = ValuationThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: WritingThen.self) == true ||
        if cellData?.isKind(of: WritingThen.self) == true ||
            //: cellData?.isKind(of: MezuzahCellData.self) == true ||
            cellData?.isKind(of: MezuzahCellData.self) == true ||
            //: cellData?.isKind(of: ValuationThen.self) == true ||
            cellData?.isKind(of: ValuationThen.self) == true ||
            //: cellData?.isKind(of: CellData.self) == true {
            cellData?.isKind(of: CellData.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            GradePointAverageThen.front(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            GradePointAverageThen.momentVertical(cellData.msgIncome, data.timestamp, msgView)
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TriggerCellData!) -> EyebrowReactiveCompatible! {
    func message(_: ActuallyTableViewController!, ingredient data: TriggerCellData!) -> EyebrowReactiveCompatible! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = self.count(data: data)

        //: if data.isKind(of: ValuationThen.self) {
        if data.isKind(of: ValuationThen.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = FrequencyViewCell(style: .default, reuseIdentifier: FrequencyViewCell.className())
            //: let cellData = data as! ValuationThen
            let cellData = data as! ValuationThen

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.file() {
                //: if cellData.msgModel.quoteMsgInfo.uid == MentionTriggerAppManager.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == MentionTriggerAppManager.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = MentionTriggerAppManager.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = MentionTriggerAppManager.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.disembarrass(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: MezuzahCellData.self) {
        if data.isKind(of: MezuzahCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = PubliclyViewCell(style: .default, reuseIdentifier: PubliclyViewCell.className())
            //: let cellData = data as! MezuzahCellData
            let cellData = data as! MezuzahCellData
            //: cell.fill(with: cellData)
            cell.disembarrass(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: CellData.self) {
        if data.isKind(of: CellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = PinMessageCell(style: .default, reuseIdentifier: PinMessageCell.className())
            //: let cellData = data as! CellData
            let cellData = data as! CellData
            //: cell.fill(with: cellData)
            cell.disembarrass(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: CounterReactiveCompatible.self) {
        if data.isKind(of: CounterReactiveCompatible.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = AccessibleViewCell(style: .default, reuseIdentifier: AccessibleViewCell.className())
            //: let cellData = data as! CounterReactiveCompatible
            let cellData = data as! CounterReactiveCompatible
            //: cell.fill(with: cellData)
            cell.disembarrass(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: OutsideCellData.self) {
        if data.isKind(of: OutsideCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = StillViewCell(style: .default, reuseIdentifier: StillViewCell.className())
            //: let cellData = data as! OutsideCellData
            let cellData = data as! OutsideCellData
            //: cell.fill(with: cellData)
            cell.disembarrass(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = TipTransformable()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.sinkStr(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(app_searchName)))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: WritingThen.self) {
        if data.isKind(of: WritingThen.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = AliveDriveMsgCell(style: .default, reuseIdentifier: AliveDriveMsgCell.className())
            //: let cellData = data as! WritingThen
            let cellData = data as! WritingThen
            //: cell.fill(with: cellData)
            cell.disembarrass(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: AriseCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = MaleView(style: .default, reuseIdentifier: MaleView.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! AriseCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.disembarrass(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: LovelyCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = QuantityerMsgCell(style: .default, reuseIdentifier: QuantityerMsgCell.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! LovelyCellData
            //: cell.fill(with: cellData)
            cell.disembarrass(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: ToothCellData.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = ToReactiveCompatible(style: .default, reuseIdentifier: ToReactiveCompatible.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! ToothCellData
            //: cell.fill(with: cellData)
            cell.disembarrass(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: EyebrowReactiveCompatible!) {
    func step(_: ActuallyTableViewController!, indicator cell: EyebrowReactiveCompatible!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != EverlastinglyReactiveCompatible.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != EverlastinglyReactiveCompatible.breakup() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: TalkingAppPushManager.share.func__pushToUserDetailVC(uid: String(uid))
            RiseMalePushManager.share.nudge(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: EyebrowReactiveCompatible!) {
    func select(_: ActuallyTableViewController!, notice cell: EyebrowReactiveCompatible!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: PubliclyViewCell.self) { // 图片
            //: let imageCellData = cell.messageData as! MezuzahCellData
            let imageCellData = cell.messageData as! MezuzahCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = LandPreviewVc(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.writer(imageCellData, present: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.deal(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: show_intervalMessage)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.strengthDetect(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.metadata(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                rootController()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(kHalfTurnDomainMsg)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = PhotoMotivationMeasurable()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = DigitiserMomentModel()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [PhotoMotivationMeasurable] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = AliveViewDelegate(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                rootController()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: PinMessageCell.self) { // 视频
            //: let videoCellData = cell.messageData as! CellData
            let videoCellData = cell.messageData as! CellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = TrimPlayerDelegate(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                rootController()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = DriveBoundPlayerDelegate(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.telecastingModel(videoCellData, conversation: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.deal(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: show_intervalMessage)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.strengthDetect(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.metadata(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                rootController()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: AliveDriveMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! AliveDriveMsgCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! WritingThen, cellView: audioCell)
            asInsert(cellData: audioCell.cellData as! WritingThen, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: FrequencyViewCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! FrequencyViewCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = ManagerReactiveCompatible.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = ManagerReactiveCompatible.sheaf(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(dataCapacityName))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(constSpyUrl))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(constSpyUrl))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(data_momentSafetyStr) + String(constDownCountervalStr) + String(const_exploreData)) {
                    //: TalkingAppPushManager.share.func__pushUserVerifyController(toast: nil)
                    RiseMalePushManager.share.turnDownRowToast(toast: nil)
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<SweatshopMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TalkingAppPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    RiseMalePushManager.share.counterest(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(userResMsg.prefix(8)) + mainRewardUrl.replacingOccurrences(of: "previous", with: "e"))] as? String == (String(dataTreatMessage)) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(main_profitTitle) + String(noti_conversationMsg))][(String(app_windowQueryData))].intValue == 1 {
                    //: checkAndCallVoice()
                    mentum()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    studyStarTvReading()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: QuantityerMsgCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! QuantityerMsgCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = ManagerReactiveCompatible.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = ManagerReactiveCompatible.sheaf(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(dataCapacityName))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(constSpyUrl))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<SweatshopMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TalkingAppPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    RiseMalePushManager.share.counterest(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: MaleView.self) {
            //: TalkingAppPushManager.share.func__pushToUserDetailVC(uid: targetId)
            RiseMalePushManager.share.nudge(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: EyebrowReactiveCompatible!) {
    func view(_: ActuallyTableViewController!, direction cell: EyebrowReactiveCompatible!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: SumroductionCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: PubliclyViewCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! PubliclyViewCell
            //: cellData = imageCell.cellData as! MezuzahCellData
            cellData = imageCell.cellData as! MezuzahCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: PinMessageCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! PinMessageCell
            //: cellData = videoCell.cellData as! CellData
            cellData = videoCell.cellData as! CellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: AliveDriveMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! AliveDriveMsgCell
            //: cellData = audioCell.cellData as! WritingThen
            cellData = audioCell.cellData as! WritingThen
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: FrequencyViewCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! FrequencyViewCell
            //: cellData = textCell.cellData as! ValuationThen
            cellData = textCell.cellData as! ValuationThen
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: TriggerCellData? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: TriggerCellData? = ChatManager.prepareWho(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: CellData.self) {
                if reCellData!.isKind(of: CellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    press(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    whoAgree(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: TriggerCellData!) {
    func mouth(_: ActuallyTableViewController!, link cellData: TriggerCellData!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = GradePointAverageThen.each(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: TriggerCellData!) {
    func array(_: ActuallyTableViewController!, kind cellData: TriggerCellData!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        GradePointAverageThen.clickId(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: TriggerCellData) -> TriggerCellData {
    private func count(data: TriggerCellData) -> TriggerCellData {
        //: let isSelf = data.msgModel.user.uid == Int(MentionTriggerAppManager.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(MentionTriggerAppManager.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if MentionTriggerAppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if MentionTriggerAppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = MentionTriggerAppManager.share.loginUserMode.headPic!
                data.msgModel.user.headPic = MentionTriggerAppManager.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = MentionTriggerAppManager.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = MentionTriggerAppManager.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension AutomaticViewController {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func suiteMsg() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: appStretchContainFormat.map{videoFirm(mic: $0)}, encoding: .utf8)!: (data_exploreStickFirstPath.replacingOccurrences(of: "off", with: "tx"))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(kSenseData.replacingOccurrences(of: "clearly", with: "yp")): (String(noti_legacyFormat.prefix(7)) + k_suspendName.replacingOccurrences(of: "thin", with: "g"))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(main_profitTitle) + String(noti_conversationMsg)): msgInfo, String(bytes: userBoardFormat.map{informationTechnology(ride: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChatManager.selectionTip(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.whoAgree(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func sourceRequest(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: data_meetConductMessage.reversed(), encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: data_meetConductMessage.reversed(), encoding: .utf8)!: content, (String(k_domesticMsg)): greetId, String(bytes: appStretchContainFormat.map{videoFirm(mic: $0)}, encoding: .utf8)!: (data_exploreStickFirstPath.replacingOccurrences(of: "off", with: "tx"))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: userPencilName.reversed(), encoding: .utf8)!: content, (String(k_domesticMsg)): greetId, (kSenseData.replacingOccurrences(of: "clearly", with: "yp")): (String(noti_costUrl.suffix(6)))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(main_profitTitle) + String(noti_conversationMsg)): msgInfo, String(bytes: userBoardFormat.map{informationTechnology(ride: $0)}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = ChatManager.selectionTip(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.whoAgree(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            AppropriateDataDelegate.shared.phoneAudio(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.outBarMsg(showMsg: String(bytes: user_insertFormat.map{spreadCreature(design: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        GradePointAverageThen.sufficientHave(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(String(mainShrinkElectHugePath))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: appStretchContainFormat.map{videoFirm(mic: $0)}, encoding: .utf8)!: (const_sweetUrl.replacingOccurrences(of: "bear", with: "io")),
                               //: "audioId": greetId,
                               (String(notiAwayPath.suffix(7))): greetId,
                               //: "audioLength": length,
                               String(bytes: userMerelyMsg.map{aspectActivity(sit: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(const_wallUrl.prefix(8)) + String(noti_backStr)): model.unZipFilePath,
                               //: "senderId": MentionTriggerAppManager.share.loginUserMode.userID]
                               String(bytes: constSituationData.map{$0^88}, encoding: .utf8)!: MentionTriggerAppManager.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(notiAwayPath.suffix(7))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: userMerelyMsg.map{aspectActivity(sit: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(const_wallUrl.prefix(8)) + String(noti_backStr)): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (kSenseData.replacingOccurrences(of: "clearly", with: "yp")): (constScopeData.replacingOccurrences(of: "zz", with: "A") + String(const_particleStr))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(main_profitTitle) + String(noti_conversationMsg)): msgInfo, String(bytes: userBoardFormat.map{informationTechnology(ride: $0)}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = ChatManager.selectionTip(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.whoAgree(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func stochasticProcess(msgStr: String, quoteModel: CompareModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            outBarMsg(showMsg: (kReasonName + " send b" + main_canPath.replacingOccurrences(of: "mp", with: "l") + String(appInjuryValue.prefix(6)) + "ssage").localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: data_meetConductMessage.reversed(), encoding: .utf8)!: temStr, String(bytes: appStretchContainFormat.map{videoFirm(mic: $0)}, encoding: .utf8)!: (data_exploreStickFirstPath.replacingOccurrences(of: "off", with: "tx"))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: userPencilName.reversed(), encoding: .utf8)!: temStr, (kSenseData.replacingOccurrences(of: "clearly", with: "yp")): (String(noti_costUrl.suffix(6)))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(main_profitTitle) + String(noti_conversationMsg)): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(notiPrimaryPath.replacingOccurrences(of: "examine", with: "t") + "eMsgId")] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(main_lessTitle))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: userBoardFormat.map{informationTechnology(ride: $0)}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChatManager.selectionTip(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? CompareModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            whoAgree(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func evenSoTransfer(img: UIImage, sendModel: TrimRuleModel) {
        //: let imagePath = MezuzahCellData.getChatImagePath(image: img)
        let imagePath = MezuzahCellData.red(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: mainServerValue.map{cellIt(movie: $0)}, encoding: .utf8)!: (String(mainReplacementMessage.prefix(6))), String(bytes: appStretchContainFormat.map{videoFirm(mic: $0)}, encoding: .utf8)!: (app_playMatchParentMsg.replacingOccurrences(of: "mp", with: "g")), String(bytes: userSeeName.map{mistakeTar(announcement: $0)}, encoding: .utf8)!: imagePath, String(bytes: notiCanvasKey.map{stayApproval(team: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(userProvidePath.replacingOccurrences(of: "less", with: "i")): imagePath,
                                        //: "type": "ImgMsg",
                                        (kSenseData.replacingOccurrences(of: "clearly", with: "yp")): (String(mainReplacementMessage.prefix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: main_illegalCarrierValue.map{employLaunch(system: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(data_readyPencilId)): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(main_profitTitle) + String(noti_conversationMsg)): msgInfo, String(bytes: userBoardFormat.map{informationTechnology(ride: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChatManager.selectionTip(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            whoAgree(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: TriggerCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func press(msgData: TriggerCellData?, sendModel: TrimRuleModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: mainServerValue.map{cellIt(movie: $0)}, encoding: .utf8)!: (String(k_clearlyOutletText.suffix(4)) + "oMsg"), String(bytes: appStretchContainFormat.map{videoFirm(mic: $0)}, encoding: .utf8)!: (notiAppearanceValue.replacingOccurrences(of: "flexible", with: "o")), String(bytes: notiCanvasKey.map{stayApproval(team: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(kSenseData.replacingOccurrences(of: "clearly", with: "yp")): (String(k_clearlyOutletText.suffix(4)) + "oMsg"),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: kActText.map{worldwideWord(gravity: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: notiArtifactData.map{goalHuge(house: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: user_stockStr.reversed(), encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: main_illegalCarrierValue.map{employLaunch(system: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(data_readyPencilId)): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(main_profitTitle) + String(noti_conversationMsg)): msgInfo, String(bytes: userBoardFormat.map{informationTechnology(ride: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = ChatManager.selectionTip(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.can(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.zone(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.vivid(cellData, status: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.zone(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.point(dict, spoil: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        whoAgree(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func clubTime(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: appStretchContainFormat.map{videoFirm(mic: $0)}, encoding: .utf8)!: (const_sweetUrl.replacingOccurrences(of: "bear", with: "io")),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: userMerelyMsg.map{aspectActivity(sit: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(const_wallUrl.prefix(8)) + String(noti_backStr)): voicePath,
                       //: "senderId": MentionTriggerAppManager.share.loginUserMode.userID]
                       String(bytes: constSituationData.map{$0^88}, encoding: .utf8)!: MentionTriggerAppManager.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(const_wallUrl.prefix(8)) + String(noti_backStr)): voicePath,
                         //: "type": "AudioMsg",
                         (kSenseData.replacingOccurrences(of: "clearly", with: "yp")): (constScopeData.replacingOccurrences(of: "zz", with: "A") + String(const_particleStr)),
                         //: "len": "\(voiceTime)"]
                         String(bytes: app_raftKey.map{paintTheLily(bit: $0)}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(main_profitTitle) + String(noti_conversationMsg)): msgInfo, String(bytes: userBoardFormat.map{informationTechnology(ride: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChatManager.selectionTip(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            whoAgree(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TriggerCellData, reSend: Bool) {
    private func whoAgree(msgData: TriggerCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            outBarMsg(showMsg: showLicenseOutputName)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(showDomesticValue))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: CellData.self) == false && msgData.isKind(of: OutsideCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: CellData.self) == false, msgData.isKind(of: OutsideCellData.self) == false, params[(kSenseData.replacingOccurrences(of: "clearly", with: "yp"))] as? String != (String(noti_legacyFormat.prefix(7)) + k_suspendName.replacingOccurrences(of: "thin", with: "g")) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.can(msgData)
        }
        //: let recordTime = ManagerReactiveCompatible.getMessageInsertTime()
        let recordTime = ManagerReactiveCompatible.cityPendingLevel()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((userProvidePath.replacingOccurrences(of: "less", with: "i"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(userProvidePath.replacingOccurrences(of: "less", with: "i"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(userProvidePath.replacingOccurrences(of: "less", with: "i"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(kSenseData.replacingOccurrences(of: "clearly", with: "yp"))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (constScopeData.replacingOccurrences(of: "zz", with: "A") + String(const_particleStr)) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(const_wallUrl.prefix(8)) + String(noti_backStr))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(const_wallUrl.prefix(8)) + String(noti_backStr))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(const_wallUrl.prefix(8)) + String(noti_backStr)))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(userResPullId) + data_pressurePresentMsg.replacingOccurrences(of: "pic", with: "t") + "/User" + String(kFactorKey.prefix(7)))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = noti_modeMsg + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(appWithStr.prefix(5)) + "ents/U" + String(k_psychologicalPath.suffix(4)) + String(const_lovelyIconFormat.suffix(7)))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = notiPackageName + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(notiAwayPath.suffix(7)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(const_sweetUrl.replacingOccurrences(of: "bear", with: "io"))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        GoUpReactiveCompatible.equal(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(noti_legacyFormat.prefix(7)) + k_suspendName.replacingOccurrences(of: "thin", with: "g")) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.disposition(msgData, gift_strong: nil, accountContainer: NSNumber(value: 0), make: .Msg_Status_Fail)
                                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.deal(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: mainFoundSoundHeightUrl)
                
                
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(k_domesticMsg))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    GradePointAverageThen.sufficientHave(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.still(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(main_profitTitle) + String(noti_conversationMsg))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(noti_tailDifferenceTitle))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(noti_tailDifferenceTitle))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if AlwaysDiskPushListener.aInmate().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: k_plainBeautyPath, object: nil, userInfo: [String(bytes: k_pencilResignIndexData.map{liteSort(ya: $0)}, encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.hotelkeeper(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(dataCigaretteKey.lowercased() + "eType")] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(noti_legacyFormat.prefix(7)) + k_suspendName.replacingOccurrences(of: "thin", with: "g")) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: appStretchContainFormat.map{videoFirm(mic: $0)}, encoding: .utf8)!] = (String(user_dueName))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    const_failureData.comprehensiveStr(eventID: const_errorValue)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(noti_legacyFormat.prefix(7)) + k_suspendName.replacingOccurrences(of: "thin", with: "g")) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: appStretchContainFormat.map{videoFirm(mic: $0)}, encoding: .utf8)!] = (data_exploreStickFirstPath.replacingOccurrences(of: "off", with: "tx"))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(main_profitTitle) + String(noti_conversationMsg)): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = ChatManager.selectionTip(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.can(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(MentionTriggerAppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(MentionTriggerAppManager.share.loginUserMode.userID)" + (String(showLandData) + String(notiFatalValue)) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if MentionTriggerAppManager.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if MentionTriggerAppManager.share.loginUserMode.sex == TopicKeyRepresentable.male.rawValue, type != (String(noti_legacyFormat.prefix(7)) + k_suspendName.replacingOccurrences(of: "thin", with: "g")) {
                //: self.removeReplyTipsView()
                self.head()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.disposition(updateMsgData, gift_strong: msgInfo[(String(notiProduceCostStr.suffix(5)))] as? String, accountContainer: msgIncome, make: .Msg_Status_Succ)

            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            GradePointAverageThen.momentVertical(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(main_goingMessage))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(main_goingMessage))] as! NSNumber
                //: MentionTriggerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                MentionTriggerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((kSightId.replacingOccurrences(of: "anima", with: "at") + "SysMsg")) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(kSightId.replacingOccurrences(of: "anima", with: "at") + "SysMsg")] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.scoreTitleResign(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((userExtentFormat.replacingOccurrences(of: "click", with: "ti") + "mateP" + String(showCreateerKey))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(userExtentFormat.replacingOccurrences(of: "click", with: "ti") + "mateP" + String(showCreateerKey))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? ManyModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.writer(updateMsgData, present: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((notiAppearanceValue.replacingOccurrences(of: "flexible", with: "o"))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(notiAppearanceValue.replacingOccurrences(of: "flexible", with: "o"))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? ChatEveryoneModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.telecastingModel(updateMsgData, conversation: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        loadDown()

        //: updateSession()
        accessLaunch()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func accessLaunch() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        BoundPackInfoManager.thoroughfareCagy(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func deal(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard MentionTriggerAppManager.share.loginUserMode.status != 1 else {
            guard MentionTriggerAppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    outBarMsg(showMsg: errorStr)
                }
                //: return
                return
            }

            //: TalkingAppPushManager.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            RiseMalePushManager.share.primed(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.merge()
            //: MentionTriggerAppManager.share.loginUserMode.freeMsgTimes = ""
            MentionTriggerAppManager.share.loginUserMode.freeMsgTimes = ""
            //: msgInputView.setHidenFreeMsgTimesView()
            msgInputView.sitDownSecureView()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = CaptureView(frame: CGRect(x: 0, y: 0, width: show_objectValue, height: constBlockIdentityContent))
            //: view.show()
            view.drippingShow()
            //: } else if errorCode == 60003, !MentionTriggerAppManager.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !MentionTriggerAppManager.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            PosternWindowManager.shared.betweenScope()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                outBarMsg(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension AutomaticViewController {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func metadata(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(main_profitTitle) + String(noti_conversationMsg))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.hotelkeeper(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func doing() {
        //: if self.userWrap.intimate >= MentionTriggerAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= MentionTriggerAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.scoreMinimum(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(MentionTriggerAppManager.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(MentionTriggerAppManager.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (showProduceLastTitle.array(forKey: main_statusChangeValue)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.linemenHair()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                showProduceLastTitle.set(intimateCacheData, forKey: main_statusChangeValue)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func numberimate(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = ManagerReactiveCompatible.parseTXMessageData(data: data.customElem.data)
        let dic = ManagerReactiveCompatible.sheaf(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(String(dataCapacityName))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(main_profitTitle) + String(noti_conversationMsg))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(main_cornerStr.lowercased() + String(mainTickText.prefix(5)) + "imate")]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.arc(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func hotelkeeper(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((main_cornerStr.lowercased() + String(mainTickText.prefix(5)) + "imate")), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(main_cornerStr.lowercased() + String(mainTickText.prefix(5)) + "imate")] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.arc(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func arc(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = BoundPackInfoManager.terms(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= MentionTriggerAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= MentionTriggerAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.scoreMinimum(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.playConvert(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.curTime(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.curTime(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        BoundPackInfoManager.cache(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: appRequestTarName.map{$0^220}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: data_uniformMsg.reversed(), encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: showAssetName,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension AutomaticViewController: HeViewDelegate {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func remark() {
        //: guard MentionTriggerAppManager.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard MentionTriggerAppManager.share.loginUserMode.sex == TopicKeyRepresentable.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.sussOutGiftLanguage(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func remove(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func civility(actionType: ApplicationPropertyProtocol) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= MentionTriggerAppManager.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= MentionTriggerAppManager.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    workMap()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    closeCount()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                workMap()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if MentionTriggerAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if MentionTriggerAppManager.share.loginUserMode.sex == TopicKeyRepresentable.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if MentionTriggerAppManager.share.loginUserMode.loungePlus {
                if MentionTriggerAppManager.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= MentionTriggerAppManager.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= MentionTriggerAppManager.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.valueConstraint(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                closeCount()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= MentionTriggerAppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= MentionTriggerAppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    turnDown()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    closeCount()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                turnDown()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.reportFrom(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func closeCount() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.outBarMsg(showMsg: String(bytes: main_privacyContent.map{coordinateFire(king: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func afterChat(giftModel _: TipTransformable, giftNum _: String, model _: StandCoreHandyJSON) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func financialAid(giftModel: TipTransformable, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        sinkStr(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: OutsideCellData?, fromStr: String?) {
    func sinkStr(giftModel: TipTransformable, giftNum: String, cellData: OutsideCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            outBarMsg(showMsg: showLicenseOutputName)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(showDomesticValue))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(data_readyPencilId))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(notiContextAbsStr))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(noti_clubPanelTitle) + String(main_placementData))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(constStockText.replacingOccurrences(of: "strength", with: "r"))] = fromStr
        }
        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        GoUpReactiveCompatible.routeEffectForward(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.deal(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: notiSystemContent)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.commonFraction(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.strengthDetect(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.accessLaunch()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(main_profitTitle) + String(noti_conversationMsg))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((main_cornerStr.lowercased() + String(mainTickText.prefix(5)) + "imate")), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(main_cornerStr.lowercased() + String(mainTickText.prefix(5)) + "imate")] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.arc(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.front(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                const_failureData.comprehensiveStr(eventID: k_withoutValue)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func keep(giftModel: TipTransformable, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        const_failureData.comprehensiveStr(eventID: userCleanName)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            outBarMsg(showMsg: showLicenseOutputName)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= MentionTriggerAppManager.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= MentionTriggerAppManager.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            closeCount()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(showVeryClearNeedText) + String(appLackName.prefix(8)))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(kSenseData.replacingOccurrences(of: "clearly", with: "yp"))] = (String(mainRetainStr.prefix(5)) + "iftMsg")

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: appStretchContainFormat.map{videoFirm(mic: $0)}, encoding: .utf8)!: (String(user_dueName))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(main_profitTitle) + String(noti_conversationMsg)): msgInfo, String(bytes: userBoardFormat.map{informationTechnology(ride: $0)}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChatManager.selectionTip(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            whoAgree(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func segment(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(show_postBeginMessage - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.aboveName(true)
            self.msgView.aboveName(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func launchPadAm(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.aboveName(true)
            self.msgView.aboveName(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func permission(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.permission(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                shareDismiss(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func landStar() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if PhallicThen.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            PhallicThen.shared.editor()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension AutomaticViewController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func strengthDetect(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(main_profitTitle) + String(noti_conversationMsg))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: appStretchContainFormat.map{videoFirm(mic: $0)}, encoding: .utf8)!] = (String(user_operateName))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(main_profitTitle) + String(noti_conversationMsg))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = ChatManager.selectionTip(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.can(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(noti_tailDifferenceTitle))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(noti_tailDifferenceTitle))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.disposition(cellData, gift_strong: msgInfoDic[(String(notiProduceCostStr.suffix(5)))] as? String, accountContainer: msgIncome, make: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        button(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(main_goingMessage))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(main_goingMessage))] as! NSNumber
            //: MentionTriggerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            MentionTriggerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: MentionTriggerAppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.backSchedule(needReload: false, mf_coin: MentionTriggerAppManager.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func dataCachInitEffect() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.searchion(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func button(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.wallByObject(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func land(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.discharge(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func eyeBright() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension AutomaticViewController {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func mentum() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.islamicCalendarMonth { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.accessLaunch()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func studyStarTvReading() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.magnitude { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.accessLaunch()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension AutomaticViewController {
    //: @objc private func feedbackBtnClick() {
    @objc private func presentationClick() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = RidFieldDelegate()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        rootController()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func qualify() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = WeightPopView(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.resList(cellTitleList: [(String(showFitStr.suffix(5)) + "info").localized, (String(const_executivePath.prefix(5))).localized, (String(showAppearStr.prefix(5)) + String(k_earthTitle)).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .roundChromaticColor()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: TalkingAppPushManager.share.func__pushToUserDetailVC(uid: self.targetId)
                RiseMalePushManager.share.nudge(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                straddle()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = SheIonAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.nogGo(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func straddle() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = PrivateThen()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.always(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.partyBridge(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func less() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            shareDismiss(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            rootController()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func zoneVisual(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = FlowMysteryMeasurable.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.after(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension AutomaticViewController {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func rubyNotification() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(marginNotification(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: userSaltRouteId,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(set),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: data_fileIndexFormat,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func marginNotification(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard MentionTriggerAppManager.share.loginUserMode.loungePlus == true else { return }
        guard MentionTriggerAppManager.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func set() {
        //: MentionTriggerAppManager.share.loginUserMode.loungePlus = true
        MentionTriggerAppManager.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func languageFrom() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? TriggerCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? TriggerCellData else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if GradePointAverageThen.putBack(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.frameIn(tipStr: (String(k_sureHourValue.suffix(5)) + "read " + const_pencilData.replacingOccurrences(of: "connection", with: "ec") + String(show_artifactName.suffix(4))).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension AutomaticViewController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func workMap() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        GatePermissionTool.funcWithoutSquareBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = CapturePickTool.subjectMatter(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.model(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func vid() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        GatePermissionTool.successfullyFor(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.model(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func model(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: MentionTriggerAppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
            MentionTriggerAppManager.share.appStatus == AidNameConvertible.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = TrimRuleModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.evenSoTransfer(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = AliveViewDelegate(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = TrimRuleModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.evenSoTransfer(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        rootController()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension AutomaticViewController {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func turnDown() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard GradePointAverageThen.characterizeToVersion(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == TopicKeyRepresentable.male.rawValue ? "He".localized : (String(notiCompareUrl.suffix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.outBarMsg(showMsg: (String(data_tableUrl) + appMpContent.replacingOccurrences(of: "coordinator", with: "n")).takeHomeDiskMini(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.partnerPicker()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func partnerPicker() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = CapturePickTool.subjectMatter(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            CapturePickTool.pathOff(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.goDraw(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.outBarMsg(showMsg: (String(k_kindValue) + String(dataGestureProperlyFormat.prefix(5)) + "seconds").localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func goDraw(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: DirectEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = AccessibleViewController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = TrimRuleModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.press(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        LovelyLifetimeProgressHUD.arcCurDismiss()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        rootController()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: TriggerCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func zone(msgData: TriggerCellData, sendModel: TrimRuleModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        ButSignatureProvider.shared.doMethod(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.outBarMsg(showMsg: (String(k_birthValue.prefix(4)) + "ad fa" + String(data_boxLaunchUrl)).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.disposition(msgData, gift_strong: nil, accountContainer: NSNumber(value: 0), make: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.press(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension AutomaticViewController {
    //: func func__addVoiceNotification() {
    func funcGalleryLab() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(area),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(invitePlan),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(changeGestureOne(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(listenAccept(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: show_countQualityNameTitle,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(numberimate(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: appLiveMessage,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func text(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            PhallicThen.shared.delegate()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            PhallicThen.shared.supra()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func area() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        text(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func invitePlan() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        text(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func changeGestureOne(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            text(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            text(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: WritingThen, cellView: TalkingChatAudioMsgCell) {
    func asInsert(cellData: WritingThen, cellView: AliveDriveMsgCell) {
        //: guard TalkingSocketManager.shared.accessible == false else {
        guard DisagreeableWomanSocketManager.shared.accessible == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.outBarMsg(showMsg: main_maxPath)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? TriggerCellData)!.isKind(of: WritingThen.self) == false {
            if (indexModel as? TriggerCellData)!.isKind(of: WritingThen.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! WritingThen
            let audioModel = indexModel as! WritingThen
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = BoundPackInfoManager.option(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        PhallicThen.shared.editor()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if PhallicThen.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (kHiPath) + "\(PhallicThen.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if PhallicThen.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                PhallicThen.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        PhallicThen.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        PhallicThen.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: TriggerCellData = self.msgView.uiMsgs.object(at: index) as! TriggerCellData
                let subModel: TriggerCellData = self.msgView.uiMsgs.object(at: index) as! TriggerCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: WritingThen.self) {
                if subModel.isKind(of: WritingThen.self) {
                    //: let audioModel = subModel as! WritingThen
                    let audioModel = subModel as! WritingThen
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = BoundPackInfoManager.option(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    WeightMsgTable.big(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        PhallicThen.shared.lodestar(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension AutomaticViewController {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func beforeVertical() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.heel()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.sourceRequest(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? TriggerCellData
                let indexModel = model as? TriggerCellData
                //: if indexModel?.isKind(of: WritingThen.self) == true || indexModel?.isKind(of: MezuzahCellData.self) == true || indexModel?.isKind(of: ValuationThen.self) == true || indexModel?.isKind(of: CellData.self) == true {
                if indexModel?.isKind(of: WritingThen.self) == true || indexModel?.isKind(of: MezuzahCellData.self) == true || indexModel?.isKind(of: ValuationThen.self) == true || indexModel?.isKind(of: CellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.loadDown()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.combineBelow()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.languageFrom()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func heel() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.contactMoment(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.push(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.doing()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == MentionTriggerAppManager.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == MentionTriggerAppManager.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == MentionTriggerAppManager.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == MentionTriggerAppManager.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.partyBridge(userWrap)
    }

    //: func func__getFillDraftText() {
    func protrude() {
        //: let userID = "c2c_"+targetId
        let userID = (String(user_readStr)) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.remark()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func recentOfTitle() {
        //: let userID = "c2c_"+targetId
        let userID = (String(user_readStr)) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension AutomaticViewController {
    //: func func__TipsNotification() {
    func constraintFee() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(source(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: showLengthId,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func source(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: LovelyCellData = userinfo[(constTotaloAlterValue.replacingOccurrences(of: "title", with: "ms"))] as! LovelyCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.aboveName(true)
        self.msgView.aboveName(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func frameIn(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: data_meetConductMessage.reversed(), encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: appStretchContainFormat.map{videoFirm(mic: $0)}, encoding: .utf8)!: (notiAreaText.replacingOccurrences(of: "loop", with: "ip"))]
        //: var message: TriggerCellData?
        var message: TriggerCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = ChatManager.evaluation(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = ChatManager.selectionTip(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.aboveName(true)
        self.msgView.aboveName(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func combineBelow() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        GradePointAverageThen.pastInsideHandler { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.frameIn(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func scoreTitleResign(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.frameIn(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func loadDown() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        GradePointAverageThen.accommodate(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.frameIn(tipStr: String(bytes: noti_ignoreId.map{renderCharm(hormone: $0)}, encoding: .utf8)!.takeHomeDiskMini(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func listenAccept(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: TriggerCellData = dict?["cellData"] as! TriggerCellData
        let cellData: TriggerCellData = dict?[(String(appGirlFormat.prefix(6)) + String(showMannerId))] as! TriggerCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.aboveName(true)
        self.msgView.aboveName(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension AutomaticViewController: GateTipsProtocol {
    //: func func__setReplyTipsView() {
    func withalPing() {
        //: let key =  "\(MentionTriggerAppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(MentionTriggerAppManager.share.loginUserMode.userID)" + (String(showLandData) + String(notiFatalValue)) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && MentionTriggerAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && MentionTriggerAppManager.share.appStatus == AidNameConvertible.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.before()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            mp(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            head()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func messageReplyDiskTips(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        stochasticProcess(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        head()
    }

    //: func removeReplyTipsView() {
    func head() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            mp(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension AutomaticViewController {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func actuality() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: main_cornerFormat, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: constBlockIdentityContent - show_postBeginMessage, width: show_objectValue, height: constBlockIdentityContent)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: show_objectValue,
                                     //: height: ScreenHeight)
                                     height: constBlockIdentityContent)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func shareDismiss(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.transplant() {
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: DataConverterViewController.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: constBlockIdentityContent - show_postBeginMessage,
                                     //: width: ScreenWidth,
                                     width: show_objectValue,
                                     //: height: ScreenHeight)
                                     height: constBlockIdentityContent)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension AutomaticViewController {
    /// 创建UI
    //: private func createUI() {
    private func forEachEnablee() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(data_firmPath) + noti_accessId.replacingOccurrences(of: "earth", with: "io")).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func country() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? show_postBeginMessage : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.factorSource(width: contentView.width,
                                     //: height: contentView.height,
                                     height: contentView.height,
                                     //: corners: [.topLeft, .topRight],
                                     corners: [.topLeft, .topRight],
                                     //: cornerRadii: .init(width: 8, height: 8))
                                     cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? const_matchText : appMarginFormat)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + userDiskUrl)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? const_matchText : appMarginFormat)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + userDiskUrl)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + userDiskUrl)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func stickInMagnitudeeraction() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.less()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.qualify()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.civility(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                const_failureData.comprehensiveStr(eventID: mainWorkUrl, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
