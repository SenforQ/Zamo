
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let noti_comeText:[UInt8] = [0x56,0x51,0x56,0x4b,0x17,0x5c,0x50,0x5b,0x5a,0x4d,0x5,0x16,0x1f,0x57,0x5e,0x4c,0x1f,0x51,0x50,0x4b,0x1f,0x5d,0x5a,0x5a,0x51,0x1f,0x56,0x52,0x4f,0x53,0x5a,0x52,0x5a,0x51,0x4b,0x5a,0x5b]

private func coverWithdraw(permission num: UInt8) -> UInt8 {
    return num ^ 63
}

/*: "roomId" :*/
fileprivate let kDisabledPaperTeeMessage:[Character] = ["r","o","o","m","I","d"]

/*: "type" :*/
fileprivate let appSharedId:[UInt8] = [0x1b,0x16,0x1f,0xa]

private func angleChin(cute num: UInt8) -> UInt8 {
    return num ^ 111
}

/*: "extra" :*/
fileprivate let kTenderMessage:String = "EXTRA"

/*: "jumpKey" :*/
fileprivate let app_customerKey:[UInt8] = [0x53,0x5e,0x56,0x59,0x34,0x4e,0x62]

fileprivate func titleureRequest(eastern num: UInt8) -> UInt8 {
    let value = Int(num) + 23
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Send Gift" :*/
fileprivate let dataBorderStr:[Character] = ["S","e","n","d"," ","G","i"]
fileprivate let user_sortStr:String = "administrator"

/*: "View info" :*/
fileprivate let dataToolName:[Character] = ["V","i","e","w"]
fileprivate let main_someoneValue:[Character] = [" ","i","n","f","o"]

/*: "Report" :*/
fileprivate let main_conductNumberNarrowMessage:String = "safety"
fileprivate let show_beyondId:[Character] = ["e","p","o","r","t"]

/*: "uid" :*/
fileprivate let notiRequestFormat:[UInt8] = [0x7b,0x67,0x6a]

private func nearDay(hand num: UInt8) -> UInt8 {
    return num ^ 14
}

/*: "name" :*/
fileprivate let main_whatText:[UInt8] = [0x20,0x2f,0x23,0x2b]

private func earthAllow(particle num: UInt8) -> UInt8 {
    return num ^ 78
}

/*: "Can't send blank message" :*/
fileprivate let const_shouldMsg:String = "Can\'t"
fileprivate let constPushMsg:String = "pending others dragank m"

/*: "user" :*/
fileprivate let noti_detailValue:String = "usdark"

/*: "content" :*/
fileprivate let appAssistMessage:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "msgType" :*/
fileprivate let data_kindAttractiveMsg:[Character] = ["m","s","g","T","y","p"]
fileprivate let noti_drawingText:String = "color"

/*: "txt" :*/
fileprivate let app_seaName:String = "learnt"

/*: "isGroup" :*/
fileprivate let noti_adjustText:[Character] = ["i"]
fileprivate let dataNobodyKey:String = "why invite cling orangesGroup"

/*: "jumps" :*/
fileprivate let kSatisfyQuoteKey:[Character] = ["j","u","m","p","s"]

/*: "message" :*/
fileprivate let main_totalFormat:[UInt8] = [0xee,0xe6,0xf4,0xf4,0xe2,0xe8,0xe6]

fileprivate func inundateRunning(dialog num: UInt8) -> UInt8 {
    let value = Int(num) + 127
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "TxtMsg" :*/
fileprivate let kGovernData:String = "TxtMsgcommunicate pan"

/*: "toUid" :*/
fileprivate let appItemId:String = "psychological linetoUid"

/*: "msgInfo" :*/
fileprivate let dataFarFirePath:[Character] = ["m","s"]
fileprivate let app_actorEachName:String = "offer bird operation allowgInfo"

/*: "params" :*/
fileprivate let k_applyPublishRetData:[UInt8] = [0x3,0x12,0x1,0x12,0x1e,0x0]

/*: "contentType" :*/
fileprivate let mainDifferencePath:[UInt8] = [0x8c,0x80,0x81,0x9b,0x8a,0x81,0x9b,0xbb,0x96,0x9f,0x8a]

/*: "ImgMsg" :*/
fileprivate let appReceiveMessage:String = "ImgMsgcertain re convert direct"

/*: "img" :*/
fileprivate let mainUponMsg:String = "icollectiong"

/*: "imageUri" :*/
fileprivate let notiCurName:[UInt8] = [0x4e,0x4a,0x46,0x40,0x42,0x72,0x55,0x4e]

private func finishAddition(describe num: UInt8) -> UInt8 {
    return num ^ 39
}

/*: "pic" :*/
fileprivate let constArtifactName:String = "procedureic"

/*: "msgId" :*/
fileprivate let kLaughUrl:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let data_addTitle:[Character] = ["t","o"]
fileprivate let app_feedFormat:String = "talMfCoinmax engage through motion"

/*: "giftId" :*/
fileprivate let k_havePremiumBetterData:String = "evaluation custom explore mountgiftI"
fileprivate let data_fleetKey:String = "worth"

/*: "giftNum" :*/
fileprivate let appVictoryKey:String = "giftNumneighbor adjustment"

/*: "pkgItemsetId" :*/
fileprivate let showActorName:String = "pkgItsnap priority physical turn digital"

/*: "msg" :*/
fileprivate let constColumnMsg:String = "mcompoundg"

/*: "tips" :*/
fileprivate let kOwnerFormat:[Character] = ["t","i","p","s"]

/*: "gift" :*/
fileprivate let show_okText:String = "sft"

/*: "nickname" :*/
fileprivate let data_largeUrl:String = "libraryicklibraryam"
fileprivate let user_mannerTitle:String = "arrange"

/*: "headPic" :*/
fileprivate let notiSumText:[UInt8] = [0xe2,0xef,0xeb,0xee,0xda,0xe3,0xe9]

private func remoteBlackSight(ion num: UInt8) -> UInt8 {
    return num ^ 138
}

/*: "All Numbers" :*/
fileprivate let kBuryTitle:String = "All Ndetailed conversion"
fileprivate let showPortData:String = "umnearrs"

/*: "sendNum" :*/
fileprivate let const_exposureTitle:String = "wonder comprehensive margin whethersendNu"
fileprivate let data_haveUrl:String = "difference"

/*: "toUser" :*/
fileprivate let notiDraftUrl:[Character] = ["t","o","U","s","e","r"]

/*: "msgIncome" :*/
fileprivate let notiGroupLastId:[Character] = ["m","s","g","I","n","c","o","m","e"]

/*: "isConnection" :*/
fileprivate let app_analyzeBondKey:[Character] = ["i","s","C","o","n","n","e","c","t"]
fileprivate let notiWeekPath:[Character] = ["i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StillChatController.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class StillChatController: EverlastinglyThen {
    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = SignatureMeasurable() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = AutomaticThen() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = SignatureMeasurable.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: noti_comeText.map{coverWithdraw(permission: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if MentionTriggerAppManager.share.appConfigMode.disableShootScreen {
            if MentionTriggerAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = RateShieldView(frame: CGRect(x: 0, y: 0, width: show_objectValue, height: constBlockIdentityContent))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        directMail()
        //: createUIConstraints()
        rootCap()
        //: bindInteraction()
        bag()
        //: addTapGestureRecognizer()
        whiteLate()
        //: func__chatRoom_insertTipMsg()
        operationMsg()
        //: chatSettings()
        jumpChatSettings()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            elucidate()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.rushAwayForthJoin(join: false)
        //: self.destroyTipsTimer()
        self.unlessListen()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: ActuallyTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = ActuallyTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(ChatMessageCell.self, forCellReuseIdentifier: ChatMessageCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(PubliclyViewCell.self, forCellReuseIdentifier: PubliclyViewCell.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(StraightReactiveCompatible.self, forCellReuseIdentifier: StraightReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(QuantityerMsgCell.self, forCellReuseIdentifier: QuantityerMsgCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: EyebrowReactiveCompatible.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(AuthorizeViewCell.self, forCellReuseIdentifier: EyebrowReactiveCompatible.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: SheViewDelegate = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = SheViewDelegate(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: DisableAnimatView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = DisableAnimatView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: RestrainTrackView = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = RestrainTrackView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: MotivationView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = MotivationView(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: HarvestReactiveCompatible = {
        //: let v = TalkingSomeoneAtMeView()
        let v = HarvestReactiveCompatible()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension StillChatController {
    /// 群聊设置
    //: private func chatSettings() {
    private func jumpChatSettings() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        MotivationTopicChatManager.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        rushAwayForthJoin(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func rushAwayForthJoin(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(kDisabledPaperTeeMessage)): self.conversationData.groupID, String(bytes: appSharedId.map{angleChin(cute: $0)}, encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: TalkingChatRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        GoUpReactiveCompatible.miniskirt(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - TrimDelegate

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension StillChatController: TrimDelegate {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TriggerCellData! {
    func toFilter(_: ActuallyTableViewController!, running data: V2TIMMessage!) -> TriggerCellData! {
        //: let dic = ManagerReactiveCompatible.parseTXMessageData(data: data.customElem.data)
        let dic = ManagerReactiveCompatible.sheaf(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = ChatManager.transferAccess(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TriggerCellData!) -> EyebrowReactiveCompatible! {
    func message(_: ActuallyTableViewController!, ingredient data: TriggerCellData!) -> EyebrowReactiveCompatible! {
        //: if data.isKind(of: SimplyReactiveCompatible.self) {
        if data.isKind(of: SimplyReactiveCompatible.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = ChatMessageCell(style: .default, reuseIdentifier: ChatMessageCell.className())
            //: let cellData = data as! SimplyReactiveCompatible
            let cellData = data as! SimplyReactiveCompatible
            //: cell.fill(with: cellData)
            cell.disembarrass(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.replaceIn(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.accumulationRemote(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: MezuzahCellData.self) {
        if data.isKind(of: MezuzahCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = PubliclyViewCell(style: .default, reuseIdentifier: PubliclyViewCell.className())
            //: let cellData = data as! MezuzahCellData
            let cellData = data as! MezuzahCellData
            //: cell.fill(with: cellData)
            cell.disembarrass(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: AcquisitionReactiveCompatible.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = StraightReactiveCompatible(style: .default, reuseIdentifier: StraightReactiveCompatible.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! AcquisitionReactiveCompatible
            //: cell.fill(with: cellData)
            cell.disembarrass(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: LovelyCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = QuantityerMsgCell(style: .default, reuseIdentifier: QuantityerMsgCell.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! LovelyCellData
            //: cell.fill(with: cellData)
            cell.disembarrass(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: EyebrowReactiveCompatible!) {
    func step(_: ActuallyTableViewController!, indicator cell: EyebrowReactiveCompatible!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(MentionTriggerAppManager.share.loginUid) {
            if userModel.uid == Int(MentionTriggerAppManager.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                replaceIn(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                closed(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: EyebrowReactiveCompatible!) {
    func select(_: ActuallyTableViewController!, notice cell: EyebrowReactiveCompatible!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: PubliclyViewCell.self) { // 图片
            //: let imageCellData = cell.messageData as! MezuzahCellData
            let imageCellData = cell.messageData as! MezuzahCellData
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(kHalfTurnDomainMsg)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = PhotoMotivationMeasurable()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = DigitiserMomentModel()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [PhotoMotivationMeasurable] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = AliveViewDelegate(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.transplant()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: ChatMessageCell.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! ChatMessageCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = ManagerReactiveCompatible.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = ManagerReactiveCompatible.sheaf(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(kTenderMessage.lowercased())] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: app_customerKey.map{titleureRequest(eastern: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<SweatshopMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TalkingAppPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    RiseMalePushManager.share.counterest(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: QuantityerMsgCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! QuantityerMsgCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = ManagerReactiveCompatible.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = ManagerReactiveCompatible.sheaf(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(kTenderMessage.lowercased())] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: app_customerKey.map{titleureRequest(eastern: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<SweatshopMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TalkingAppPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    RiseMalePushManager.share.counterest(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: EyebrowReactiveCompatible!) {
    func view(_: ActuallyTableViewController!, direction cell: EyebrowReactiveCompatible!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        cropDiscount(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension StillChatController {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func unlessListen() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func closed(user: EnableingModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = WeightPopView(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.resList(cellTitleList: [(String(dataBorderStr) + user_sortStr.replacingOccurrences(of: "administrator", with: "ft")).localized, "@", (String(dataToolName) + String(main_someoneValue)).localized, (main_conductNumberNarrowMessage.replacingOccurrences(of: "safety", with: "R") + String(show_beyondId)).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.recognize(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.passUpFlag(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.replaceIn(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.speedUid(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func recognize(user: EnableingModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.totaleractionMic()
        //: let model = TalkingChatRoomMemberModel()
        let model = StandCoreHandyJSON()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.meanSunTrack(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func passUpFlag(user: EnableingModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: notiRequestFormat.map{nearDay(hand: $0)}, encoding: .utf8)!: String(user.uid), String(bytes: main_whatText.map{earthAllow(particle: $0)}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.fit(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func replaceIn(uid: Int) {
        //: TalkingAppPushManager.share.func__pushToUserDetailVC(uid: String(uid))
        RiseMalePushManager.share.nudge(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func speedUid(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = SheIonAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.nogGo(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension StillChatController {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func stochasticProcess(msgStr: String, quoteModel _: CompareModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            outBarMsg(showMsg: (const_shouldMsg + " send bl" + String(constPushMsg.suffix(5)) + "essage").localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: app_customerKey.map{titleureRequest(eastern: $0)}, encoding: .utf8)!: (noti_detailValue.replacingOccurrences(of: "dark", with: "er")), String(bytes: notiRequestFormat.map{nearDay(hand: $0)}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: appAssistMessage.reversed(), encoding: .utf8)!: temStr, (String(data_kindAttractiveMsg) + noti_drawingText.replacingOccurrences(of: "color", with: "e")): (app_seaName.replacingOccurrences(of: "learn", with: "tx")), (String(noti_adjustText) + String(dataNobodyKey.suffix(6))): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(String(kSatisfyQuoteKey))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: main_totalFormat.map{inundateRunning(dialog: $0)}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: appSharedId.map{angleChin(cute: $0)}, encoding: .utf8)!: (String(kGovernData.prefix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(kDisabledPaperTeeMessage)): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(appItemId.suffix(5)))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(dataFarFirePath) + String(app_actorEachName.suffix(5))): msgInfo, String(bytes: k_applyPublishRetData.map{$0^115}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChatManager.selectionTip(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            cropDiscount(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func conImg(img: UIImage) {
        //: let imagePath = MezuzahCellData.getChatImagePath(image: img)
        let imagePath = MezuzahCellData.red(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: mainDifferencePath.map{$0^239}, encoding: .utf8)!: (String(appReceiveMessage.prefix(6))), (String(data_kindAttractiveMsg) + noti_drawingText.replacingOccurrences(of: "color", with: "e")): (mainUponMsg.replacingOccurrences(of: "collection", with: "m")), String(bytes: notiCurName.map{finishAddition(describe: $0)}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(constArtifactName.replacingOccurrences(of: "procedure", with: "p")): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: appSharedId.map{angleChin(cute: $0)}, encoding: .utf8)!: (String(appReceiveMessage.prefix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(kDisabledPaperTeeMessage)): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(dataFarFirePath) + String(app_actorEachName.suffix(5))): msgInfo, String(bytes: k_applyPublishRetData.map{$0^115}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChatManager.selectionTip(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            cropDiscount(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TriggerCellData, reSend: Bool) {
    private func cropDiscount(msgData: TriggerCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            outBarMsg(showMsg: showLicenseOutputName)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.can(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((constArtifactName.replacingOccurrences(of: "procedure", with: "p"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(constArtifactName.replacingOccurrences(of: "procedure", with: "p"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(constArtifactName.replacingOccurrences(of: "procedure", with: "p"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        GoUpReactiveCompatible.aboveExtremeCompletion(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.disposition(msgData, gift_strong: nil, accountContainer: NSNumber(value: 0), make: .Msg_Status_Fail)
                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.search(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(dataFarFirePath) + String(app_actorEachName.suffix(5)))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.disposition(msgData, gift_strong: msgInfo[(String(kLaughUrl))] as? String, accountContainer: NSNumber(value: 0), make: .Msg_Status_Succ)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(data_addTitle) + String(app_feedFormat.prefix(9)))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(data_addTitle) + String(app_feedFormat.prefix(9)))] as! NSNumber
                //: MentionTriggerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                MentionTriggerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func search(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard MentionTriggerAppManager.share.loginUserMode.status != 1 else {
            guard MentionTriggerAppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    outBarMsg(showMsg: errorStr)
                }
                //: return
                return
            }

            //: TalkingAppPushManager.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            RiseMalePushManager.share.primed(clickEvent: notiSystemContent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.merge()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            equalFee(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = CaptureView(frame: CGRect(x: 0, y: 0, width: show_objectValue, height: constBlockIdentityContent))
            //: view.show()
            view.drippingShow()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                outBarMsg(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension StillChatController: HeViewDelegate {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func keep(giftModel _: TipTransformable, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func remark() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func remove(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = SchemeRecognizerDelegate()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.fit(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func civility(actionType: ApplicationPropertyProtocol) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            half()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func financialAid(giftModel _: TipTransformable, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func afterChat(giftModel: TipTransformable, giftNum: String, model: StandCoreHandyJSON) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            outBarMsg(showMsg: showLicenseOutputName)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(appItemId.suffix(5)))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(k_havePremiumBetterData.suffix(5)) + data_fleetKey.replacingOccurrences(of: "worth", with: "d"))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(appVictoryKey.prefix(7)))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(kDisabledPaperTeeMessage))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(showActorName.prefix(5)) + "emsetId")] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        GoUpReactiveCompatible.worker(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.search(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.commonFraction(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.searchedShouldBirdSEyeView(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func segment(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.aboveName(true)
            self.msgView.aboveName(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func launchPadAm(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.aboveName(true)
            self.msgView.aboveName(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func permission(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.permission(tap)
    }

    //: func func__starRecordBtnDown() {}
    func landStar() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func clubTime(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension StillChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func half() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        GatePermissionTool.funcWithoutSquareBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = CapturePickTool.subjectMatter(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: TalkingAppPushManager.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                RiseMalePushManager.share.rf()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.conImg(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func reply() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        GatePermissionTool.successfullyFor(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.conImg(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension StillChatController {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func need(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(kDisabledPaperTeeMessage))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension StillChatController {
    //: func func__TipsNotification() {
    func plugIn() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(stayFresh(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: showLengthId,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func stayFresh(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: LovelyCellData = userinfo[(constColumnMsg.replacingOccurrences(of: "compound", with: "s"))] as! LovelyCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.aboveName(true)
        self.msgView.aboveName(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func equalFee(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: appAssistMessage.reversed(), encoding: .utf8)!: tipStr, (String(data_kindAttractiveMsg) + noti_drawingText.replacingOccurrences(of: "color", with: "e")): (String(kOwnerFormat))]
        //: var message: TriggerCellData?
        var message: TriggerCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = ChatManager.selectionTip(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.aboveName(true)
        self.msgView.aboveName(true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func operationMsg() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard MentionTriggerAppManager.share.appUserConfigMode.rTips.count > 0 else { return }
            guard MentionTriggerAppManager.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: MentionTriggerAppManager.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.equalFee(tipStr: MentionTriggerAppManager.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension StillChatController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func searchedShouldBirdSEyeView(extral: [String: Any?], model: StandCoreHandyJSON, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(dataFarFirePath) + String(app_actorEachName.suffix(5)))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(noti_adjustText) + String(dataNobodyKey.suffix(6)))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(data_kindAttractiveMsg) + noti_drawingText.replacingOccurrences(of: "color", with: "e"))] = (show_okText.replacingOccurrences(of: "s", with: "gi"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(dataFarFirePath) + String(app_actorEachName.suffix(5)))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: notiRequestFormat.map{nearDay(hand: $0)}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (data_largeUrl.replacingOccurrences(of: "library", with: "n") + user_mannerTitle.replacingOccurrences(of: "arrange", with: "e")): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: notiSumText.map{remoteBlackSight(ion: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(kBuryTitle.prefix(5)) + showPortData.replacingOccurrences(of: "near", with: "be")).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((show_okText.replacingOccurrences(of: "s", with: "gi"))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(show_okText.replacingOccurrences(of: "s", with: "gi"))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(data_largeUrl.replacingOccurrences(of: "library", with: "n") + user_mannerTitle.replacingOccurrences(of: "arrange", with: "e"))] = "\(giftdic[(String(const_exposureTitle.suffix(6)) + data_haveUrl.replacingOccurrences(of: "difference", with: "m"))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(notiDraftUrl))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = ChatManager.selectionTip(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.can(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(notiGroupLastId))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(notiGroupLastId))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.disposition(cellData, gift_strong: msgInfoDic[(String(kLaughUrl))] as? String, accountContainer: msgIncome, make: .Msg_Status_Succ)
        
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        beforeCombine(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(data_addTitle) + String(app_feedFormat.prefix(9)))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(data_addTitle) + String(app_feedFormat.prefix(9)))] as! NSNumber
            //: MentionTriggerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            MentionTriggerAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: MentionTriggerAppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.backSchedule(needReload: false, mf_coin: MentionTriggerAppManager.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.readyMembers()
    }

    //: func initCachEffectData() {
    func elucidate() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.searchion(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func beforeCombine(giftDic: [String: Any], model: StandCoreHandyJSON) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.tickFramework(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in RugReactiveCompatible.share.perInput() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [LifetimeArrModel]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.containerStream(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.wallByObject(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func medalArr(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in RugReactiveCompatible.share.perInput() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [LifetimeArrModel]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.containerStream(model: giftModel as! HomeModelType)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.discharge(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension StillChatController {
    /// 创建UI
    //: private func createUI() {
    private func directMail() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func rootCap() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(appMarginFormat)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(appMarginFormat)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - userDiskUrl)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func bag() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = SchemeRecognizerDelegate()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! TriggerCellData).isKind(of: SimplyReactiveCompatible.self) {
                    if (cellData as! TriggerCellData).isKind(of: SimplyReactiveCompatible.self) {
                        //: let groupCellData = cellData as! SimplyReactiveCompatible
                        let groupCellData = cellData as! SimplyReactiveCompatible
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(kLaughUrl))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: AssistantV2Listener.shared.rx
        AssistantV2Listener.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(app_analyzeBondKey) + String(notiWeekPath)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
