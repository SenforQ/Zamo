
//: Declare String Begin

/*: "msgType" :*/
fileprivate let k_outputFormat:[Character] = ["m","s","g","T","y","p","e"]

/*: "tips" :*/
fileprivate let showRunningShapePath:String = "TIPS"

/*: "extra" :*/
fileprivate let showBlockMessage:String = "stilltra"

/*: "msgInfo" :*/
fileprivate let appLeadingMsg:String = "round fire am nobody intomsgIn"
fileprivate let data_runMessage:[Character] = ["f","o"]

/*: "user" :*/
fileprivate let constClubFormat:[Character] = ["u","s","e","r"]

/*: "uid" :*/
fileprivate let noti_elementFormat:String = "UID"

/*: "nickname" :*/
fileprivate let show_trailValue:[UInt8] = [0x8b,0x8c,0x86,0x8e,0x8b,0x84,0x88,0x80]

private func tweedledeeAndTweedledum(hidden num: UInt8) -> UInt8 {
    return num ^ 229
}

/*: "headPic" :*/
fileprivate let showCaptureText:[UInt8] = [0x5,0x2,0xfe,0x1,0xed,0x6,0x0]

fileprivate func hormoneMid(become num: UInt8) -> UInt8 {
    let value = Int(num) - 157
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "age" :*/
fileprivate let userProvideData:[UInt8] = [0x7e,0x78,0x7a]

/*: "gift" :*/
fileprivate let kConcreteValue:String = "giftopic"

/*: "isGroup" :*/
fileprivate let user_financialMessage:String = "improve estimate restoreisGroup"

/*: "WantGift" :*/
fileprivate let notiStrategyKey:String = "WantGproduct provide user app nut"
fileprivate let constBulletEndPath:[Character] = ["i","f","t"]

/*: "txt" :*/
fileprivate let userJungleBuryPath:[Character] = ["t","x","t"]

/*: "img" :*/
fileprivate let userFollowingUrl:[Character] = ["i","m","g"]

/*: "video" :*/
fileprivate let const_exhibitName:String = "videcycle"

/*: "audio" :*/
fileprivate let noti_termsUrl:[Character] = ["a","u","d","i","o"]

/*: "messageType" :*/
fileprivate let show_capacityUrl:String = "MESSA"
fileprivate let noti_brushValue:[Character] = ["g","e","T","y","p","e"]

/*: "contentType" :*/
fileprivate let notiBroadcastMakeupValue:String = "cograyte"
fileprivate let noti_mainCostAlivePath:[Character] = ["n","t","T","y","p","e"]

/*: "ImgMsg" :*/
fileprivate let appTotalroMessage:String = "ImgMsghappy remind you"

/*: "imageUri" :*/
fileprivate let const_interruptKey:[Character] = ["i","m","a","g","e","U","r","i"]

/*: "type" :*/
fileprivate let dataBrideBehaviorUrl:String = "targetype"

/*: "pic" :*/
fileprivate let noti_headValue:[Character] = ["p","i","c"]

/*: "isBurn" :*/
fileprivate let noti_markServeData:String = "isBurnmanage condition selection single"

/*: "giftId" :*/
fileprivate let noti_militaryFormat:String = "equal center challenge stopgiftId"

/*: "params" :*/
fileprivate let showMustData:[Character] = ["p","a","r","a","m","s"]

/*: "audioLength" :*/
fileprivate let showThresholdReplaceKey:String = "A"
fileprivate let constChemistryPath:String = "uddimensiono"

/*: "len" :*/
fileprivate let mainWhetherName:String = "leestimated"

/*: "audioSandbox" :*/
fileprivate let show_containMsg:[Character] = ["a","u","d","i","o","S","a","n","d","b","o","x"]

/*: "senderId" :*/
fileprivate let app_ehMovieKey:String = "universalnde"
fileprivate let userCapablePath:[Character] = ["r","I","d"]

/*: "AudioMsg" :*/
fileprivate let user_exactlyPath:[Character] = ["A","u","d","i","o","M","s","g"]

/*: "content" :*/
fileprivate let kSubId:String = "corunterunt"

/*: "TxtMsg" :*/
fileprivate let constAgainstValue:String = "TxtMsgautomatic color mystery"

/*: "message" :*/
fileprivate let show_buildUrl:[Character] = ["m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let constVisualId:[Character] = ["q","u","o","t","e","M","s"]
fileprivate let appConductAnalyzeKey:String = "give like double retgId"

/*: "quoteMsgInfo" :*/
fileprivate let userNoticeData:[Character] = ["q","u","o","t","e","M","s","g","I","n","f","o"]

/*: "rYMsgType" :*/
fileprivate let k_exactlyAgainstTitle:[Character] = ["r","Y","M","s","g","T","y","p","e"]

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let show_domainName:[Character] = ["G","J",":","S","e","s"]
fileprivate let constProcessorUrl:String = "selection"
fileprivate let dataLateGoingFormat:String = ":Autchild someone me up"
fileprivate let showVariationMsg:String = "et:Txservice convey certain processing"

/*: "changeDirection" :*/
fileprivate let show_nextName:String = "cpostan"
fileprivate let app_tunName:String = "ctiworker"
fileprivate let mainSmallSucceedValue:[Character] = ["n"]

/*: "system" :*/
fileprivate let constMaleAnswerEqualFormat:[Character] = ["s","y","s","t","e","m"]

/*: "title" :*/
fileprivate let user_donationId:[Character] = ["t","i","t","l","e"]

/*: "icon" :*/
fileprivate let const_proposePath:String = "ichello"

/*: "all" :*/
fileprivate let data_victoryMessage:String = "shll"

/*: "icon_chats_pcr" :*/
fileprivate let kSpecContent:String = "icon_process lead lack tone"

/*: "status" :*/
fileprivate let k_likeShiftKey:String = "sapproximatetus"

/*: "msgIncome" :*/
fileprivate let kQualifyStr:String = "msgIncotolerance pose pan layer street"
fileprivate let showAttributePath:[Character] = ["m","e"]

/*: "msgId" :*/
fileprivate let constFlowId:[Character] = ["m","s","g","I","d"]

/*: "StillValueConvertible" :*/
fileprivate let showSeeTitle:[Character] = ["T","M","s","g","T","r"]
fileprivate let appCalculationMsg:String = "AN"
fileprivate let app_birthdayFinishPath:[Character] = ["s","l","a","t","e","S","t","a","t","u","s"]

/*: "TMsgTranslatedContent" :*/
fileprivate let main_arcMessage:[Character] = ["T","M","s","g","T","r","a","n","s","l","a","t","e","d","C","o","n"]
fileprivate let data_processUrl:String = "TENT"

/*: "formatType" :*/
fileprivate let constHuntingMessage:[Character] = ["f","o","r","m","a","t","T","y","p","e"]

/*: "plain" :*/
fileprivate let userCommunicateMessage:[Character] = ["p","l","a","i","n"]

/*: "imageData" :*/
fileprivate let noti_mName:[Character] = ["i"]
fileprivate let dataRespectPath:[Character] = ["m","a","g","e","D","a","t","a"]

/*: "VideoMsg" :*/
fileprivate let mainMomentName:[Character] = ["V","i","d","e","o","M","s","g"]

/*: "match" :*/
fileprivate let app_lockMessage:String = "matcalculation"

/*: "userIntro" :*/
fileprivate let notiHaveFormat:String = "singlee"
fileprivate let app_stockAttentionTitle:[Character] = ["r","I","n","t","r","o"]

/*: "targetId" :*/
fileprivate let notiIssueMsg:[Character] = ["t","a","r","g","e","t","I"]
fileprivate let kHighlightId:[Character] = ["d"]

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let data_sweetKey:String = "yyyy-audience pretty attention temp"
fileprivate let show_dailyDataValue:[Character] = ["H","H",":","m","m",":","s","s"]

/*: "sendTime" :*/
fileprivate let show_medicationTitle:[Character] = ["s","e","n","d","T","i","m","e"]

/*: "isRead" :*/
fileprivate let app_absoluteStr:String = "none along male albumisRead"

/*: "audioData" :*/
fileprivate let main_lockIndexId:String = "ahoneyd"
fileprivate let appLiftStr:String = "ioDatai under check nut"

/*: "audioUri" :*/
fileprivate let data_foundationMessage:[Character] = ["a","u","d","i","o","U","r","i"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ChatManager.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum AidHashable: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - ChatManager

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class ChatManager: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func month() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> TriggerCellData? {
    public class func evaluation(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> TriggerCellData? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = selectionTip(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: AssistantV2Listener.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            AssistantV2Listener.shared.ratingLess(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> TriggerCellData? {
    public class func selectionTip(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> TriggerCellData? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(k_outputFormat))] = (showRunningShapePath.lowercased())
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(showBlockMessage.replacingOccurrences(of: "still", with: "ex")): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = throwWeightData(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = LovelyCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = OrganModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? SumerventionModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(appLeadingMsg.suffix(5)) + String(data_runMessage))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(String(constClubFormat))] = [
            //: "uid": MentionTriggerAppManager.share.loginUserMode.userID,
            (noti_elementFormat.lowercased()): MentionTriggerAppManager.share.loginUserMode.userID,
            //: "nickname": MentionTriggerAppManager.share.loginUserMode.nickname,
            String(bytes: show_trailValue.map{tweedledeeAndTweedledum(hidden: $0)}, encoding: .utf8)!: MentionTriggerAppManager.share.loginUserMode.nickname,
            //: "headPic": MentionTriggerAppManager.share.loginUserMode.headPic,
            String(bytes: showCaptureText.map{hormoneMid(become: $0)}, encoding: .utf8)!: MentionTriggerAppManager.share.loginUserMode.headPic,
            //: "age": MentionTriggerAppManager.share.loginUserMode.age,
            String(bytes: userProvideData.map{$0^31}, encoding: .utf8)!: MentionTriggerAppManager.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(showBlockMessage.replacingOccurrences(of: "still", with: "ex")): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = throwWeightData(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(k_outputFormat))] as? String == (kConcreteValue.replacingOccurrences(of: "topic", with: "t")) {
            //: var data: TriggerCellData
            var data: TriggerCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(user_financialMessage.suffix(7)))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = AcquisitionReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = CounterReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = CounterReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? OrganModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(k_outputFormat))] as? String == (String(notiStrategyKey.prefix(5)) + String(constBulletEndPath)) {
            //: let data = OutsideCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = OutsideCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? OrganModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(k_outputFormat))] as? String == (String(userJungleBuryPath)) {
            //: var data: TriggerCellData
            var data: TriggerCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(user_financialMessage.suffix(7)))] as? String == "1" { // 群聊
                //: data = SimplyReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = SimplyReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = ValuationThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ValuationThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? OrganModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(k_outputFormat))] as? String == (String(userFollowingUrl)) {
            //: let data = MezuzahCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = MezuzahCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? OrganModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(k_outputFormat))] as? String == (const_exhibitName.replacingOccurrences(of: "cycle", with: "o")) {
            //: let data = CellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = CellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? OrganModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(k_outputFormat))] as? String == (String(noti_termsUrl)) {
            //: let data = WritingThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = WritingThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? OrganModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(kConcreteValue.replacingOccurrences(of: "topic", with: "t"))] != nil && (msgInfo[(show_capacityUrl.lowercased() + String(noti_brushValue))] as? Int) == 4 {
            //: let data = ValuationThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ValuationThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? OrganModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: TriggerCellData) -> TriggerCellData? {
    public class func prepareWho(withData cellData: TriggerCellData) -> TriggerCellData? {
        // 图片
        //: if let reCellData = cellData as? MezuzahCellData {
        if let reCellData = cellData as? MezuzahCellData {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(EverlastinglyReactiveCompatible.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(EverlastinglyReactiveCompatible.subsume())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(notiBroadcastMakeupValue.replacingOccurrences(of: "gray", with: "n") + String(noti_mainCostAlivePath))] = (String(appTotalroMessage.prefix(6)))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(k_outputFormat))] = (String(userFollowingUrl))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(String(const_interruptKey))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(appLeadingMsg.suffix(5)) + String(data_runMessage))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(dataBrideBehaviorUrl.replacingOccurrences(of: "target", with: "t"))] = (String(appTotalroMessage.prefix(6)))
            //: params["pic"] = filePath
            params[(String(noti_headValue))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(noti_markServeData.prefix(6)))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(noti_markServeData.prefix(6)))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(noti_militaryFormat.suffix(6)))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(noti_militaryFormat.suffix(6)))] = "0"
            }

            //: dic["params"] = params
            dic[(String(showMustData))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = selectionTip(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? CellData {
        if let reCellData = cellData as? CellData {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? WritingThen {
        if let reCellData = cellData as? WritingThen {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = BoundPackInfoManager.option(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(showThresholdReplaceKey.lowercased() + constChemistryPath.replacingOccurrences(of: "dimension", with: "i") + "Length")] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(mainWhetherName.replacingOccurrences(of: "estimated", with: "n"))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(String(show_containMsg))] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(String(show_containMsg))] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(k_outputFormat))] = (String(noti_termsUrl))
            //: msgInfo["senderId"] = MentionTriggerAppManager.share.loginUserMode.userID
            msgInfo[(app_ehMovieKey.replacingOccurrences(of: "universal", with: "se") + String(userCapablePath))] = MentionTriggerAppManager.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(appLeadingMsg.suffix(5)) + String(data_runMessage))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(dataBrideBehaviorUrl.replacingOccurrences(of: "target", with: "t"))] = (String(user_exactlyPath))
            //: dic["params"] = params
            dic[(String(showMustData))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = selectionTip(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? ValuationThen {
        if let reCellData = cellData as? ValuationThen {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(kSubId.replacingOccurrences(of: "run", with: "n"))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(k_outputFormat))] = (String(userJungleBuryPath))
            //: dic["msgInfo"] = msgInfo
            dic[(String(appLeadingMsg.suffix(5)) + String(data_runMessage))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(dataBrideBehaviorUrl.replacingOccurrences(of: "target", with: "t"))] = (String(constAgainstValue.prefix(6)))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(String(show_buildUrl))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.file() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(String(constVisualId) + String(appConductAnalyzeKey.suffix(3)))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(userNoticeData))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: dic["params"] = params
            dic[(String(showMustData))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = selectionTip(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> TriggerCellData? {
    public class func transferAccess(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> TriggerCellData? {
        //: var cellData: TriggerCellData?
        var cellData: TriggerCellData?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(showBlockMessage.replacingOccurrences(of: "still", with: "ex"))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(appLeadingMsg.suffix(5)) + String(data_runMessage))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(String(constClubFormat))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? OrganModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == MentionTriggerAppManager.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == MentionTriggerAppManager.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == MentionTriggerAppManager.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == MentionTriggerAppManager.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(k_exactlyAgainstTitle))] as? String == (String(show_domainName) + constProcessorUrl.replacingOccurrences(of: "selection", with: "s") + String(dataLateGoingFormat.prefix(4)) + "oGre" + String(showVariationMsg.prefix(5)) + "tCustom") {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(show_nextName.replacingOccurrences(of: "post", with: "h") + "geDire" + app_tunName.replacingOccurrences(of: "worker", with: "o") + String(mainSmallSucceedValue))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = explainTelephoneMessage(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(k_outputFormat))] as? String == (String(constMaleAnswerEqualFormat)) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = OrganModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? SumerventionModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = LovelyCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(kSubId.replacingOccurrences(of: "run", with: "n"))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = MezuzahCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = MezuzahCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = CellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = CellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = AcquisitionReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = CounterReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = CounterReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = basic(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(showBlockMessage.replacingOccurrences(of: "still", with: "ex"))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(appLeadingMsg.suffix(5)) + String(data_runMessage))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? ConfirmationModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = OutsideCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = OutsideCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = WritingThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = WritingThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? WritingThen {
            if let audioCellData = cellData as? WritingThen {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = strengthManager(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = WeightMsgTable.recordingLess(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = WeightMsgTable.belowQuality(visible(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = AriseCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(String(user_donationId))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(kSubId.replacingOccurrences(of: "run", with: "n"))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(String(userFollowingUrl))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ToothCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = SimplyReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = SimplyReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = ValuationThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ValuationThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? EnableingModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(const_proposePath.replacingOccurrences(of: "hello", with: "on"))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: MentionTriggerAppManager.share.loginUserMode.headPic ?? ""
                        MentionTriggerAppManager.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = ConfirmationModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(kSubId.replacingOccurrences(of: "run", with: "n"))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(showBlockMessage.replacingOccurrences(of: "still", with: "ex"))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = PersonalModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (data_victoryMessage.replacingOccurrences(of: "sh", with: "a"))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(kSpecContent.prefix(5)) + "chats_pcr")
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.centralism(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(k_likeShiftKey.replacingOccurrences(of: "approximate", with: "ta"))] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(kQualifyStr.prefix(7)) + String(showAttributePath))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(show_capacityUrl.lowercased() + String(noti_brushValue))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(constFlowId))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(showSeeTitle) + appCalculationMsg.lowercased() + String(app_birthdayFinishPath))] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = StillValueConvertible(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(main_arcMessage) + data_processUrl.lowercased())] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = ManyModel.atStatus()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = ManyModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = ChatEveryoneModel.stateSupportedRaftExpected()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = ChatEveryoneModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = RedModel.nearSend()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func explainTelephoneMessage(_ extraDic: [String: Any]) -> AidHashable {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(showBlockMessage.replacingOccurrences(of: "still", with: "ex"))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(appLeadingMsg.suffix(5)) + String(data_runMessage))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? OrganModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(String(constHuntingMessage))] as? String == (String(userCommunicateMessage)) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(String(const_interruptKey))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(String(noti_mName) + String(dataRespectPath))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(mainMomentName)) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = calculate(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(app_lockMessage.replacingOccurrences(of: "calculation", with: "ch"))] as? Int) == 1 || (msgInfo?[(app_lockMessage.replacingOccurrences(of: "calculation", with: "ch"))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(k_outputFormat))] as? String == (showRunningShapePath.lowercased()) || (dic?[(show_capacityUrl.lowercased() + String(noti_brushValue))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(String(constHuntingMessage))] as? String == "mf") && ((dic?[(show_capacityUrl.lowercased() + String(noti_brushValue))] as? Int) == 15 || (dic?[(show_capacityUrl.lowercased() + String(noti_brushValue))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(k_outputFormat))] as? String == (notiHaveFormat.replacingOccurrences(of: "single", with: "us") + String(app_stockAttentionTitle)) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(k_outputFormat))] as? String == (String(notiStrategyKey.prefix(5)) + String(constBulletEndPath)) || (msgInfo?[(show_capacityUrl.lowercased() + String(noti_brushValue))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func visible(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.centralism(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(showBlockMessage.replacingOccurrences(of: "still", with: "ex"))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(appLeadingMsg.suffix(5)) + String(data_runMessage))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(String(constClubFormat))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(app_ehMovieKey.replacingOccurrences(of: "universal", with: "se") + String(userCapablePath))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == MentionTriggerAppManager.share.loginUserMode.userID {
        if senderId == MentionTriggerAppManager.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(app_ehMovieKey.replacingOccurrences(of: "universal", with: "se") + String(userCapablePath))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(String(notiIssueMsg) + String(kHighlightId))] = message.userID ?? (user?[(String(notiIssueMsg) + String(kHighlightId))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(app_ehMovieKey.replacingOccurrences(of: "universal", with: "se") + String(userCapablePath))] = "\(msgInfo?[(noti_elementFormat.lowercased())] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(notiIssueMsg) + String(kHighlightId))] = "\(msgInfo?[(noti_elementFormat.lowercased())] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(constFlowId))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(constFlowId))] = "\(msgInfo?[(String(constFlowId))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.centralism(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(constFlowId))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(constFlowId))] = "\(customDic[(String(constFlowId))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(constFlowId))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((String(show_containMsg))) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(String(show_containMsg))] = "\(msgInfo?[(String(show_containMsg))] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((showThresholdReplaceKey.lowercased() + constChemistryPath.replacingOccurrences(of: "dimension", with: "i") + "Length")) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(showThresholdReplaceKey.lowercased() + constChemistryPath.replacingOccurrences(of: "dimension", with: "i") + "Length")] = "\(msgInfo?[(showThresholdReplaceKey.lowercased() + constChemistryPath.replacingOccurrences(of: "dimension", with: "i") + "Length")] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (String(data_sweetKey.prefix(5)) + "MM-dd " + String(show_dailyDataValue))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(show_medicationTitle))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == MentionTriggerAppManager.share.loginUserMode.userID ? "1" : "0"
        info?[(String(app_absoluteStr.suffix(6)))] = senderId == MentionTriggerAppManager.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func strengthManager(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = visible(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(constFlowId))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func basic(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func throwWeightData(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func calculate(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(k_outputFormat))] as? String, msgType == (String(noti_termsUrl)) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(notiBroadcastMakeupValue.replacingOccurrences(of: "gray", with: "n") + String(noti_mainCostAlivePath))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(user_exactlyPath))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(main_lockIndexId.replacingOccurrences(of: "honey", with: "u") + String(appLiftStr.prefix(6)))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(String(data_foundationMessage))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
