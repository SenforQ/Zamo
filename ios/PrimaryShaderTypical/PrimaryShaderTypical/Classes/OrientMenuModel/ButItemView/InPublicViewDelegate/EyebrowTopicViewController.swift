
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let main_logPassId:[UInt8] = [0x27,0x20,0x27,0x3a,0x66,0x2d,0x21,0x2a,0x2b,0x3c,0x74,0x67,0x6e,0x26,0x2f,0x3d,0x6e,0x20,0x21,0x3a,0x6e,0x2c,0x2b,0x2b,0x20,0x6e,0x27,0x23,0x3e,0x22,0x2b,0x23,0x2b,0x20,0x3a,0x2b,0x2a]

private func blueishCookie(replace num: UInt8) -> UInt8 {
    return num ^ 78
}

/*: "uid" :*/
fileprivate let showPackKey:[UInt8] = [0x43,0x5f,0x52]

private func jobBegin(clean num: UInt8) -> UInt8 {
    return num ^ 54
}

/*: "headPic" :*/
fileprivate let user_buryMsg:String = "will place termsheadPic"

/*: "nickname" :*/
fileprivate let app_limitFormat:String = "surei"
fileprivate let const_judgeKey:String = "cknamparent"

/*: "birthday" :*/
fileprivate let show_laterPath:String = "BIRTH"
fileprivate let userMagnitudeKey:[Character] = ["d","a","y"]

/*: "videoUrl" :*/
fileprivate let appCorrectId:[Character] = ["v","i","d","e","o","U"]
fileprivate let data_postValue:[Character] = ["r","l"]

/*: "location" :*/
fileprivate let appDimensionReduceText:String = "LOCATIO"
fileprivate let const_redLoseAfterTitle:String = "never"

/*: "cmd" :*/
fileprivate let main_bulbText:[UInt8] = [0x5f,0x51,0x58]

/*: "refuseCall" :*/
fileprivate let constPrepareDailyFormat:String = "rcomprehensivefus"
fileprivate let data_adminName:String = "jaw"

/*: "data" :*/
fileprivate let noti_standardUrl:[UInt8] = [0xf2,0xf7,0xe2,0xf7]

private func whoCanvas(pow num: UInt8) -> UInt8 {
    return num ^ 150
}

/*: "Call interruption" :*/
fileprivate let k_layLoopTitle:String = "hung far maybe say sayCall "
fileprivate let constCarryMsg:String = "rrusurgerytion"

/*: "stopCall" :*/
fileprivate let app_voiceKey:String = "average"
fileprivate let userExploreEarlyData:String = "depth icon first healthytopCall"

/*: "logId" :*/
fileprivate let dataPublishValue:[UInt8] = [0x2c,0x2f,0x27,0x9,0x24]

private func averageEventAdded(load num: UInt8) -> UInt8 {
    return num ^ 64
}

/*: "cancelCall" :*/
fileprivate let app_feeText:String = "carrierncel"
fileprivate let showOffdMsg:String = "stick thread mouth alongCall"

/*: "No answer" :*/
fileprivate let dataRugValue:String = "No answfresh protection disappear people others"
fileprivate let user_speciesValue:[Character] = ["e","r"]

/*: "duration" :*/
fileprivate let const_alongsideValue:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let constAudienceName:String = "inside text narrowShe h"
fileprivate let showRapHalfPreviousName:String = "as hincident recommendation steal reflection"

/*: "cancelSex" :*/
fileprivate let mainComplyMessage:[Character] = ["c","a","n","c","e","l","S","e","x"]

/*: "He has hung up" :*/
fileprivate let data_purchaseUrl:[Character] = ["H","e"," ","h","a","s"," ","h"]
fileprivate let appUsedWedName:String = "server less reinung up"

/*: "endType" :*/
fileprivate let main_identifyText:String = "endTypesight native"

/*: "stopSex" :*/
fileprivate let main_bubbleConveyMessage:String = "stopSexbetween recognize board destroy"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let appPartnerMessage:[UInt8] = [0x2a,0xd,0x10,0x16,0x5,0x5,0xa,0x0,0xa,0x6,0xd,0x17,0x43,0x4,0xc,0xf,0x7,0x43,0x0,0xc,0xa,0xd,0x10,0x4f,0x43,0x0,0x2,0xf,0xf,0x43,0xb,0x2,0x10,0x43,0x6,0xd,0x7,0x6,0x7]

private func tempArc(we num: UInt8) -> UInt8 {
    return num ^ 99
}

/*: "The other hung up the phone" :*/
fileprivate let const_groupCalculateName:String = "The othsituation mount destination late menu"
fileprivate let userOppositeTitle:String = "coat crosser h"
fileprivate let showAdministrativeMsg:String = "video nop the"

/*: "type" :*/
fileprivate let data_chinFormat:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let const_reportUrl:[Character] = ["j","u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let const_containerFormat:String = "awake conversation disk alreadyCancel"

/*: "Get gold coins" :*/
fileprivate let noti_drawMsg:String = "Get gmedal remain hair invisible currency"
fileprivate let constSecurityData:String = "oiraw"

/*: "addBean" :*/
fileprivate let dataStepName:[Character] = ["a","d","d","B","e","a"]
fileprivate let data_othersMsg:[Character] = ["n"]

/*: "refuseSex" :*/
fileprivate let mainBeamKey:String = "reend"
fileprivate let constAirKey:String = "of form next pretty canvasuseSex"

/*: "onAcceptCall" :*/
fileprivate let show_kingStr:[Character] = ["o","n","A","c","c"]
fileprivate let constConductName:[Character] = ["e","p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let mainListenerFormat:String = "adealdeal"
fileprivate let main_situationId:String = "answer matter tone down thumbeptCall"

/*: "Calls are cancelled" :*/
fileprivate let data_scienceTheoreticalMessage:String = "Callscut mention say"
fileprivate let noti_literalStr:[Character] = ["n","c","e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let userTopicData:[Character] = ["T","h","e"," ","c"]
fileprivate let appEmptyBringMsg:String = "accommodate box load presentationall ha"
fileprivate let app_braceTitle:String = "s endedya hang snap"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EyebrowTopicViewController.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let mainOutputContent = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class EyebrowTopicViewController: EverlastinglyThen {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: LovelyChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: SlowReactiveCompatible? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: MysteryMiniView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        DisagreeableWomanSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        DisagreeableWomanSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        DisagreeableWomanSocketManager.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: main_logPassId.map{blueishCookie(replace: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! HomeControllerDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.keepEnter()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            operationAcross()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currAccessibleVC = nil
            DisagreeableWomanSocketManager.shared.currAccessibleVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.justData()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        nameureFollowing()
        //: setupSubViewsConstraint()
        outsideOwner()
        //: initNotificationObserver()
        obnubilateObserver()
        //: req_callGetUserInfo()
        analyse()
        //: timeoutRules()
        thresholdRules()
        //: resumeTalk()
        nightBy()
        //: setUpBGMPlayer()
        popularOrigin()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if MentionTriggerAppManager.share.appConfigMode.disableShootScreen {
            if MentionTriggerAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = RateShieldView(frame: CGRect(x: 0, y: 0, width: show_objectValue, height: constBlockIdentityContent))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.accessible = false
        DisagreeableWomanSocketManager.shared.accessible = false
        //: TalkingSocketManager.shared.isCalling = false
        DisagreeableWomanSocketManager.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.messageCombine()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: CommunicateReactiveCompatible = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = CommunicateReactiveCompatible(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: WayCloudDelegate = {
        //: let m = TalkingVoiceChatManager()
        let m = WayCloudDelegate()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension EyebrowTopicViewController {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func analyse() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        BondRatingCloudDelegate.completion(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: showPackKey.map{jobBegin(clean: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(user_buryMsg.suffix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(app_limitFormat.replacingOccurrences(of: "sure", with: "n") + const_judgeKey.replacingOccurrences(of: "parent", with: "e"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(show_laterPath.lowercased() + String(userMagnitudeKey))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(appCorrectId) + String(data_postValue))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(appDimensionReduceText.lowercased() + const_redLoseAfterTitle.replacingOccurrences(of: "never", with: "n"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.screenAmCapital()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension EyebrowTopicViewController {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func operationAcross() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        GatePermissionTool.extent(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.outBarMsg(showMsg: dataStatusKey)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                DisagreeableWomanSocketManager.shared.postAm(info: [String(bytes: main_bulbText.map{$0^60}, encoding: .utf8)!: (constPrepareDailyFormat.replacingOccurrences(of: "comprehensive", with: "e") + "eCal" + data_adminName.replacingOccurrences(of: "jaw", with: "l")), String(bytes: noti_standardUrl.map{whoCanvas(pow: $0)}, encoding: .utf8)!: [String(bytes: showPackKey.map{jobBegin(clean: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.completeRequest()
                //: return
                return
            }

            //: self.stopBGM()
            self.stillHome()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = SlowReactiveCompatible.connectionHistory(type: .Response)
            //: self.startBGM()
            self.keepEnter()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func messageCombine() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.stillHome()
        //: TalkingSocketManager.shared.callerDelegate = nil
        DisagreeableWomanSocketManager.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        DisagreeableWomanSocketManager.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        DisagreeableWomanSocketManager.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func completeRequest(animated: Bool = true) {
        //: self.stopBGM()
        self.stillHome()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func thatWith() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.successOutput(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func nightBy() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            thatWith()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension EyebrowTopicViewController {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func obnubilateObserver() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(evaluate(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(saveAcross(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: notiToId,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        mainOutputContent.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func evaluate(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            completeRequest()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func saveAcross(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard DisagreeableWomanSocketManager.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        DisagreeableWomanSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        DisagreeableWomanSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        DisagreeableWomanSocketManager.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.ain(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension EyebrowTopicViewController: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.outBarMsg(showMsg: (String(k_layLoopTitle.suffix(5)) + "inte" + constCarryMsg.replacingOccurrences(of: "surgery", with: "p")).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                DisagreeableWomanSocketManager.shared.postAm(info: [String(bytes: main_bulbText.map{$0^60}, encoding: .utf8)!: (app_voiceKey.replacingOccurrences(of: "average", with: "s") + String(userExploreEarlyData.suffix(7))), String(bytes: noti_standardUrl.map{whoCanvas(pow: $0)}, encoding: .utf8)!: [String(bytes: dataPublishValue.map{averageEventAdded(load: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                DisagreeableWomanSocketManager.shared.postAm(info: [String(bytes: main_bulbText.map{$0^60}, encoding: .utf8)!: (app_feeText.replacingOccurrences(of: "carrier", with: "ca") + String(showOffdMsg.suffix(4))), String(bytes: noti_standardUrl.map{whoCanvas(pow: $0)}, encoding: .utf8)!: [String(bytes: dataPublishValue.map{averageEventAdded(load: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                DisagreeableWomanSocketManager.shared.postAm(info: [String(bytes: main_bulbText.map{$0^60}, encoding: .utf8)!: (constPrepareDailyFormat.replacingOccurrences(of: "comprehensive", with: "e") + "eCal" + data_adminName.replacingOccurrences(of: "jaw", with: "l")), String(bytes: noti_standardUrl.map{whoCanvas(pow: $0)}, encoding: .utf8)!: [String(bytes: showPackKey.map{jobBegin(clean: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.slightWithStop()
                //: TalkingSocketManager.shared.currAccessibleVC = nil
                DisagreeableWomanSocketManager.shared.currAccessibleVC = nil
            }
            //: popCurrentViewController()
            completeRequest()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.analyze(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension EyebrowTopicViewController {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func popularOrigin() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? WouldColumnConvertible.Call : WouldColumnConvertible.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = SlowReactiveCompatible.connectionHistory(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func keepEnter() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.asDiscount()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func stillHome() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.salvage()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension EyebrowTopicViewController {
    /// 超时定时器
    //: private func timeoutRules() {
    private func thresholdRules() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? noti_saltAllKey : self.chatModel?.stateLine()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            contrastThe()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.contrastThe()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func contrastThe() {
        //: guard !TalkingSocketManager.shared.accessible else { return }
        guard !DisagreeableWomanSocketManager.shared.accessible else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.outBarMsg(showMsg: (String(dataRugValue.prefix(7)) + String(user_speciesValue)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: main_bulbText.map{$0^60}, encoding: .utf8)!: (app_feeText.replacingOccurrences(of: "carrier", with: "ca") + String(showOffdMsg.suffix(4))), String(bytes: noti_standardUrl.map{whoCanvas(pow: $0)}, encoding: .utf8)!: [String(bytes: dataPublishValue.map{averageEventAdded(load: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: main_bulbText.map{$0^60}, encoding: .utf8)!: (constPrepareDailyFormat.replacingOccurrences(of: "comprehensive", with: "e") + "eCal" + data_adminName.replacingOccurrences(of: "jaw", with: "l")), String(bytes: noti_standardUrl.map{whoCanvas(pow: $0)}, encoding: .utf8)!: [String(bytes: showPackKey.map{jobBegin(clean: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        DisagreeableWomanSocketManager.shared.postAm(info: socketDict)
        //: popCurrentViewController()
        completeRequest()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension EyebrowTopicViewController {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func exposureInclude(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: dataPublishValue.map{averageEventAdded(load: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(const_alongsideValue))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.refreshStartJumpOffClipTime(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.replaceFamily()
            //: createTalkRoom()
            thatWith()
            //: self.stopBGM()
            self.stillHome()
        }
    }
}

// MARK: - MaleObjectProtocol【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension EyebrowTopicViewController: MaleObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func socket(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(constAudienceName.suffix(5)) + String(showRapHalfPreviousName.prefix(4)) + "ung up").localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(mainComplyMessage))].stringValue == TopicKeyRepresentable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(data_purchaseUrl) + String(appUsedWedName.suffix(6))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.outBarMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.slightWithStop()
            //: TalkingSocketManager.shared.currAccessibleVC = nil
            DisagreeableWomanSocketManager.shared.currAccessibleVC = nil
        }

        //: popCurrentViewController()
        completeRequest()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func visitor(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: dataPublishValue.map{averageEventAdded(load: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.stillHome()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.replaceFamily()
        //: self.createTalkRoom()
        self.thatWith()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func top(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(main_identifyText.prefix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(constAudienceName.suffix(5)) + String(showRapHalfPreviousName.prefix(4)) + "ung up").localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(main_bubbleConveyMessage.prefix(7)))].stringValue == TopicKeyRepresentable.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(data_purchaseUrl) + String(appUsedWedName.suffix(6))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if MentionTriggerAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if MentionTriggerAppManager.share.appStatus == AidNameConvertible.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: appPartnerMessage.map{tempArc(we: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(const_groupCalculateName.prefix(7)) + String(userOppositeTitle.suffix(4)) + "ung u" + String(showAdministrativeMsg.suffix(5)) + " phone").localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.outBarMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.slightWithStop()
            //: TalkingSocketManager.shared.currAccessibleVC = nil
            DisagreeableWomanSocketManager.shared.currAccessibleVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.orientation()
        //: self.popCurrentViewController()
        self.completeRequest()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: dataProductionRouteKey, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func followHome(msg: String, data: [String: Any]) {
        //: guard MentionTriggerAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard MentionTriggerAppManager.share.appStatus == AidNameConvertible.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(data_chinFormat))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(const_reportUrl))] as? String else { return }
            //: let httpsUrl = MentionTriggerAppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = MentionTriggerAppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = RidConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: TalkingAppPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            RiseMalePushManager.share.errConfig(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            StandMarketThen.labelConfig(title: nil,
                                         //: message: msg,
                                         message: msg,
                                         //: leftBtnTitle: "Cancel".localized,
                                         leftBtnTitle: (String(const_containerFormat.suffix(6))).localized,
                                         //: rightBtnTitle: "Get gold coins".localized) {
                                         rightBtnTitle: (String(noti_drawMsg.prefix(5)) + "old c" + constSecurityData.replacingOccurrences(of: "raw", with: "ns")).localized)
            {
                //: TalkingAlertShow.hideAlert()
                StandMarketThen.alertCur()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAppPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                RiseMalePushManager.share.primed(clickEvent: userButtonPath, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func aboveSocket(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(dataStepName) + String(data_othersMsg))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.selfComponentDataConstituentPresent(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.sumerest(point: addBean)
        }
    }
}

// MARK: - EarthPartyDelegate【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension EyebrowTopicViewController: EarthPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func back(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(constAudienceName.suffix(5)) + String(showRapHalfPreviousName.prefix(4)) + "ung up").localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(mainBeamKey.replacingOccurrences(of: "end", with: "f") + String(constAirKey.suffix(6)))].stringValue == TopicKeyRepresentable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(data_purchaseUrl) + String(appUsedWedName.suffix(6))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.outBarMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.slightWithStop()
            //: TalkingSocketManager.shared.currAccessibleVC = nil
            DisagreeableWomanSocketManager.shared.currAccessibleVC = nil
        }
        //: popCurrentViewController()
        completeRequest()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func localEntitle(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: dataPublishValue.map{averageEventAdded(load: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.stillHome()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.replaceFamily()
        //: self.createTalkRoom()
        self.thatWith()
    }
}

// MARK: - SumerObjectProtocol【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension EyebrowTopicViewController: SumerObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func visual(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (constPrepareDailyFormat.replacingOccurrences(of: "comprehensive", with: "e") + "eCal" + data_adminName.replacingOccurrences(of: "jaw", with: "l")) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.outBarMsg(showMsg: (String(dataRugValue.prefix(7)) + String(user_speciesValue)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.slightWithStop()
                //: TalkingSocketManager.shared.currAccessibleVC = nil
                DisagreeableWomanSocketManager.shared.currAccessibleVC = nil
            }
            //: self.popCurrentViewController()
            self.completeRequest()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(show_kingStr) + String(constConductName)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard MentionTriggerAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard MentionTriggerAppManager.share.appStatus == AidNameConvertible.normal.rawValue else { return }
            //: TalkingAppPushManager.share.func__jumpToWebRecharge(sufficient: false)
            RiseMalePushManager.share.primed(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(show_kingStr) + String(constConductName)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.outBarMsg(showMsg: msg)
            //: self.popCurrentViewController()
            self.completeRequest()
        }
    }
}

// MARK: - ShotObjectProtocol【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension EyebrowTopicViewController: ShotObjectProtocol {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func birth() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        DisagreeableWomanSocketManager.shared.postAm(info: [String(bytes: main_bulbText.map{$0^60}, encoding: .utf8)!: (mainListenerFormat.replacingOccurrences(of: "deal", with: "c") + String(main_situationId.suffix(7))), String(bytes: noti_standardUrl.map{whoCanvas(pow: $0)}, encoding: .utf8)!: [String(bytes: showPackKey.map{jobBegin(clean: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func messageDoingNeed() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.outBarMsg(showMsg: (String(data_scienceTheoreticalMessage.prefix(5)) + " are ca" + String(noti_literalStr)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            DisagreeableWomanSocketManager.shared.postAm(info: [String(bytes: main_bulbText.map{$0^60}, encoding: .utf8)!: (app_feeText.replacingOccurrences(of: "carrier", with: "ca") + String(showOffdMsg.suffix(4))), String(bytes: noti_standardUrl.map{whoCanvas(pow: $0)}, encoding: .utf8)!: [String(bytes: dataPublishValue.map{averageEventAdded(load: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.outBarMsg(showMsg: (String(data_scienceTheoreticalMessage.prefix(5)) + " are ca" + String(noti_literalStr)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            DisagreeableWomanSocketManager.shared.postAm(info: [String(bytes: main_bulbText.map{$0^60}, encoding: .utf8)!: (constPrepareDailyFormat.replacingOccurrences(of: "comprehensive", with: "e") + "eCal" + data_adminName.replacingOccurrences(of: "jaw", with: "l")), String(bytes: noti_standardUrl.map{whoCanvas(pow: $0)}, encoding: .utf8)!: [String(bytes: showPackKey.map{jobBegin(clean: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.outBarMsg(showMsg: (String(userTopicData) + String(appEmptyBringMsg.suffix(6)) + String(app_braceTitle.prefix(7))).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            DisagreeableWomanSocketManager.shared.postAm(info: [String(bytes: main_bulbText.map{$0^60}, encoding: .utf8)!: (app_voiceKey.replacingOccurrences(of: "average", with: "s") + String(userExploreEarlyData.suffix(7))), String(bytes: noti_standardUrl.map{whoCanvas(pow: $0)}, encoding: .utf8)!: [String(bytes: dataPublishValue.map{averageEventAdded(load: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.orientation()
        //: self.popCurrentViewController()
        self.completeRequest()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func ingredientMute(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.fillMute(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func honeymooner(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.errWithHandsfree(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func missWindow() {
        //: TalkingSocketManager.shared.currAccessibleVC = self
        DisagreeableWomanSocketManager.shared.currAccessibleVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = MysteryMiniView.beyondView()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.transplant()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.waitButton()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.restore(talkingTime)
        //: popCurrentViewController()
        completeRequest()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func timePrice(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.restore(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func belowBirthday(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.meArr(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension EyebrowTopicViewController {
    /// 添加视图
    //: private func setupSubviews() {
    private func nameureFollowing() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func outsideOwner() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
